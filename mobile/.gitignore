node_modules/
.expo/
dist/
web-build/
*.log

# Never commit real Firebase native config or local env overrides.
google-services.json
GoogleService-Info.plist
.env.local
.env*.local

# Native project dirs are only generated by `expo prebuild` — this repo
# stays managed-workflow-first for now.
/ios/
/android/
