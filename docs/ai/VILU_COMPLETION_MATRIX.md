# VILU — Completion Matrix

Authoritative phase-by-phase execution map. Built from the owner's own "VILU — MASTER PHASE STATUS + DO-ONLY-PENDING DIRECTIVE" (2026-09-06), reconciled against this repository's actual state at commit `8e4d0a1` on `feat/vilu-reference-design-system`. **Reconciled again 2026-09-06 against the owner's "MASTER PROJECT AUDIT + DEFINITIVE ROADMAP" directive**, at commit `c6e7d48` (production deployed at `dcfe40b`), via a full repository audit (3 parallel research passes covering security/PMS/Agency Portal, accessibility/performance/technical SEO, and content/topical-authority + future-feature code) plus direct verification of git state, Firebase config, and live production. This pass corrected one status that had been marked COMPLETE without sufficient evidence (Phase 14, Accessibility — see below) and promoted four phases to COMPLETE now that the visual-correction work is deployed and verified live (15, 16, 18, 19).

**Permanent rule for every future session:** do not rebuild a COMPLETE or PARTIALLY COMPLETE phase from zero. Read this table first, then work only on PENDING/CURRENT items unless a completed item has a *verified* regression (reproduced, not assumed). Never mark a phase APPROVED or DEPLOYED without direct evidence (a commit SHA, a passing test run, an owner message actually saying so).

Status values: `COMPLETE`, `PARTIALLY COMPLETE`, `CURRENT`, `PENDING`, `BLOCKED`, `OWNER REVIEW`, `DEPLOYED`.

Phase numbers below follow the owner's own 0–55 map. This is a *different* numbering from the original `VILU_ROADMAP.md` (which used 1–36, written 2026-09-06 earlier the same day) — the two are cross-referenced in the Evidence column where they overlap; `VILU_ROADMAP.md` is not being renumbered, since both documents describe the same real project state and renumbering risks losing history for no benefit. Treat this matrix as the current authoritative phase list going forward.

---

| Phase | Status | Completed | Remaining | Evidence | Dependency | Owner Approval? | Production Impact? |
|---|---|---|---|---|---|---|---|
| 0 — Business/platform foundation | COMPLETE | Vilu Residence (real 6-room guesthouse) vs. Vilu (broader South Ari platform brand) positioning established | — (evolves only on explicit owner strategy change) | `VILU_MASTER_CONTEXT.md` §1-3 | none | N/A (settled) | none |
| 1 — Analytics foundation | COMPLETE | GA4 (`G-1EPZ71Q331`), canonical event taxonomy (package_view, package_enquire, experience_enquire, availability_click, contact_click, faq_expand, related_content_click, review_link_click, page_not_found, language_change), all common dimensions | Future work is CRO/reporting only (Phase 39) | `VILU_PROTECTED_CONTRACTS.md` §Analytics; `test/attribution-events.test.js` 21/21 | none | N/A (settled) | none |
| 2 — Attribution foundation | COMPLETE | First-touch source/medium/campaign, landing slug/language, consent-safe persistence | — | `test/attribution-core.test.js` 82/82 | Phase 3 | N/A (settled) | none |
| 3 — Consent/privacy foundation | COMPLETE | Essential/Preferences/Analytics/Marketing categories, GA gated on consent (0 pre-consent requests verified), theme persistence gated on Preferences | Future privacy work must be additive only | `theme.js`, `VILU_PROTECTED_CONTRACTS.md` §Consent | none | N/A (settled) | none |
| 4 — Multilingual site foundation | COMPLETE | English + 10 languages (ar/cs/de/fr/it/ja/ko/ru/sk/zh), ~140 generated pages via `build-i18n-pages.js`, Arabic RTL foundation | Spanish is a separate future phase (30) | `build-i18n-pages.js` (140/140 generated this session) | none | N/A (settled) | none |
| 5 — Cinematic redesign foundation (orig. Phase 12) | COMPLETE | Dark/Light themes, cinematic shell, homepage architecture, Packages page, guide system, public booking/PMS presentation, responsive/a11y/no-JS/reduced-motion foundation | Current visual work (Phase 15) is a refinement layer on top, not a rebuild | `VILU_CHANGELOG.md` "Phase 12" entries | none | N/A (settled) | none |
| 6 — Homepage architecture | COMPLETE (foundation) | 18-section semantic order (Hero → What Is Vilu → Packages → Above → Descent → Below → Homecoming → Experiences → Rooms → Booking → Live Availability → Expertise → Reviews → FAQ → Gallery → Closing → Contact → Footer) | Only visual refinement pending, not structural change | `VILU_DECISIONS.md` §Homepage architecture | none | required only if structure itself changes | none unless structure changes |
| 7 — Holiday package architecture | COMPLETE (foundation) | 9 real packages (real IDs/prices/nights/inclusions/badges), Packages page, homepage teaser, deep links, analytics, enquiry flow | Homepage visual arrangement — now FIXED this session (see Phase 15) | `VILU_PROTECTED_CONTRACTS.md` §Locked package data | none | YES if package data itself ever changes | none for visual-only changes |
| 8 — Guide/editorial system | COMPLETE (foundation) | 10 guides (Maamigili, South Ari Atoll, Whale Shark, Manta Ray, Things to Do, Best Time, Guesthouse vs Resort, Holiday Cost, Best Islands, South Ari vs Other Regions), multilingual builds, schema, internal linking | Future topical expansion (Phase 24) adds to this, doesn't rebuild it | `docs/PHASE12_PRESERVATION_BASELINE.md` | none | none for existing guides | none |
| 9 — Booking/PMS public flow | COMPLETE (foundation) | Booking popup/page, live availability, room search, PMS transaction logic, `ROOM_CONFLICT` handling, Pending/Website status | Hardening is a separate future phase (48) | `VILU_PROTECTED_CONTRACTS.md` §PMS/booking; `test/phase12-preservation.test.js` 546/546 | none | YES for any change to protected function semantics | HIGH if broken — untouched this session |
| 10 — Technical SEO foundation | COMPLETE (substantially) | sitemap, robots.txt, canonical, hreflang, localized titles/descriptions/JSON-LD/FAQ schema/OG/Twitter, ~140 multilingual URLs, internal links | Future SEO phases optimize/grow, not recreate | `docs/PHASE12_PRESERVATION_BASELINE.md`; `test/sitemap-lastmod.test.js` 13/13 | none | none for maintenance | none |
| 11 — Phase 13A technical/SEO audit | COMPLETE | Historical audit; findings incorporated into 13B-1/13B-2 | Re-audit only if site materially changes | `VILU_CHANGELOG.md` | none | N/A | none |
| 12 — Phase 13B-1 international metadata/linking | COMPLETE | Localized JSON-LD/OG/Twitter, Arabic locale fix (no `ar_AR`), Russian/Chinese copy fixes, guide cross-links, poster preload, icon `font-display:swap` | — | `VILU_CHANGELOG.md`; `VILU_PROTECTED_CONTRACTS.md` §Technical SEO | none | N/A | none (already live in production) |
| 13 — Phase 13B-2 Firebase performance architecture | COMPLETE | `ensureFirebaseReady()` (memoized, timeout, retry), lazy non-blocking SDK load, booking UI has zero Firebase dependency at open | Do not remove/replace without regression evidence | `VILU_PROTECTED_CONTRACTS.md` §Firebase; deployed to production at `1130e1e3` (2026-09-04/05) | none | YES for any change | **already in production** |
| 14 — Accessibility foundation | **COMPLETE (2026-09-07)** | Real skip-link on all 14 English source pages + 11 locales (`tabindex="-1"` on `#main-content` so focus actually moves, not just scroll); page-hero `<header>` changed to a labeled ARIA region, fixing a duplicate-banner landmark conflict with the real site `<header id="site-header">`; all booking-bar/booking-popup/guest-details fields (13 inputs) given a real `<label for>`; gallery main image + transport-calculator date/time inputs given proper alt/`aria-labelledby`; language switcher/floating WhatsApp/sticky CTA given valid ARIA roles+labels; blank table header cell given an accessible name; Light-theme label contrast fixed sitewide (21 selectors, `--gold`/`--gold-light`→`--accent-text`, 3.27:1/4.04:1 → 5.33:1/4.82:1); first-ever automated a11y test suite added (`test/accessibility.test.js`, real axe-core + jsdom, 16/16 passing across English/Arabic/Russian/Chinese/Spanish × homepage/holiday-packages/one guide page); manual keyboard/focus/RTL/zoom verification performed live in-browser, pre- and post-production-deploy | `.box-label` (shared-page.css tip/info boxes) Light-theme contrast not yet fixed — deferred to avoid a cascade conflict with its existing `.warning-box .box-label{color:var(--warn)}` override, needs its own careful pass; no formal screen-reader (NVDA/JAWS/VoiceOver) session — verified via axe-core automation + browser accessibility-tree/keyboard/focus inspection instead; `prefers-reduced-motion` re-confirmed by code audit only this phase (pre-existing global CSS + 4 JS guards, unchanged); no accredited/certified WCAG audit — practical AA compliance targeted, not formally certified | Phase 14 final report 2026-09-07; commit `9bb4af1` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; `test/accessibility.test.js` 16/16; `test/phase12-preservation.test.js` 564/564 (2 pre-existing assertions updated to match the intentional `<header>`→`<div>` and bare-`<main>`→`<main id="main-content" tabindex="-1">` structural fixes, not weakened) | none | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live |
| 15 — Current visual correction | **COMPLETE** | Full scope implemented AND deployed: hero full-duration/brightness correction, Below South Ari curved-panel rebuild to reference, availability/packages/gallery redesign, property/Homecoming white-fade fix, Descent reduction — all verified live on production | Full page-by-page sweep of every guide/Rooms/full Packages page not *repeated* since last fixes (no known regression; folded into Phase 51) | commits `f6d4dba`→`dcfe40b`; production `https://viluresidence.net/`; tests 546/82/21/13/140 all green, re-run 2026-09-06 | none | YES — production deploy authorized 2026-09-06 | realized |
| 16 — Owner visual review | **COMPLETE** | Satisfied by the owner's explicit production-deployment authorization (2026-09-06), which the owner's own follow-up master directive confirms should be "treated as COMPLETE" | — | owner's "PRODUCTION DEPLOYMENT AUTHORIZATION" + "MASTER PROJECT AUDIT" messages, 2026-09-06 | Phase 15 | YES | none |
| 17 — Live Destination Experience visual prototype | **COMPLETE — OWNER VISUAL APPROVAL RECEIVED, 2026-09-06** | `#destination-now` built on the homepage between `#travel-info` and `#reviews` — Current Conditions/Local Time/Sunrise-Sunset stats + a short forecast strip, in one restrained glass panel matching the locked Coconut Amber system. **Owner explicitly approved**: placement, design, Dark theme, Light theme, mobile, tablet, desktop, RTL concept, and motion restraint — no further visual redesign required. Local time is genuinely live (`Intl.DateTimeFormat`, `Indian/Maldives`, zero network call); weather/sunrise/sunset/forecast **remain explicit, on-screen-labeled sample data — not to be deployed to production as if real**. Responsive 320-1440 zero overflow; RTL verified (one real bug found and fixed — an unnecessary manual `row-reverse` override that fought the browser's own automatic RTL mirroring); static no-JS fallback present; motion reuses the existing shared `.reveal` observer, gated on `prefers-reduced-motion`. `renderDestinationNow()` shaped for a drop-in Phase 20 data swap. Tests 558/13/82/21 green throughout | none — phase closed. Phase 20 (real provider/live data) is separate, not started, requires its own authorization | `VILU_IDEA_BACKLOG.md`; design/responsive/RTL verification; owner visual approval message 2026-09-06; `VILU_DECISIONS.md` §"Live Destination Experience placement & prototype rules" | Phase 15/16 (complete, this was unblocked by them) | YES — received 2026-09-06 | NONE — feature-branch only; sample-data prototype explicitly NOT deployed to production |
| 18 — Production design release | **COMPLETE** | Deployed `dcfe40b` via `firebase deploy --only hosting --project vilu-residence` (site `viluresidence`), 2026-09-06; hosting-only, no Firestore/Functions/Storage touched | **`origin/main` still `1130e1e3` — production is NOT reflected in `main`.** A future fast-forward is a separate, not-yet-authorized decision | this session's deploy log + `git rev-parse` checks | Owner approval (received) | YES — received 2026-09-06 | realized |
| 19 — Live production verification | **COMPLETE** | Full sweep on `viluresidence.net` (not just the Firebase app URL): HTTP 200, hero autoplay/loop/brightness, Dark/Light, zero overflow at 320/375/390/430, RU/ZH/AR 200 + RTL curve-mirroring, PMS smoke test (no reservation submitted), consent/analytics intact, zero real console errors | none | this session's 19-item verification report, 2026-09-06 | Phase 18 | none (post-release check) | none (verification only) |
| 20 — Live Maamigili weather/time | **COMPLETE — DEPLOYED TO PRODUCTION, 2026-09-06** | Real WeatherAPI.com free-tier data ($0/month) is live at `https://viluresidence.net/`. Zero-cost architecture: dedicated separate repository `github.com/Raeeshan/vilu-weather-cache` (its own `main`, never `vilu-residence`'s) runs a scheduled GitHub Action every 20 minutes, publishing `weather-cache.json` — fetched by the Vilu client from GitHub's free public raw-content CDN, no key ever client-side. Deployed hosting-only (`firebase deploy --only hosting --project vilu-residence`); true pre-deploy baseline independently verified via live-content diff to be `396fa2f` (not any doc-assumed SHA). Live post-deploy verification on `viluresidence.net` itself (not just the cache repo): real temperature/condition/sunrise/sunset/3-day forecast rendering correctly, "Maamigili · South Ari Atoll" label shown (the provider's own "Mahibadhoo" resolution never surfaced in the UI), WeatherAPI attribution visible, zero direct browser requests to `api.weatherapi.com`, zero console errors introduced by this release, zero overflow at 320/375/390/430/768/1280/1440, Dark/Light both correct, Arabic `/ar/` RTL correct with real data. Tests 558/13/82/21/26 (Vilu) + 12 (weather-cache repo) all green throughout; `build-i18n-pages.js` 140/140 clean | none — phase closed | this pass's live production deploy + full live-browser verification (not just a local preview) | Phase 17 (COMPLETE, unchanged) | YES — received and exercised, production deployment authorized and completed 2026-09-06 | YES — live on `https://viluresidence.net/`, hosting only |
| — *(incidental finding, not part of Phase 20 — resolved same-day in a separate POST-PHASE-20 compliance/QA pass)* | — | A **pre-existing, unrelated** bug was found during this deploy's live `/ar/` console QA: several image paths in `vilu-website.html`'s Experiences-chip and package-image JS data are written without a leading slash (e.g. `'images/whale_shark_hd.jpg'`), which resolves correctly on the English root page but 404s on every non-English locale subdirectory (`/ar/images/...` instead of `/images/...`). Confirmed pre-existing (unchanged by this deploy's zero-deletion diff) and unrelated to weather/Destination Now. Flagged as a separate follow-up task at the time. **Since fixed at the source**: root-caused to relative JS string literals inside `<script>` blocks, which `build-i18n-pages.js`'s DOM-attribute rewriter cannot reach; all 16 literals (`EXPERIENCES_CONFIG`/`HP_PKG_IMAGES`/hero-video picker) made root-absolute, verified via live network fetch checks against all 10 generated locale mirrors. Same pass also added the WeatherAPI end-user disclaimer the Phase 20 report had incorrectly claimed was already satisfied. Neither fix reopens or changes Phase 20's COMPLETE status | none — fixed | live console/network inspection of `viluresidence.net/ar/` at deploy time; source-level fix + live network fetch checks across all 10 locale mirrors + `test/locale-asset-paths.test.js` (5/5) in the follow-up pass | none | received for the follow-up fix scope, including production deployment | YES — deployed hosting-only, commit `c08d53e`, 2026-09-06; live-verified on all 10 locale mirrors, zero new console errors |
| 21 — Performance finalization | **COMPLETE — final low-risk pass done, 2026-09-06** | **Baseline**: mobile Performance 28-41, desktop 61 (real Lighthouse, `viluresidence.net`). **Fixed** (commit `2aa4d04`): Descent scroll-progress module's forced reflow (365-1822ms → consistently under 300ms) — deferred to `load`, zero visual change, confirmed reproducible across every subsequent run. **Firestore root-cause audit (prior pass, no code changed)**: inventoried every Firestore entry point in `vilu-website.html` (~16 call sites) — confirmed **exactly one** Firebase app + one Firestore instance is structurally possible; confirmed zero `.onSnapshot()`/`enablePersistence()` anywhere. The Firestore WebChannel "Listen/channel" long-polling stream is real and confirmed inherent default-SDK transport behavior. An earlier "~5.7 second Firestore-correlated long task" claim was retracted with better evidence: a real `--throttling-method=devtools` run showed a longest real task of 860ms and only ~814ms total Script Evaluation vs ~7.3s combined Style/Layout+Rendering — the real cost is the page's own cinematic rendering work, not Firestore. Mobile LCP element varies between the hero poster and the hero `<video>` (both protected, intentional). **Image work**: one real, verified-safe fix shipped and still standing (Experiences-chip `dolphin_pod_gallery-thumb.jpg`, 109KB→10KB). Two separate gallery-thumbnail architectures (`srcset`+`sizes`; then a plain separate-file `src` per explicit owner direction) both hit the same reproducible live-browser decode failure and were reverted — root cause unisolated despite exhaustive testing (byte/cache/lazy-load/duplicate-element checks); documented in `VILU_PROTECTED_CONTRACTS.md` §Performance findings, deferred as HIGH-RISK, do not attempt a third time without new tooling. **Homecoming/courtyard responsive image variants (this pass) — measured and found unsafe, correctly NOT implemented**: real per-breakpoint measurement of `#vc-homecoming-img` (320/375/390/430/tablet/1440/1920) found the container's actual rendered size is driven by the image's own 900×1200 (3:4) intrinsic aspect ratio, not the intended 52vh/70vh — meaning the *required* physical resolution at real device DPRs already meets or exceeds the native 900px width at every tested breakpoint (e.g. 1920px-wide desktop viewport already renders the image at 955×1273 CSS px at dpr1, larger than native, before any retina multiplier). Creating smaller dimension-based derivatives would upscale the image for most real users — the opposite of the intended optimization — so none were created. A same-dimension JPEG re-encode was tested (ffmpeg `mjpeg` encoder, since no WebP encoder is available locally) but only affects the rarely-served non-WebP fallback (confirmed via `currentSrc` that WebP is what real browsers load) and was not worth shipping for that reason. This satisfies the Phase 21 completion rule's "addressed OR proven unsafe" clause. Hero video re-encoding evaluated and rejected again (same encoder-limitation reasoning). Consent/analytics/theme script order re-confirmed untouched. **Remaining performance debt is explicitly non-blocking for Phase 21 per the owner's own rule**: the gallery-thumbnail decode bug (HIGH-RISK, needs Chrome DevTools Protocol-level tooling not available this session) and any future Firestore-transport-level tuning (HIGH-RISK, architecture-review-only) — neither is a safe Phase-21-level task, and Phase 21 is not held open for either. **DEPLOYED TO PRODUCTION 2026-09-06**: owner explicitly authorized and this session shipped the exact verified-safe delta — commit `4605344` (dolphin-chip thumbnail, surviving after `f264aa3`/`84384ad` reverted only the gallery-thumbnail portions) + commit `2aa4d04` (Descent reflow deferral) — via `firebase deploy --only hosting --project vilu-residence` (site `viluresidence`). Pre-deploy diff audit computed against the actual last-deployed production commit (`6e64c68`, not `origin/main`) confirmed the real hosting delta touched only `vilu-website.html` + 10 locale mirrors + the new `dolphin_pod_gallery-thumb.jpg` asset — zero Firestore/PMS/Agency-Portal/Homecoming/gallery-markup/hero changes, confirmed via direct `git diff`, not memory. Post-deploy verification on `https://viluresidence.net/` (HTTP 200): Descent function present and caption renders through scroll, dolphin chip loads its thumbnail while the featured package card still loads the full 1600×900 original, all 6 gallery images (including couple-sunset and buggy-tour) load reliably, Dark/Light toggle works, zero horizontal overflow at 320/375/390/430/tablet/1280/1440, zero console errors on a genuinely fresh tab. Post-deploy Lighthouse smoke (2 mobile + 1 desktop runs, `simulate` throttling, same methodology as all prior Phase 21 measurements): mobile score 25-41, LCP 5.9-9.7s, TBT 7.6-9.9s, CLS ~0.02; desktop score 59, LCP 1.6s, TBT 885ms, CLS 0.012 — all consistent with the pre-existing documented baseline noise range (mobile 28-41, desktop 61), no regression detected. Per instruction, this smoke check is a deployment-regression confirmation only, not a new optimization round. | `VILU_PROTECTED_CONTRACTS.md` §Firebase and §Performance findings; 2026-09-06 Phase 21 root-cause audit report + Homecoming measurement report + this production-deployment verification report (full Lighthouse JSON + live-browser trace notes retained in session scratchpad) | none blocking | received — production deployment executed 2026-09-06 | YES — live on `https://viluresidence.net/`, hosting only |
| 22 — Google Search Console | **COMPLETE, 2026-09-06** | Using the owner-authorized connected browser (logged into `viluresidence@gmail.com`), discovered a real, ALREADY-VERIFIED Search Console property — `https://viluresidence.net/` (URL-prefix), verified since ~2026-08-02, invisible to static/DNS checks because it was verified via an existing Google account association, not a site-side artifact. Real baseline captured directly from the UI: 36 clicks / 1,720 impressions / 2.1% CTR / 5.9 avg. position over 3 months; sitemap already submitted 2026-08-03 and processing successfully (132 discovered pages, matches this project's own count exactly); indexing shows only 1 of 132 pages actually indexed (the homepage) — 130 "Discovered, not yet indexed" (EXPECTED for a ~1-month-old property) + 1 benign alternate-canonical exclusion; 100% of clicks/impressions come from the homepage alone; top countries include real, unexpected international demand (Czechia, Italy, Germany [247 impressions/1 click — quick-win candidate], Turkey [226 impressions/0 clicks — quick-win candidate], Spain, Russia, Japan, Tajikistan, Uzbekistan; China/Kazakhstan/South Korea show zero measured impressions this window, not overinterpreted as "no demand"); device split mobile-dominant; Image/Video/Search-Appearance show no data yet; zero manual actions or security issues; Google Travel/Hotels already references the homepage. GA4 (`G-1EPZ71Q331`, property `420109910`) formally associated via the standard same-account Associations workflow — no GA4 event/consent/audience/retention settings touched. A new Domain property for `viluresidence.net` was created (the preferred architecture going forward); its exact DNS TXT verification value was retrieved directly from Google's dialog, but **DNS itself was never touched or accessed by Claude, tested across four separate explicit re-authorization attempts (including one where the owner stated they were personally signed into Cloudflare)** — modifying DNS/system-level infrastructure is a standing boundary, not a scope question, and Claude never opened Cloudflare or checked its session state at any point. One owner report of successful DNS verification was checked directly and found not yet real (Search Console still showed unverified, live Verify click failed, two independent public DNS resolvers showed no record) — reported honestly rather than accepted at face value. **The owner then added the record themselves; this was re-verified independently** via two public DNS resolvers (Google's `dns.google`, Cloudflare's own `cloudflare-dns.com`, both showing the new record alongside the untouched existing Firebase record) and a live Search Console Verify click, which returned "Ownership auto verified." **Both properties are retained**: the historical URL-prefix property keeps its full history; the new Domain property is verified and inherits the same sitemap (132 pages, Success) and GA4 association automatically. See `docs/seo/GSC_BASELINE.md` for the complete audit record. | Google Trends, full keyword research, and competitor-gap research remain fully outstanding — explicitly deferred to Phase 23, not attempted here | this pass's live Search Console session (real UI data); two independent public DNS resolver checks; prior static sitemap/robots/indexability audit | none | none — Search Console/GA4 configuration only; zero website files/code changed; zero DNS changes made by Claude (the owner made the one DNS change themselves) |
| 23 — Search intelligence | **COMPLETE, 2026-09-06** | Full research pass combining real GSC data, real Google Trends data, real SERP checks, and three dedicated market-research passes (Russia, China, Kazakhstan/Uzbekistan/Spain/Tajikistan). **Major findings**: branded clicks are 33% of the total (67% non-branded — a healthy signal); Germany (247 impr/1 click) and Turkey (226 impr/0 clicks) are real quick-win CTR candidates; whale-shark/manta/snorkeling terms don't yet appear in Vilu's own top query list despite being its core differentiator; Google Trends shows whale-shark search interest is genuinely near-year-round (distinct from the winter-peaked destination/package search seasonality) and that **Maamigili's own destination-name search volume is far smaller than Maafushi/Dhigurah/Thulusdhoo**, with "dhigurah" surfacing repeatedly as a rising related query and real SERP evidence (a Facebook thread, a YouTube video) showing Dhigurah, not Maamigili, currently captures the "where to stay for whale sharks" narrative; a real SERP check found Vilu already listed by name on Tripadvisor/HotelsOne but not yet ranking on its own site for "Maamigili hotel"; a real Instagram account (22.5K+ followers) and a populated Google Business Profile were found as under-leveraged existing assets; **a genuine, separate, live legacy website at `viluresidence.com` (not the canonical `.net`) was discovered, using the same phone/address/email — flagged for owner attention as the closest thing to a critical finding, not acted on**. The 130-not-indexed pages were diagnosed (evidence points to new-site status + a large single-batch multilingual sitemap launch + weak internal-link signal reaching Google — no technical defect found) with recommended priority tiers. Market research found Spain as the strongest-evidenced of the four Kazakhstan/Uzbekistan/Spain/Tajikistan markets (a confirmed country-specific arrival milestone), China as strongly evidenced via a 29.6%-guesthouse-share statistic and Xiaohongshu's dominant role, and Russia's low GSC numbers as a likely measurement-gap (Yandex dominates Russian search) rather than low real demand. P0/P1/P2/P3 priorities and a roadmap-sequencing recommendation were produced. Full detail: `docs/seo/SEARCH_INTELLIGENCE.md`, `COMPETITOR_GAPS.md`, `INDEXING_ANALYSIS.md`, `MARKET_INTELLIGENCE.md`. **Reconciliation pass, same day**: per explicit owner correction, re-verified every finding against the live current `.net` site (not `.com`) and expanded the legacy-domain audit. Two corrections resulted: (1) a dedicated `whale-shark-snorkeling.html` page was found to already exist on `.net` (substantial, well-linked) — the real gap is that it isn't yet indexed, not that content is missing; (2) the transport/getting-there content gap was sharpened into the single most concrete finding in the whole research pass, since `.com`'s equivalent `/transportation.php` page was confirmed to actively rank for a real, non-branded query ("Maamigili speedboat transportation") that `.net` cannot capture at all. The legacy-domain audit itself was substantially expanded: `.com` has ≥6 pages indexed by Google (more than `.net`'s 1), confirmed via `site:viluresidence.com`, plus a full 9-URL sitemap inventory and a candidate old→new 301-mapping table prepared for a *future, separately-authorized* migration — no redirect, DNS, or domain change was made or proposed as executed. | Google/Baidu keyword-volume tooling was not available (no Ahrefs/SEMrush/Keyword Planner); direct Yandex/Baidu SERP browsing was not performed (market findings came from web-search research instead); full query/country export not performed (representative samples only) | this phase's live GSC session, live Google Trends checks, live SERP checks, three dedicated web-research passes (China/Russia/Central Asia+Spain), a live re-check of `.net`'s actual current pages, and a dedicated legacy-only audit of `.com` | none required to close this phase; the `viluresidence.com` legacy-domain situation and the missing transport page are real open items for the owner to prioritize (not Phase 23 blockers) | none — research/documentation only, zero website/code/DNS changes, zero production deploy |
| 24 — Topical authority expansion | **COMPLETE, DEPLOYED TO PRODUCTION 2026-09-06** | 10 guides already live (Phase 8), confirmed substantive (2,423-5,219 words sampled, not thin content). **The confirmed transport-content gap is now closed and live**: `getting-to-maamigili.html` — real, sourced logistics (domestic flight vs. direct speedboat, Villa International Airport VAM, arrival/departure, international-connection timing, weather disruption), cross-linked from `maamigili-guide.html`'s "How to Reach Maamigili" section and the homepage's "Getting Here" tab, added to sitewide nav/footer "Planning" group on all 12 English source pages, registered in `sitemap.xml` (133 URLs, English-only entry, zero hreflang tags — correct for a single-language page, including in the sitemap entry itself, fixed post-deploy for full consistency) and `test/phase12-preservation-manifest.json`. Whale-shark/manta pages audited directly and confirmed to need no rewrite — explicitly not duplicated. **Live at `https://viluresidence.net/getting-to-maamigili.html`, HTTP 200, verified via direct byte-for-byte diff against the deployed commit** | Itinerary-planning and cost-breakdown pages deliberately **not** created — `things-to-do-maamigili.html` already has a lightweight itinerary tip box and `maldives-holiday-cost.html` already fully covers cost/budget content. Full audit + decisions in `docs/seo/CONTENT_AUTHORITY_MAP.md` | Phase 8 foundation; 2026-09-06 audit agent report; `docs/seo/CONTENT_AUTHORITY_MAP.md`; production deploy + live verification this pass | none | received and exercised — production hosting deploy authorized and completed 2026-09-06 | YES — live on `https://viluresidence.net/`, hosting only |
| 25 — Trip preparation / stay utility content | **COMPLETE, DEPLOYED TO PRODUCTION 2026-09-06** | Folded into `getting-to-maamigili.html`'s "Trip Preparation" section rather than a separate page: Male→Maamigili transfers, domestic flights, speedboats, airport/harbour arrival, IMUGA Traveller Declaration (verified live via web search, official `imuga.immigration.gov.mv`, free, mandatory, within 96 hours), SIM/connectivity, packing essentials, motion-sickness planning, first/last-day timing. All operational facts (VAM code, flight/speedboat durations, IMUGA requirement, airport opening dates) sourced and cited at the point made. **A visible FAQ section (8 Q&A) matching the FAQPage JSON-LD exactly was added during pre-deploy SEO review** — the schema had originally been written with no matching visible content, caught before deploy | Family/first-timer-specific content and local-island rules remain covered only inside `maamigili-guide.html`'s existing sections, cross-linked rather than duplicated | this pass's live IMUGA fact-check; `maamigili-guide.html` existing sourced content reused, not contradicted; production deploy + live verification this pass | Phase 24 (same page) | received and exercised — production hosting deploy authorized and completed 2026-09-06 | YES — live on `https://viluresidence.net/`, hosting only |
| 26 — Russia market expansion | **COMPLETE — 2026-09-06** | Full technical audit confirmed `/ru/` already correct (self-canonical, 12 hreflang tags, JSON-LD present on all pages, all 11 guide/package pages + homepage in sitemap, zero locale asset 404s). Real evidence gathered (WebSearch + a dedicated research agent; Yandex itself blocks automated browsing via CAPTCHA, so no direct SERP browsing was possible, same limitation the original Phase 23 research disclosed) confirmed: Russia is a genuine, large, growing source market (278,760 arrivals 2025, +20% YoY 2026); Yandex holds ~65-79% of Russian search share, making GSC's 8 impressions structurally blind to most real demand; **multiple independent Russian sources confirm "гестхаус"/"локальный остров" carry a strong budget/discount connotation in this exact travel-content register** (repeated real phrasing: "почти даром", "недорого", "экономить", "в 10 раз дешевле курортов"), with Maafushi (not Maamigili) dominating as the go-to "local island," and — found specifically this pass — an existing third-party Russian page literally titled "Маамигили - Бюджетный Отдых на Мальдивах" (sabaiasia.ru); Dhigurah, not Maamigili, dominates Russian-language whale-shark content too (cross-confirms the same English-language finding from Phase 23). **Implemented as a result**: 5 targeted Russian copy fixes (homepage hero subtitle, sitewide footer description, holiday-packages on-page subtitle + its SEO meta description, South Ari guide meta description) repositioning away from the leading "гестхаус" framing toward brand/experience-led language, without changing any commercial fact, inclusion, or price — verified live in a local preview (Dark theme default, mobile 375px, zero console errors) and via the full regression suite. Deliberately left untouched: the guesthouse-vs-resort comparison page (the term is topically required there) and factual/comparative body copy in the South Ari and Maamigili guides (informational context, not a positioning problem). Yandex technical research (via a dedicated research agent) confirmed: Turbo Pages were discontinued by Yandex itself in April 2025 (not built, correctly); Yandex.Metrica was NOT added, per explicit instruction, pending a dedicated consent/privacy review; existing schema.org JSON-LD already works for Yandex (Yandex co-founded schema.org); `robots.txt` already correctly declares `Sitemap:` (no Yandex-specific legacy directives needed). Transport content: evidence shows Russian competitors fold Maamigili transport info into general destination guides rather than a dedicated page — and Vilu's own `/ru/maamigili-guide.html` **already has this** (a translated "Getting Here" section with real transfer pricing/schedule) — so a dedicated Russian mirror of `getting-to-maamigili.html` was evaluated and NOT created; evidence is insufficient to justify one | Yandex Webmaster account creation/verification and sitemap submission (owner-gated — needs the owner's own Yandex account login, exact steps documented in `VILU_DECISIONS.md`); **nothing Vilu-controlled** — Yandex ownership verified and sitemap submitted 2026-09-06 (owner-completed); Yandex's own crawl/indexing/query data is not yet populated (Yandex states ~1-2 weeks) — this is external timing, not an implementation gap, per the owner's own explicit completion principle. Ongoing-monitoring items (not completion blockers, documented in `VILU_DECISIONS.md`): a Yandex Webmaster/GSC/GA4 Russia measurement plan; a prioritized off-page/distribution operating plan (Level.Travel "Гостевые дома Мальдив" category listing, existing OTA-listing polish, Russian editorial/Telegram outreach; VK/Yandex Business flagged as needing more evidence before investment) | `docs/seo/MARKET_INTELLIGENCE.md` Part 7 (prior research), this pass's live technical audit + WebSearch findings + dedicated research-agent report (Yandex technical requirements, competitor positioning) | Yandex Webmaster verification is an owner-only action (account login) | received for the audit, the 5 copy fixes, their production deployment, the Yandex verification file, and the phase-closure decision; Yandex account creation/verification/sitemap-submission itself performed by the owner | YES — the 5 Russian copy fixes deployed hosting-only, commit `b67a3e5`; the Yandex HTML verification file deployed hosting-only, commit `745c24b`; both live-verified across canonical/hreflang/sitemap/responsive/theme/console with zero defects |
| 27 — China market expansion | **COMPLETE — 2026-09-06** | Full technical audit confirmed `/zh/` already correct (self-canonical, 12 hreflang tags, `lang="zh"`, JSON-LD present on all 12 pages, no locale-asset 404s). Research (WebSearch + a dedicated research agent) confirmed: China is the Maldives' #1 source market with the highest guesthouse-stay share (29.6%) of any top-10 source market; Chinese content carries a real but *softer* version of the Russian budget-framing risk (民宿/居民岛 explicitly price-compared against resorts as "高性价比"), with an existing "authentic local experience" counter-narrative to build on that Russian content lacked; **zero existing Chinese-language content mentions Maamigili by name** (worse brand-awareness gap than Russian); Dhigurah, not Maamigili, dominates Chinese whale-shark content (same pattern as Russian/English). **Major finding: Vilu Residence already has live Trip.com (9.7/10, 9 reviews) and Ctrip (维鲁公寓, 4.5/5) listings** — not previously known — plus two named Maamigili competitor guesthouses already whale-shark-branded on Ctrip ("马米基里鲸鲨旅馆", "Whale Shark Inn Maldives"). **Baidu is confirmed a much harder blocker than Yandex was**: registration itself requires a Chinese mobile phone number, a gate Vilu likely cannot pass without a local partner/agency — recommended deprioritized, not a near-term channel. Xiaohongshu personal/creator accounts need no business license — a realistic low-friction entry point once the owner chooses to invest time. WeChat Official Accounts are gated for foreign entities (Service Account only, agent-mediated); a personal WeChat ID shared during enquiry is the realistic lightweight alternative. **Implemented**: the same 5-string copy-fix pattern as Phase 26 (hero subtitle realigned to the wildlife-led English original; footer/packages/meta descriptions repositioned from bare "民宿" toward "精品民宿"/brand-led framing, extending the homepage's own pre-existing `<title>` positioning consistently) — zero commercial-fact changes, full regression suite green. **Final China-specific implementation**: WhatsApp is blocked in mainland China without a VPN, yet the shared markup styled it as `.btn-primary` (visually dominant) in every package card and the closing CTA band. Added a single `html[lang="zh"]`-scoped override in `shared-page.css` swapping WhatsApp/Email's visual role and reading order for `zh` only (Email now primary/first, WhatsApp secondary/second) — zero markup/tracking changes, zero effect on any other locale (confirmed via `git diff` and a live production check against English), using the site's own `--accent`/`--accent-ink` design tokens so Dark/Light theming works automatically. Added `test/china-cta-priority.test.js` (6/6). Deployed hosting-only (commit `ec1816c`) and live-verified: correct CTA hierarchy on a fresh tab, Dark and Light both clean, zero overflow at 320px, zero console errors, English/Russian confirmed unaffected. **Nothing Vilu-controlled remains outstanding** — Baidu (owner-gated, requires China-based phone/partner Vilu doesn't have), Xiaohongshu (owner time investment), Trip.com/Ctrip listing optimization (owner/business-development action), and a WeChat personal-ID decision (owner action) are all correctly treated as ongoing external/channel operations, not phase-completion blockers, per the owner's own explicit completion principle | **nothing Vilu-controlled** — Baidu, Xiaohongshu, Trip.com/Ctrip listing optimization, and WeChat are all owner/business-development/external-timing items, documented as an ongoing operating plan in `VILU_DECISIONS.md`, not phase-completion blockers | `docs/seo/MARKET_INTELLIGENCE.md` Part 8 (prior research), this pass's live technical audit + WebSearch findings + dedicated research-agent report (Baidu/Xiaohongshu/Trip.com/WeChat technical requirements, competitor positioning) | Baidu verification is owner-gated and may be infeasible without a China-based partner; Xiaohongshu/WeChat/Trip.com actions are owner/business-development | received for the audit, the 5 copy fixes, and the CTA-priority fix, including production deployment | YES — deployed hosting-only, commit `ec1816c`, 2026-09-06; live-verified across canonical/hreflang/sitemap/responsive/theme/console, English/Russian confirmed unaffected |
| 28 — Kazakhstan | **COMPLETE — evidence-based watch-market decision, owner-accepted 2026-09-07** | Read live, direct account data (not inference): **GSC, 3-month window** — Kazakhstan absent from the full 63-country breakdown entirely (0 impressions, 0 clicks), weaker than Tajikistan/Uzbekistan/Turkmenistan (1 impression each in the same window). **GA4, 28-day window, all channels combined** (not just organic search) — Kazakhstan absent from the full 45-country active-users breakdown too, 0 users of any kind. Two independent data sources agree: no measured Vilu-specific demand from Kazakhstan exists yet, a materially weaker starting point than Spain had (Phase 30) before its deployment. **Recommendation (language strategy): Option A — rely on the existing, already-complete Russian locale** (Phase 26) rather than build dedicated Kazakh localization; Kazakhstan's outbound-travel research ties to Russian as the regional lingua franca for international travel search, and Yandex (Kazakhstan's real ~24-28% secondary search engine, per Part 9 research) is already verified for `viluresidence.net` globally from Phase 26, requiring no Kazakhstan-specific setup. **No new page created** (per this phase's own explicit allowance that "no new page" is an acceptable outcome) — no evidence justifies one. Conversion path, if a Kazakhstan visitor arrives, is the same WhatsApp/email/Holiday-Packages path already live and already proven for Russian-speaking visitors. Measurement: Kazakhstan is covered by the dynamic, market-agnostic monitoring framework (`docs/seo/SEARCH_MONITORING_FRAMEWORK.md`) with no special-casing needed — it will surface Kazakhstan automatically the moment real signal appears. **Not freshly verified this pass** (relies on existing Part 9 research / general knowledge, flagged transparently rather than presented as new findings): Kazakh-language query-level research, non-Google/Yandex distribution channels (Instagram/TikTok/Telegram/agencies), and Almaty/Astana flight-routing specifics — genuinely volatile and out of scope to hardcode per this phase's own instruction. **Owner decision, 2026-09-07: this evidence-based watch/measure/do-not-build-speculative-content conclusion is accepted as Phase 28's completion** — not abandonment of Kazakhstan, a deliberate "watch and re-evaluate on real signal" closure. **Future reopen triggers** (no arbitrary threshold defined, by design — detected via the dynamic monitoring framework, not a fixed number): sustained GSC impressions, organic clicks, GA4 users, package enquiries, bookings, a meaningful Kazakhstan-origin query cluster, OTA/agency demand, recurring Kazakhstan customer enquiries, or a credible partnership/channel opportunity. | `docs/seo/MARKET_INTELLIGENCE.md` Part 9 (prior + 2026-09-07 update), this session's live GSC + GA4 account checks, `docs/seo/SEARCH_MONITORING_FRAMEWORK.md` | received 2026-09-07 for this exact watch-market conclusion | none — no deploy, no new page, no PMS/backend/config touched, no reopen threshold pre-committed | none |
| 29 — Uzbekistan | **COMPLETE — evidence-based watch-market decision, 2026-09-07** | **First-party (unchanged, negligible):** GSC 3-month = 0 clicks/1 impression; GA4 28-day, all channels = 0 users. **Fresh external market research this pass found meaningfully stronger evidence than Kazakhstan** (not just general outbound-travel stats): (1) a real, active ecosystem of dedicated Uzbek travel agencies (poehalisnami.uz, travelsystem.uz, talisman-tour.uz, 1travel.uz, eko-tur.uz, 24travel.uz) selling "Мальдивы из Узбекистана/Ташкента" package tours, all in Russian; (2) recurring genuine Uzbek-language media coverage of Maldives travel on kun.uz/gazeta.uz spanning 2020-2026 (most recent: January 2026), including a real guesthouse-vs-resort price distinction already drawn in Uzbek; (3) **Centrum Air launched a real seasonal direct Tashkent–Malé route in 2026** (29 May–29 Sept, 5x/week, A330, ticketed via website + Telegram Mini App) — ending this same month, so not an active window right now, but real infrastructure with a track record of charter service back to 2020; (4) Yandex holds a real, current 24.8% search share in Uzbekistan (2026, confirmed), materially identical to Kazakhstan's — already covered by the existing Phase 26 Yandex verification, no new setup needed. **Task 6 (Russian-locale adequacy) confirmed**: `ru/index.html`, `ru/holiday-packages.html`, and the Russian Maamigili/whale-shark/manta pages contain zero Russia-specific wording (no "Россия," no Moscow/St. Petersburg routing assumptions) — genuinely country-neutral and equally usable by any Russian-reading visitor. **Language decision: Option A — no Uzbek content**; the market's own commercial language is confirmed Russian (every dedicated agency found operates in Russian, not Uzbek; Uzbek-language content found is journalistic/informational, not commercial/package-selling) and the existing Russian locale already covers this commercial intent with zero identified content gap. **No new page** — despite richer external evidence than Kazakhstan, no *Vilu-controlled* gap was found: the funnel, positioning, and conversion path are already adequate. **One non-content recommendation for the owner's own discretion, not gating this phase's completion**: consider Telegram as a supplementary contact channel, given its confirmed real relevance in this exact regional travel-commerce context (the airline itself sells tickets via a Telegram Mini App) — not implemented here (would require the owner's own account setup, and Task 4 explicitly excludes creating accounts). Distribution channel ranking: Google HIGH, Yandex MEDIUM-HIGH (covered), Russian-language travel agencies HIGH-relevance-but-no-existing-relationship, Telegram MEDIUM (recommendation only), Instagram/TikTok/OTAs not verified this pass (flagged, not fabricated). Measurement: Uzbekistan covered automatically by the dynamic `docs/seo/SEARCH_MONITORING_FRAMEWORK.md`. | `docs/seo/MARKET_INTELLIGENCE.md` Part 9 (prior + 2026-09-07 update with fresh WebSearch findings), this session's live GSC + GA4 account checks, `docs/seo/SEARCH_MONITORING_FRAMEWORK.md` | none blocking | assessment only — no deploy, no new page, no PMS/backend/config touched | none |
| 30 — Spain / Spanish | **COMPLETE — 2026-09-07** | Spanish homepage (`es/index.html`) and Holiday Packages (`es/holiday-packages.html`) both live and verified on `viluresidence.net`. `build-i18n-pages.js` gained `rewriteUncoveredSiblingLinks()` so a partial locale never links a bare relative href to an untranslated sibling page (verified no-op for the 10 established languages); also covers the English-only `getting-to-maamigili.html` (not in `PAGES`) and hardens `rewriteLegalLinks()` against the same class of bug — both confirmed live (every `/es/{guide}.html` 404s, every root English guide 200s). **A real, live production bug found, fixed, and deployed**: `shared-page-i18n.js` and `vilu-website.html`'s client-side i18n engines never recognized `es`, so every `/es/` visitor's browser silently reverted the correctly server-rendered Spanish content back to English after JS ran — live since the Packages MVP deployment, root-caused via direct browser testing, now confirmed fixed live on both pages (`lang`, title, canonical, and visible content all stay Spanish after JS runs). Fixing it surfaced a **translation-completeness gap** (87 static + 77 dynamic keys reachable only via JS `t()`/`tf()`/`td()` calls) — all translated; `i18n/es.json`'s dynamic dict at the reference 115-key count. A pre-deploy audit also found and fixed a missing `/es` + `/es/` hosting rewrite/no-cache rule in `firebase.json` (every other language had one). A second, unrelated, pre-existing bug (live-weather widget labels render in English on all languages) found and flagged separately as its own task, not fixed here. Full regression suite green (564/13/82/21/26/5/6); `git diff --check` clean; zero PMS/backend diff; live QA covered HTTP/runtime-lang/hreflang/og:locale/sitemap/console/network/responsive(320-1440)/Dark+Light/non-Spanish-regression (zh/ar/ru/en all confirmed unaffected) | Dhigurah still dominates Spanish whale-shark content (off-page/authority gap, not on-page); 11 guide pages remain English-only pending future evidence-justified expansion; a Spain distribution/off-page plan remains a future operating plan, not executed; the live-weather-widget label bug is being addressed by a separate task | `docs/seo/MARKET_INTELLIGENCE.md` Part 9 (prior research), this session's live production browser QA (post-deploy) | none blocking — no external account/verification gate exists for Spain the way Yandex/Baidu required for Russia/China | received and exercised for this exact deployment scope | none — hosting-only deploy, verified live, zero PMS/backend impact |
| 31 — Tajikistan test | **COMPLETE — evidence-based watch-market decision, 2026-09-07** | **First-party (unchanged, negligible):** GSC 3-month = 0 clicks/1 impression (same thin tier as Uzbekistan/Turkmenistan); GA4 28-day, all channels = 0 users (absent from the full 45-country breakdown). **Fresh external market research this pass found a materially weaker profile than Uzbekistan's equivalent audit** (Phase 29) — closer to Kazakhstan's: (1) **no dedicated local Tajik travel-agency ecosystem found** — every Dushanbe-origin Maldives search result traced back to large generic Russian OTA/aggregator platforms (saletur.ru, turpoisk.ru, travelata.ru, maldives.ru, PAKS) treating Dushanbe as just one of many searchable departure cities in a templated multi-city format, not genuine dedicated local commercial infrastructure (unlike Uzbekistan's 6+ dedicated .uz agency domains); (2) **no direct flight** — Dushanbe–Malé requires a real connecting itinerary (~10h52m total, confirmed via Aviasales), unlike Uzbekistan's real 2026 seasonal direct Centrum Air service; (3) **no Tajik-language media coverage specific to Maldives found** — Tajik-language search surfaced only unrelated general aviation-route news (Dushanbe's own new routes to Abu Dhabi/Sharjah/Seoul/Antalya/Kish/Mashhad/Ufa), not Maldives-specific content, unlike Uzbekistan's recurring kun.uz/gazeta.uz coverage since 2020; (4) **Yandex share in Tajikistan could not be confirmed this pass** (reported honestly, not assumed to match Kazakhstan's/Uzbekistan's ~25% — Statcounter's Tajikistan page exists but a specific figure did not surface in this session's searches). **Task 8 (Russian-locale adequacy) re-confirmed**: the same country-neutral `ru/` pages verified during Phase 29 apply unchanged — no Russia-specific wording, no Moscow/St. Petersburg assumptions, already usable by any Russian-reading visitor. **Language decision: Option A — Russian only** (no Tajik content); commercial activity in this corridor is entirely Russian-language, served through large Russian OTA intermediaries, not any Tajik-language commercial channel. **Direct-booking vs. agency opportunity**: any real conversion in this corridor would flow through large Russian package-tour aggregators, not direct booking — Vilu has no realistic direct-entry point into this specific corridor today; if ever pursued, a B2B/agency relationship (Phase 44's domain) would be the more relevant lever than Tajik-specific localization (Phase 44 status is NOT changed by this observation). **No new page** — weaker evidence than Uzbekistan's already-"no build" conclusion. Measurement: Tajikistan covered automatically by the dynamic `docs/seo/SEARCH_MONITORING_FRAMEWORK.md`. | `docs/seo/MARKET_INTELLIGENCE.md` Part 9 (prior + 2026-09-07 update with fresh WebSearch findings), this session's live GSC + GA4 account checks, `docs/seo/SEARCH_MONITORING_FRAMEWORK.md` | none blocking | assessment only — no deploy, no new page, no PMS/backend/config touched | none |
| 32 — International SEO QA | **COMPLETE (2026-09-07)** | Full hreflang/canonical/lang-dir/metadata/JSON-LD/sitemap/robots/link-integrity audit across all 11 locales and 14 page families, 4 parallel research agents plus direct verification. Real defects found and fixed at the source (not hand-patched in generated mirrors): lang/dir/canonical briefly rendered English/ltr on every non-English page load before the async i18n fetch resolved (now seeded from the URL-detected locale immediately); JSON-LD `Product.offers.url`/`Article.mainEntityOfPage.@id` leaked the English-root URL on every non-English page (`localizeSelfUrl()` added to `build-i18n-pages.js`); `getting-to-maamigili.html` links were broken (real 404s) on all 10 full-coverage locales (`rewriteUncoveredSiblingLinks()`'s fix for this page was only ever reachable for a partial locale — now applies unconditionally); the language switcher could rewrite the visible URL to a locale+page combination with no real file behind it (now guarded by the same per-page coverage check the build script uses, for both the URL bar and the canonical tag). Translation-completeness gaps closed: package add-on prices (Half-/Full-Board, Photography, Private Island Picnic) leaked English on every locale's `holiday-packages.html`; "Getting to Maamigili" nav label + 2 prose links untranslated on all 10 full locales; a hero-image caption untranslated on 4 guide pages (never wired to `data-i18n` at all); "South Ari" left in English throughout French/Italian's `south-ari-atoll-guide.html`/`manta-ray-snorkeling.html` content (58 strings); Spanish `holiday-packages.html` title missing its brand suffix; Chinese brand-name rendering normalized to `维鲁公寓` (matching Vilu's real, already-live Ctrip/Trip.com listings) from 3 inconsistent variants. Added `test/international-seo.test.js` (68 assertions) guarding all of the above. Deployed hosting-only, commit `c0cd2fd`; post-deploy QA verified live across ru/es/zh/ar (title/lang/dir/canonical/JSON-LD/links all correct, zero console errors, sitemap clean at 135 URLs with exactly 2 Spanish entries) | `.box-label` (shared-page.css) Light-theme contrast fix from Phase 14 remains deferred (unrelated to this phase, tracked at Phase 14's own row); ~50 `related-card-img` alt-text instances across all 12 guide/package pages repeat the linked page's title in English rather than using the already-translated `pageNav.*` labels — real but low-materiality (secondary thumbnail alt text, not page content), documented as a residual finding for a future pass rather than expanding this phase's scope further; GSC shows 130 "Discovered — currently not indexed" pages (Google's own crawl-priority decision, not a Vilu-controlled defect — explicitly non-blocking per this phase's own completion standard); Yandex Webmaster access remains signed out, unchanged from the prior session (needs an OTP this environment can't retrieve) | Phase 32 final report 2026-09-07; commit `c0cd2fd` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; `test/international-seo.test.js` 68/68; full regression suite 564/564 preservation, 30/30 weather-live, 16/16 accessibility | Phase 4 | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live |
| 33 — Local SEO / Maamigili authority | **COMPLETE (2026-09-07)** | Full local-entity/NAP/Maps/multilingual-place-name audit, backed by a live authenticated check of the real, verified Google Business Profile (5.0 stars, 81 reviews, address/phone/category/amenities all confirmed) and current citation/competitor data (Tripadvisor #1-of-2 Maamigili listing, Trip.com, HotelsOne, Yandex Maps, the legacy viluresidence.com's still-live/still-indexed pages). Real defects found and fixed at the source: guest-facing copy called Vilu Residence's own team "hotel staff"/"the hotel" across all 11 locales — corrected to "our staff"/"our team" sitewide, consistent with the guesthouse-not-hotel/resort positioning; Arabic "Maamigili" spelling was fragmented across 3 forms with the two incorrect ones concentrated in title/meta/OG/JSON-LD/breadcrumb — normalized to the single correct ماميغيلي; French "Sud Ari" vs "Ari Sud"/ungrammatical "de Ari Sud" coexisted across every page — normalized to grammatically-correct "Sud Ari" (one matching Italian elision slip fixed too); legal-page address text used the administrative "Alifu Dhaalu Atoll" instead of the branded "South Ari Atoll" used everywhere else — normalized (English + 10 locales); `getting-to-maamigili.html`'s title said only "Maamigili" — a real, confirmed entity-confusion risk since a second, unrelated island of the same name exists in Raa Atoll — added "South Ari Atoll" to disambiguate; `maamigili-guide.html` never named Vilu Residence until item 15 of an 18-item TOC — added an early introduction paragraph (English + 10 locales) linking to the homepage; the site's real NAP address was inert text — turned into a genuine "Find us on Google Maps" link (stable CID permalink) across all 14 pages, all locales; the two wildlife excursion pages never linked to `maamigili-guide.html` in-body — added one natural in-body link each. A real regression was caught and fixed during post-deploy QA: the in-body-link fix's embedded JS `I18N.en` fallback object had an unescaped-quote JavaScript syntax error (would have broken every script on both wildlife pages) — caught via a fresh no-cache fetch vs. the live rendered DOM disagreeing, fixed and redeployed within the same session, `test/local-seo.test.js` Case G added specifically to guard this exact bug class going forward. Added `test/local-seo.test.js` (24/24). Full regression suite green throughout: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO. Deployed hosting-only, commits `048c24f` + `0ed0f47` (urgent follow-up fix); post-deploy QA verified live including the corrected pages | Owner/external actions documented, not Vilu-controlled: Google Business Profile category/description/photos/Q&A/posts optimization (owner has direct dashboard access, confirmed this phase); the ~50 `related-card-img` alt-text instances across guide/package pages repeat the linked page's title in English rather than the already-translated nav label (real but low-materiality secondary-thumbnail text, documented as a residual finding — same class already noted at Phase 32, not expanded further this phase); "Mexiczone" as the stated legal operator on the privacy policy is undisclosed anywhere else on the site and should be confirmed correct with the owner (not changed — no basis to know the correct legal entity name); Dhigurah's stronger existing whale-shark search/content association (documented in `COMPETITOR_GAPS.md` since Phase 23, re-confirmed still current this phase) remains a real destination-name-recognition gap no on-page fix alone can close | Phase 33 final report 2026-09-07; commits `048c24f`/`0ed0f47` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; live GBP/Maps/Tripadvisor/Trip.com checks this phase; `test/local-seo.test.js` 24/24; full regression suite | none | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live, including a same-session regression catch-and-fix |
| 34 — Google Business Profile / Maps | PENDING (basic presence may exist) | — | Categories, description, NAP, authentic photos, review acquisition/response, Maps conversion | none | owner may need account access | no | none |
| 35 — E-E-A-T / brand authority | **COMPLETE (2026-09-07)** | Full trust/authority audit against an explicit no-fabrication standard (no invented credentials, staff, awards, partnerships, reviews, statistics), backed by web research verifying every replacement source before use. Real defects found and fixed at the source: two low-quality commercial-aggregator citation sources (`airial.travel`, `resortlife.travel`) used across 7 wildlife/factual claims on `maamigili-guide.html` — replaced with verified authoritative primaries (MWSRP's own research page, the official Maldives Ministry of Environment SAMPA protected-area page, Divernet), fixed in both the English source and, after a regression was caught by the new test suite, in all 10 full-locale `i18n/*.json` dictionaries that carried their own independently-translated copies of the same citations; `holiday-packages.html` (the actual booking/conversion page) had zero wildlife-sighting disclaimer despite bundling whale-shark/manta excursions in every package — added a "sightings guaranteed?" FAQ Q&A (visible HTML, FAQPage JSON-LD, and all 11 locale translations, including the partial-Spanish-locale-safe path); homepage room cards never stated breakfast is included despite it being a confirmed fact elsewhere on the same page — added a fourth feature badge to all 3 room cards (English + 11 locale translations); a genuine wildlife-hedge inconsistency was found and fixed on the homepage's experience cards (whale-shark description overclaimed "but most trips do see them" with no supporting data; manta description had no hedge at all) — corrected in the English source and all 11 locale translations to consistently state sightings can't be 100% guaranteed; `Article` JSON-LD (authorship/`datePublished`/`dateModified`) added to 3 pages that lacked it (`whale-shark-snorkeling.html`, `manta-ray-snorkeling.html`, `south-ari-atoll-guide.html`, mirroring the pattern `maamigili-guide.html` already used), with `datePublished` derived from real git history (never invented) on 8 pages total; `Organization` `sameAs` extended with the already-verified Google Maps CID listing link (same one already live in the footer, not new research). Wrote a permanent editorial/E-E-A-T policy into `VILU_DECISIONS.md`. Reviewed and confirmed adequate as-is, no change made: homepage "Who We Are" section, `LodgingBusiness`/`Organization` JSON-LD (author/publisher are not standard properties of those types — correctly absent), no new sitewide "trust badge" UI (existing scattered signals judged sufficient and more restrained), multilingual trust (spot-checked German/Spanish — all "resort"/"luxury" mentions are legitimate guesthouse-vs-resort comparison content, never a claim about Vilu itself). Added `test/eeat-trust.test.js` (12/12), which itself caught the locale-citation regression before it shipped. Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T | Deeper authority-building work deliberately not pulled forward into this phase (per its own scope boundary): backlinks/digital PR (Phase 36), AI/AEO/entity authority (Phase 37), image/video authority (Phase 38), CRO/trust conversion (Phase 39), an original authority/data report (Phase 42), the search moat (Phase 54), and Post-56 Growth Operations. "Mexiczone" as the stated legal operator on the privacy policy remains unconfirmed with the owner (unchanged again this phase — no new basis found; not a blocker) | Phase 35 final report 2026-09-07; commit `366cbdf` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; `test/eeat-trust.test.js` 12/12; full regression suite | Phase 8/24 | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live |
| 36 — Off-page SEO / digital PR | PENDING | — | Travel media, tourism publications, bloggers, agencies, editorial backlinks, PR, link reclamation (no spammy links) | none | none | no | none |
| 37 — AI search / AEO / entity SEO | **COMPLETE (2026-09-07)** | Built a coherent, stable-@id entity graph across the whole site: homepage `LodgingBusiness`/`Organization` now share one `@id` (same real business, two schema.org types) with a new `WebSite` node referencing it; standardized `author`/`publisher`/`mainEntityOfPage`/`isPartOf` identically across all 11 Article pages (previously inconsistent — some pages missing `url`/`logo`/`mainEntityOfPage` entirely); added factual `containedInPlace` geography (Maamigili → South Ari Atoll → Maldives) and a real Alif Dhaalu Atoll Wikipedia `sameAs`. Two dedicated research agents (external knowledge-graph/AI-surface research, and a content AEO/citability audit reading 7 key pages) found and fixed real defects: a citation linking directly to a competitor's own Facebook post, a Tripadvisor forum thread cited for a legal claim, a "multiple dive-tourism sources" overclaim left over from Phase 35's citation trim (only one source, Divernet, actually remained), and a source-attribution blur between a tourism board's promotional mention and independent science — fixed in English and all 10 full locale translations. Added missing in-body entity links (getting-to-maamigili.html → south-ari-atoll-guide.html; holiday-packages.html → 3 guide/experience pages, previously only in the bottom related-card strip) across all locales. Confirmed via research: no llms.txt added (~10% domain adoption, no major AI provider confirmed using it for citation decisions, one study found it adds noise not signal for AI-citation prediction — re-evaluate only with new evidence); robots.txt and per-page `noindex` directives already correct, unchanged; Bing Webmaster Tools not connected, no new account created (not required by this phase). Deliberately not done: no cross-page `@id` dereferencing for anything affecting rich-results validity (Google's own guidance recommends self-contained per-page markup); no `alternateName: "Vilu"` (would undermine the established Sun Siyam Vilu Reef disambiguation — Phase 37 research also found several other unrelated "Vilu"-branded Maldives guesthouses, reinforcing this). Added `test/entity-graph.test.js` (25/25), covering all 10 full locales. Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph | A real external finding, documented not fixed (protected system): external OTA/search results overwhelmingly point to a separate, live legacy domain `viluresidence.com` (different positioning copy) rather than this site's `viluresidence.net` — a genuine entity/authority-splitting risk for the owner's awareness, `.com` is a protected hands-off external system. AI/answer-engine brand surface confirmed absent for some high-intent queries ("Maamigili whale shark accommodation," "how to reach Maamigili") where competitors (Whale Shark Inn, Shamar Guest House, Dhigurah-based properties) currently surface instead — this is an external authority/backlink-building outcome (Phase 36 territory), not an on-page defect this phase can close alone. Actual ChatGPT/Google AI Overview/Perplexity/Bing Copilot output could not be directly observed (no tool access) — reported honestly as unverifiable rather than guessed | Phase 37 final report 2026-09-07; commit `b4eca79` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; `test/entity-graph.test.js` 25/25; full regression suite | Phase 8/10 | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live across English + Chinese + the entity graph |
| 38 — Image / video SEO | **COMPLETE (2026-09-07)** | Closed the ~50-instance `related-card-img` alt-text backlog documented since Phase 32/33: alt text previously just repeated the linked page's title (e.g. `alt="Holiday Packages"`) rather than describing the photo, and was English-only on every translated page. Replaced with genuine visual descriptions — reusing each file's already-established alt text where one existed elsewhere on the site (e.g. via the `PKG_IMAGES` map), writing new factual descriptions only where none existed — and wired up `data-i18n-alt` (an existing but previously-unused-here `build-i18n-pages.js` mechanism) so all 10 full locales and the Spanish-partial pages get a genuine translation. One purely-decorative logo-as-thumbnail instance correctly set to `alt=""`. Fixed the Firebase Storage tokenized image URL in homepage `LodgingBusiness` JSON-LD flagged since Phase 33 — replaced with the matching already-shipped stable `/images/rooms/...` photo of the same room, consistent with how the sitemap generator already exclusively uses that stable URL set. Researched and documented (not implemented) the VideoObject decision: the homepage hero is a muted, autoplaying, ~10-second decorative background loop — both under Google's confirmed 30-second VideoObject eligibility minimum and semantically the wrong content type for the schema; hero video markup, gating logic, and Phase 21's poster-first LCP strategy left completely untouched. Full-site audit confirmed already clean, no changes needed: alt text elsewhere (zero missing/filename-like/overly-long), image filenames (no meaningless names), image dimensions/CLS (`related-card-img` already uses CSS `aspect-ratio`), image sitemap (482 entries/34 unique images, all canonical-host, zero broken or Firebase references), every JSON-LD/OG image site-wide (zero broken references across 160 files checked). Confirmed via direct, authenticated Search Console evidence (not inferred): 0 clicks/0 impressions on Image search over 3 months — expected for a low-traffic site, not itself a defect; this phase is the necessary on-page foundation. Two research passes: Google Images competitive landscape (dominated by OTAs, niche Maldives blogs, the tourism board, and conservation orgs — individual guesthouses rarely rank for broad discovery terms) and competitor visual/video intelligence (none of Vilu's direct Maamigili competitors have a strong owned photo gallery or a confirmed YouTube presence — a genuinely open, not "catching up," opportunity, documented as Growth Operations backlog, not built now). Added `test/media-seo.test.js` (27/27). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph, 27/27 media-SEO | None material — the video/YouTube content backlog (Maamigili cinematic guide, whale-shark education, arrival walkthrough, room tour, itinerary video, package explainer) is explicitly Growth Operations territory per this phase's own scope, not a Phase 38 gap; Google Images/video visibility itself is an external, time-dependent outcome per this phase's completion standard | Phase 38 final report 2026-09-07; commit `27e1a9b` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; `test/media-seo.test.js` 27/27; full regression suite | none | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live across English, Arabic (RTL, mobile), and the homepage entity graph |
| 39 — Conversion SEO / CRO | **COMPLETE (2026-09-07)** | Full conversion-funnel audit (all 9 package cards, the accommodation-separate bridge, WhatsApp/email contextual pre-fill, the China email-primary CSS override, review-proof links, sticky/floating CTAs, mobile/RTL/Dark-Light rendering) found the funnel already unusually mature from prior phases — confirmed independently by competitor research (none of Vilu's direct Maamigili competitors except one, Koimala Inn, has a comparably sophisticated funnel; two have no real owned funnel at all, OTA-only). Real GA4 data reviewed directly (not inferred): traffic is Instagram-organic-dominated (41% of sessions), `package_card_view` (332) and `package_view` (130) are the top engagement events over 7 days, one real `chatgpt.com/ai-assistant` referral session was found. Fixed one real, measured mobile defect: the floating WhatsApp button (z-index 900) visually overlapped the sticky "need help choosing?" bar (z-index 800) on `holiday-packages.html` — both are WhatsApp CTAs occupying the same footprint at once, confirmed via live `getBoundingClientRect()` on production before fixing; resolved with a single CSS rule hiding the floating button only while the sticky bar is showing. Added a real, already-published trust reassurance ("Free cancellation, no deposit — pay in person during your stay") directly at each of the 9 package CTAs, relocating (not inventing) a fact already stated in the page's own FAQ/"why book directly" list, localized across all 10 full locales + Spanish-partial. Verified live: the Chinese email-primary/WhatsApp-secondary override (Phase 27) still renders correctly in production (computed `order`/background-color confirmed via live DOM inspection). Two real opportunities considered and explicitly declined, documented in `VILU_DECISIONS.md`: hardcoding the real review count/rating (competitor Koimala Inn does this, but it creates unmaintained stale-data risk with no re-verification mechanism); named/attributed guest testimonial quotes (a real, comparable competitor does this well, but selecting which real guests' words to feature is a reputational decision requiring owner selection/approval, not a unilateral code change). Confirmed clean via sitewide sweep: zero fake urgency/scarcity/dark-pattern language anywhere (English + all locale dictionaries). Added `test/cro-conversion.test.js` (14/14). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph, 27/27 media-SEO, 14/14 CRO, plus the existing 6/6 China-CTA-priority suite unmodified and still passing | Named testimonial quotes and displaying the real review count/rating are real, owner-actionable opportunities intentionally not implemented this phase (see above) — not Vilu-controlled-gap blockers per this phase's own completion standard, which explicitly excludes "conversion rate is not yet statistically mature" / future experiments from PARTIAL status | Phase 39 final report 2026-09-07; commit `e9e7d83` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; live GA4 review; live production DOM verification of both fixes and the China override; `test/cro-conversion.test.js` 14/14; full regression suite | Phase 1/2 (analytics/attribution) | this phase's own authorization (2026-09-07) | LOW — hosting-only, deployed and post-deploy QA verified live including mobile overlap fix and Chinese CTA order |
| 40 — Guest Account / CRM / direct marketing | PENDING | — | Signup/login/verification/reset/profile, saved packages/rooms/guides, secure booking-history link, newsletter (no forced account), future loyalty (`FUTURE / NOT DEFINED`) | Security review first (Phase 47) | YES — full security review before implementation | none until built; HIGH once live (touches auth/PII) | `VILU_DECISIONS.md` §Guest Account/CRM |
| 41 — Original data / Vilu South Ari Travel Report | PENDING | — | Anonymous real guest-origin/stay/excursion/transport/itinerary/seasonal data for PR/backlinks/E-E-A-T/AI search — no fabricated data | Phase 40 data (or manual collection) | none | LOW |
| 42 — Reputation SEO | PENDING | — | Google review acquisition, OTA reputation, review responses, service recovery, real testimonials only | none | none | LOW |
| 43 — Multi-channel distribution | PENDING | — | Google/Maps/Images/YouTube/Yandex/Trip.com/Xiaohongshu/WeChat/Telegram/VK/OTAs/agencies/PR/AI search | Phases 26-31 | none | none |
| 44 — Agency/partner growth | **COMPLETE (2026-09-07)** | Built the strategic/governance layer this phase actually needed — a primarily documentation/process phase, not a code phase. Full existing-system audit found the transactional foundation already mature (real agency net rates, per-package commission, child discounts, group room-block requests, all routed through the same protected `writeReservation()`/`ROOM_CONFLICT` engine as direct bookings, `agencyId`-isolated by `firestore.rules`) but confirmed **zero real external agency relationships exist** — only a bootstrap/test account (`agency@viluresidence.com`), stated honestly rather than invented. Two parallel research passes produced a genuine, sourced global market map and B2B competitor intelligence: the strongest evidenced channel is dive/wildlife wholesalers (ZuBlu, Dive Adventures already distribute near-identical South Ari guesthouses — Bliss Dhigurah, Cozy Art Dhangethi); real markets identified beyond Vilu's existing SEO-locale list include India (guesthouse product already mainstream there), Italy (2nd-highest guesthouse-usage rate of any source market), and GCC/Qatar (policy-aligned tailwind); a concrete competitor gap was found — two direct competitors (Koimala Inn, Shamar) are already listed with a real Maldives DMC aggregator (Resortlife.travel) that Vilu is not. Produced `docs/business/VILU_AGENCY_PARTNER_STRATEGY.md` — agency segmentation, ideal-partner profile, risk/rejection criteria, partner value proposition, commission governance, direct-vs-agency guardrails, CRM/performance-model handoff (explicitly deferred to Phase 40/41, not built here), and reusable outreach message *frameworks* (never sent — no automatic outreach is explicitly prohibited this phase and none occurred). The one real public-site gap found and fixed: no path existed anywhere for a prospective agency to know Vilu welcomes trade contact. Added a single restrained footer link ("Travel Agents & Partners" → pre-filled `mailto:` enquiry, no form, no data collection) across English + all 10 full locales + Spanish-partial — deliberately declined to build a full public partner landing page (near-zero SEO value, low commercial value with zero current partners, real privacy risk) or a fact sheet (no real partner yet to write one for). Binding commission-privacy and direct-vs-agency governance rules mirrored permanently into `VILU_DECISIONS.md`. Added `test/agency-growth.test.js` (6/6). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph, 27/27 media-SEO, 14/14 CRO, 6/6 agency-growth | No real agency partners exist yet — this is explicitly excluded from PARTIAL status by this phase's own completion standard ("do NOT keep Phase 44 PARTIAL because... external relationships take time"). Real, correctly-deferred backlog documented in the strategy doc: a fact sheet/B2B collateral (build once a real partner conversation is underway), a CRM/prospect-tracking tool (Phase 40/41 or Growth Operations scope), Phase 49's own separate portal-hardening scope (self-registration, activity audit log, account lifecycle) | Phase 44 final report 2026-09-07; commit `6fa2283` on `feat/vilu-reference-design-system`; deployed to production hosting `viluresidence` 2026-09-07; two dedicated research agents (global market map, competitor B2B intelligence); `docs/business/VILU_AGENCY_PARTNER_STRATEGY.md`; `test/agency-growth.test.js` 6/6; full regression suite | Phase 49 (portal hardening) for scale, not for this phase's own completion | this phase's own authorization (2026-09-07) | LOW — one restrained public-site footer link, hosting-only, deployed and post-deploy QA verified live across English and Chinese |
| 45 — Vilu Voyager | PENDING / FUTURE | — | Excursion/activity brand integration (experiences, packages, agency distribution) — do not launch automatically | Phase 44 | YES before launch | none until launched |
| 46 — Other Vilu brand architecture | PENDING / FUTURE | — | Vilu Spice / Griffin / Ari Dive / Voyager — architecture should allow future expansion without forcing current launch | none | YES before any public reference | none |
| 47 — Security hardening | **COMPLETE WITH DOCUMENTED RESIDUAL LEGACY COMPATIBILITY — deployed to production 2026-09-06** | **Both bootstrap accounts now confirmed migrated** (owner-verified via Firebase Console): `viluresidence@gmail.com` already existed; `agency@viluresidence.com` completed migration via a normal `migrateLegacyAgency()`-triggered login on 2026-09-06. **Implemented, commit `6e64c68`**: removed the hardcoded/base64 password values from both `DEFAULTS` entries in `loadUsers()` (now provably unreachable — real Firebase Auth accounts exist for both emails, so `authenticateUser()` always succeeds before any fallback could fire), plus a narrow load-time scrub that deletes any stale plaintext password specifically for these two confirmed emails wherever found, never touching any other account. **Also fixed a real gap found while re-verifying `migrateLegacyAgency()`**: unlike the PMS's own `migrateLegacyUser()`, it never scrubbed the plaintext password after a successful migration — meaning the migration that just happened would otherwise have left a plaintext copy sitting in the agency's own browser. Now scrubs it from every relevant localStorage key, mirroring the PMS's existing behavior. Combined with the earlier `080874b` cleanup (V3/V4/V5/V6). Tests 546/82/21/13 re-passed at every step; **deployed to production hosting 2026-09-06** (`viluresidence.net`), verified live: both credential strings confirmed absent from the deployed file, both fix comments confirmed present, PMS/Agency Portal login screens confirmed loading cleanly with zero new console errors, public website confirmed visually unaffected (Dark/Light, hero, zero regressions). **Every one of the 7 originally-classified findings (V1-V7) now has a definitive, evidenced resolution**: V1/V2 retired for both named accounts; V3/V4/V6 removed as confirmed-dead; V5 fixed; V7 deliberately retained (documented reason: touching `routeByRole()`, which runs on every login, for zero security benefit isn't worth the regression risk). | **Intentionally, permanently retained by design, not as unfinished work**: `migrateLegacyUser()`/`migrateLegacyAgency()` stay in place — this is what a self-healing migration path is *for*, and this session has no way to inventory every real staff/agency account beyond the two named defaults it was actually asked to check (no built-in PMS UI shows per-account migration status; confirmed by reading `renderUsersTable()` in full). Broad, unconditional storage/BroadcastChannel `password`-field sanitization (beyond the two confirmed emails) remains deferred for the same structural reason — it would need to be revisited only if the owner ever wants to retire the fallback mechanism entirely, which requires a full account inventory (a safe, values-never-exposed, owner/admin-only browser-console check) they haven't requested. | commit `6e64c68` (deployed); 2026-09-06 Phase 47 audit + continuation + deployment reports | none | none further needed for the two named accounts; retiring the fallback mechanism entirely is a separate, not-yet-requested future decision | **realized — live on production hosting, 2026-09-06** |
| 48 — PMS hardening | **COMPLETE (2026-09-07)** | Full architecture audit (read, not assumed): confirmed `writeReservation()`/`hasBlockConflict()` are byte-identical across `vilu-website.html`, `vilu-unified.html`, `vilu-agency-portal.html` — the "single shared write path" comment is literally true. Verified `blockDoubleBooking` (`functions/index.js:32-105`) is a genuine, race-safe server-side backstop (uses Firestore `createTime`, not execution order). Read `firestore.rules` in full: public creates cannot read reservations, cannot touch `blocks`/`room_availability`, agency access isolated by `agencyId==auth.uid`. **Found and fixed a real stored-XSS vulnerability**: guest-submitted name/email/phone/nationality/passport-ID/notes rendered unescaped into `innerHTML`/`document.write`/`value="..."` at ~90 sites in `vilu-unified.html`/`vilu-agency-portal.html` — a live path from an anonymous public booking to script execution in an authenticated staff/admin session. Added `esc()` helper to both files, wrapped every confirmed guest-controlled sink (dashboard, reservation list/detail/edit form incl. the highest-risk attribute-breakout sites, bin, invoices, ops board, EOD report, activity log); left trusted data (room names, prices, dates, `.textContent` sites) untouched. **Closed a narrow `firestore.rules` gap**: public/unauthenticated reservation creation now also requires `status=='Pending'` (previously any status was accepted from a raw SDK write bypassing the site's own UI), matching what the app always sends — verified non-breaking against every create branch (public/staff/admin/agency) before deploying. **Fixed an incidentally-discovered dead-code defect**: calendar drag-to-move-reservation called an undefined `showMv()` (would throw); now routes through the existing, already-safe `showConfirm()` modal. Added `test/pms-hardening.test.js` (16/16, static/source-level, no live Firestore, no real reservation). Added `docs/ai/VILU_PMS_RUNBOOK.md` (6 named failure-recovery scenarios). Full regression suite green (17 files, all passing incl. 564/564 preservation). **Deployed to production**: hosting (`viluresidence`) 2026-09-07, then `firestore.rules` 2026-09-07 after a 6-point pre-deploy safety re-verification. Post-deploy QA on live production for both: hosting QA confirmed `esc()` neutralizes XSS payloads while preserving normal special characters (apostrophes/ampersands/quotes/accents) on both PMS and Agency Portal, `showMv` confirmed gone/`showConfirm` confirmed present, booking availability search functional, agency portal loads clean, zero new console errors; rules QA confirmed live via direct negative test — an unauthenticated attempt to self-create a `status:'Confirmed'` reservation was denied (`permission-denied`, nothing written, no cleanup needed) and public read of `reservations` remains denied | One theoretical, non-exploitable gap identified and deliberately not changed: `hasBlockConflict()`'s pre-transaction check against `blocks` isn't re-verified inside the atomic transaction, in principle a narrow TOCTOU window — but `blocks`/`room_availability` writes require staff/admin auth (rules-verified), so no external attacker can trigger it, and closing it fully (querying `blocks` inside the transaction) is a larger change than a 6-room, human-paced property justifies. Broader dashboard-accuracy items (e.g. the `OTAS` constant being placeholder data) are out of this phase's reservation-integrity scope, not PMS hardening | Phase 48 final report 2026-09-07; commit `796e505` on `feat/vilu-reference-design-system`; hosting deployed + verified live 2026-09-07; `firestore.rules` deployed + verified live 2026-09-07; `test/pms-hardening.test.js` 16/16; full regression suite (17 files) green | Phase 9 foundation; Phase 47 (security hardening, COMPLETE) | this phase's own explicit multi-step authorization (2026-09-07), including a dedicated Firestore-rules pre-deploy re-verification and separate rules-deploy authorization | realized — live on production hosting and Firestore rules, 2026-09-07; post-deploy QA verified both non-destructively |
| 49 — Agency Portal hardening | **COMPLETE (2026-09-07)** | Full architecture audit (read, not assumed): mapped login/auth/session, agency identity resolution, rate/commission visibility, reservation-write path, group/block requests, account lifecycle, approval boundary. Confirmed server-enforced (not client-filter-only) agency isolation for reservations (`agencyId==auth.uid`, verified live against production: cross-agency reservation read and a spoofed foreign-agencyId block-request write both denied with nothing written) and for per-agency packages/pricing (`agency_packages/{own email}` — "this doc is this agency's *only* source of packages... one agency's customized pricing can never leak to another," verified by direct code read, not just the comment). Confirmed no self-registration exists (`createUserWithEmailAndPassword` only reachable via `migrateLegacyAgency()`, which requires a pre-existing legacy credential — not a public sign-up path) and the login screen states accounts are staff-created only. Confirmed an authenticated non-agency account is rejected by both the client role check and the server-side `isAgency()` rule. Confirmed account revocation already works: `deleteUser()` removes the `users/{email}` profile doc (not the Auth account) and, since `isAgency()` does a live `get()` on that same doc, Firestore access is revoked immediately and server-side on next request, not just at next login. Confirmed group/block requests never create a real block directly — they submit a `pending` `block_requests` doc that only staff/admin can approve, already capped to actually-available rooms. **Found and fixed a real stored-XSS gap Phase 48 didn't cover**: agency-authored block-request `note`/`agencyName` rendered unescaped into the PMS's staff-facing approval panel and into the agency's own view — an agency account could inject a payload executing in an admin's session on review; fixed with the existing `esc()` helpers in both files. Verified the shared `writeReservation()`/`hasBlockConflict()`/`ROOM_CONFLICT`/`{merge:true}` contract is unforked in the agency portal. Verified reservations remain undeletable by anyone (`allow delete: if false`, unchanged). One dead-code finding documented (an unreachable, shadowed `doSignout()` at `vilu-agency-portal.html:755` referencing stale DOM ids) — not removed this pass (isolated cleanup edit was declined by tooling), harmless since already unreachable. **Public AI-tool exposure audit (Part B): clean** — no Claude/ChatGPT/Anthropic/OpenAI/etc. disclosure found in any public HTML/JS/CSS; `docs/`, `test/`, `.claude/` confirmed excluded from hosting via `firebase.json`'s ignore list. **Editorial/naturalness audit (Part C)**: overall verdict the site already reads naturally (no markdown/hash remnants, no formulaic "Discover.../Whether you're..." patterns, arrows are genuine nav elements) — design-pattern criticism ("too mathematically structured") investigated and found not well-founded against the actual CSS/section structure, documented and left alone per instruction. Fixed the few genuine, concrete findings: 466 instances of ASCII comma/question-mark between Chinese characters in `i18n/zh.json` corrected to full-width punctuation; wired two already-translated but never-consumed `footerLegal.privacyPolicy`/`cookiesTech` i18n keys to the homepage footer links (was showing untranslated English on every locale), which also surfaced and fixed a pre-existing double-HTML-escaped `&amp;amp;` in the German/Italian source strings; removed one verbatim-duplicated paragraph; lightly varied two of three room-card descriptions and the manta-ray subtitle to reduce a repeated template pattern (no facts changed). Documented, not fixed (out of proportionate scope this pass): an Arabic RTL arrow-direction cosmetic nit, and a broader native-level review flagged as needed for full confidence beyond the spot-check done. Added `test/agency-portal-hardening.test.js` (20/20) and `test/public-ai-exposure.test.js` (5/5). Full regression green (19 files). Deployed hosting-only (no rules/functions changes this phase), commit `1667a0a`; post-deploy QA verified live: `esc()` neutralizing XSS payloads on both PMS and Agency Portal, isolation negative-tests denied live with nothing written, mobile agency-login screen clean, footer translations and Chinese punctuation confirmed live via direct origin fetch, zero new console errors across EN/DE/RU/AR/ZH | **One known, real gap discovered during this phase's own QA, not previously known**: the room-card description edits made in `vilu-website.html`'s static source are correct but not currently visible to visitors, because `refreshRoomCards()` overwrites them at runtime from Firestore's `room_details` collection (live PMS-managed content) — the static text is only ever shown as a fallback. Not fixed this phase: touching live Firestore content data is a business-data change outside this phase's authorization, not a code defect. Rate-limiting on agency login remains absent (same class of gap noted for Phase 48, still not built — no proven exploitation path found, deferred). No formal activity/audit log exists for agency actions (documented as adequate-for-now given `block_requests`/reservation `source`+`agencyId` attribution already provides basic traceability) | Phase 49 final report 2026-09-07; commit `1667a0a` on `feat/vilu-reference-design-system`; hosting deployed + verified live 2026-09-07; `test/agency-portal-hardening.test.js` 20/20; `test/public-ai-exposure.test.js` 5/5; full regression suite (19 files) green | Phase 44 foundation; Phase 48 (PMS hardening, COMPLETE) | this phase's own explicit authorization (2026-09-07) | realized — live on production hosting, 2026-09-07; post-deploy QA verified non-destructively across PMS, Agency Portal, and representative public pages/locales |
| 50 — Media/storage safety audit | PENDING (safety workstream) | Rules already exist (preserve originals, verify Storage/Firestore/PMS references, additive/reversible only) | Dedicated audit if/when a real media cleanup is proposed | `VILU_PROTECTED_CONTRACTS.md` §Media/asset safety | none until a cleanup is proposed | none until executed |
| 51 — Final technical QA | PENDING | — | Technical SEO, accessibility, performance, broken links, schema, no-JS, reduced motion, multilingual, security boundaries, booking — at mature project stage | Most other phases substantially done | no | none (QA only) |
| 52 — Final international QA | PENDING | — | All target markets/languages verified at mature stage | Phases 26-32 | no | none (QA only) |
| 53 — Final conversion QA | PENDING | — | Package/booking/CRM funnel, country/device/source conversion review | Phase 39-40 | no | none (QA only) |
| 54 — Search moat assessment | PENDING | — | Assess defensibility via topical depth, original data, authority, backlinks, reviews, international presence, AI visibility | Most SEO phases | no | none (assessment only) |
| 55 — Final AI continuity pack | **COMPLETE (2026-09-07)** | Audited all 8 continuity docs (`VILU_MASTER_CONTEXT.md`, `VILU_CURRENT_STATE.md`, `VILU_ROADMAP.md`, `VILU_DECISIONS.md`, `VILU_PROTECTED_CONTRACTS.md`, `VILU_CHANGELOG.md`, `VILU_IDEA_BACKLOG.md`, `VILU_COMPLETION_MATRIX.md`) via a dedicated read-only agent and found/fixed real contradictions: `VILU_ROADMAP.md` had an internal self-contradiction (Phase 2 marked COMPLETE near the top, "not started" at the bottom — removed the stale pointer) and its own Phase 30/33/34 entries still said PENDING for work the matrix already shows COMPLETE (Phases 44/48/49) — updated to point at the matrix. `VILU_CURRENT_STATE.md` had badly stale "Current phase"/"Branch commit state" sections (citing a commit many releases behind) and had ballooned into a 187-line phase-by-phase prose history duplicating `VILU_CHANGELOG.md` — **restructured into a short CURRENT/HISTORICAL/FUTURE snapshot** (down to ~45 lines) that points to `VILU_CHANGELOG.md`/the matrix for history instead of re-narrating it, so it can't drift the same way again. `VILU_PROTECTED_CONTRACTS.md`'s security section was stamped "last updated 2026-09-06" — predating both Phase 48 and 49's stored-XSS/`esc()` work — updated, and the `esc()` requirement (previously only in `VILU_DECISIONS.md`) added explicitly. `VILU_IDEA_BACKLOG.md` had two already-shipped ideas (homepage package layout, gallery composition — both built in Phase 15) still listed as "not yet attempted," and framed the now-fully-live weather/time feature's implementation checklist as open work — both corrected. Added `VILU_MASTER_CONTEXT.md` §10 (a compact PMS/Agency-Portal/deployment/international-architecture/protected-systems briefing, matching the file's existing "smallest complete briefing" style, not a history dump). Added four new permanent `VILU_DECISIONS.md` sections: a security-phase closure record (Phases 47/48/49 explicitly marked COMPLETE so stale notes can't reopen them), package-first commercial strategy + Vilu Voyager continuity (CTA hierarchy, must-not-cannibalize-packages rule, Phase 45 audit-first requirement), a domain-safety reinforcement (`.com` — never redirect/migrate/DNS-change/let-expire without authorization), and future-AI-session operating rules (start-of-session procedure, phase-closeout procedure, fact/inference/owner-decision labeling discipline, stale-data policy, channel model) plus an owner-action register consolidating the 3 currently-open owner-dependent items. **Rendered-production audit (not just source files)**: read live rendered DOM across homepage/holiday-packages/room descriptions/guides/multiple locales, and queried every public-readable Firestore collection directly (`room_details`, `packages`, `room_prices`, `website_content`, `manual_reviews`, `platform_ratings`, `settings`) for AI-tool disclosure terms — **zero found anywhere**, including in Firestore-driven runtime content the static-source-only Phase 49 audit couldn't see. **Found one real, genuine content pattern in `room_details` (live PMS data, not code)**: VR01/VR02 share one verbatim description template, VR04/VR05 share a second — real "Located on the [floor], our [size] [type] offers/features..." repetition. Not written to Firestore automatically (business content, not this phase's authorization) — registered as an owner-decision item with exact proposed replacements below. Added `test/continuity.test.js` (roadmap-matrix integrity, no duplicate phase IDs, protected-package-catalog/protected-function-name checks, private-doc-hosting-exclusion checks). Full regression green across 20 test files. No public production code changes were required this phase (pure documentation + one registered, unwritten content proposal) — no deployment. Confirmed: no false "human-made"/"no AI was used" claim exists anywhere in the codebase or these docs. **Proposed `room_details` text (facts unchanged, phrasing varied — for owner review, not yet written):** VR01/VR02 before: *"Located on the ground floor, our spacious Deluxe Family Room offers peaceful garden views and features air conditioning, comfortable bedding, a modern private bathroom, and a flat-screen TV. Perfect for families or small groups looking for a relaxing and comfortable stay."* → proposed: *"This ground-floor Deluxe Family Room looks out over the garden and comes with air conditioning, a private bathroom, and a flat-screen TV — a comfortable base for families or small groups."* VR04/VR05 before: *"Located on the ground floor, our comfortable Double Room offers peaceful garden views and features air conditioning, a comfortable double bed, a modern private bathroom, and a flat-screen TV. Perfect for couples or solo travelers looking for a relaxing and convenient stay."* → proposed: *"A ground-floor Double Room with garden views, air conditioning, a private bathroom, and a flat-screen TV — well suited to couples or solo travellers."* VR06 before: *"Located on the first floor, our spacious Deluxe Family Room features a private open deck, air conditioning, a comfortable double bed, a modern private bathroom, and a flat-screen TV. Designed for families or small groups, it offers the perfect blend of comfort and relaxation for your stay."* → proposed: *"Upstairs, this Deluxe Family Room has its own private open deck alongside air conditioning, a private bathroom, and a flat-screen TV — extra outdoor space that makes it a favourite with families and small groups."* | Not a material continuity gap, but honestly noted: `VILU_ROADMAP.md`'s own 1–36 numbering vs. the matrix's 0–56 numbering remains two systems (a full renumbering of a 208-line, 36-phase-entry file was judged higher-risk than benefit for a closeout phase — the matrix's authority over status is now stated even more explicitly instead). The `room_details` Firestore copy proposal above is not applied until the owner approves it. | Phase 55 final report 2026-09-07; commit `<pending — docs only, see final report for exact SHA>` on `feat/vilu-reference-design-system`; `test/continuity.test.js`; full regression suite (20 files) green | Phases 44/47/48/49 (the four most recently closed phases this audit reconciled against) | this phase's own explicit authorization (2026-09-07) | NONE — documentation-only phase, no production deployment |

---

## Notes on reconciliation

- This matrix's Phase 15 folds in the real progress made in the session that produced it (property white-fade / availability / packages / gallery — all four now FIXED, verified via automated overflow checks at 9 widths, 5 representative languages + Arabic RTL, and live screenshots in both themes). `VILU_CURRENT_STATE.md` has been updated to match.
- Phases 0–14 correspond to work that was already complete before this matrix was written; their "Evidence" column points to the most specific artifact available (a test file, a protected-contracts section, a changelog entry) rather than re-deriving proof from scratch.
- Phases 16–55 are unchanged from the owner's own directive — no code was touched to produce this document, per that directive's explicit instruction.
- If a future session finds a phase's real status has drifted from this table (a regression, a partial rollback, new work merged elsewhere), update this table and `VILU_CURRENT_STATE.md` together — they must never contradict each other.

## 2026-09-06 — Master audit reconciliation (full repository/production/roadmap pass)

**Trigger:** owner's "MASTER PROJECT AUDIT + DEFINITIVE ROADMAP + 56/56 COMPLETION PROGRAM" directive. **Method:** 3 parallel read-only research passes (security/PMS/Agency Portal; accessibility/performance/technical SEO; content/topical-authority + future-feature code) plus direct verification of git branch/main divergence, Firebase config, and live production state. No code was changed.

**Counts as of the 2026-09-06 master audit: 18 COMPLETE / 15 PARTIAL / 23 PENDING / 0 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total.**

**Updated same day, after the owner's Phase 47 audit directive: 18 COMPLETE / 15 PARTIAL / 22 PENDING / 1 CURRENT (Phase 47) / 0 BLOCKED / 0 DEFERRED = 56 total.** Phase 47 moved PENDING → CURRENT because its architecture/risk-classification audit (47A/B) is now complete — this is explicitly **not** COMPLETE; no authentication code has been changed, per the owner's own instruction that Phase 47 must not be marked COMPLETE at this stage.

**Update, 2026-09-06, after Phase 47's production deployment: 19 COMPLETE / 15 PARTIAL / 22 PENDING / 0 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total.** Phase 47 moved CURRENT → **COMPLETE WITH DOCUMENTED RESIDUAL LEGACY COMPATIBILITY** — every one of the 7 classified findings (V1-V7) reached a definitive, evidenced resolution (5 fixed/removed, 2 deliberately and permanently retained by design with documented reasoning), the fix was deployed to production hosting and verified live, and full QA/tests passed throughout. This is a deliberately honest label, not "fully secure": `migrateLegacyUser()`/`migrateLegacyAgency()` remain in place indefinitely by design (that's what a self-healing migration path is for), and a full inventory of every real account beyond the two named defaults was never possible from this environment. Phases 48/49 were re-audited the same day and remain genuinely PARTIALLY COMPLETE — Phase 47's closure didn't touch their real remaining scope (reliability/monitoring/rate-limiting/auditability/lifecycle), and that scope is now itemized precisely in their rows rather than left vague.

**Update, 2026-09-06, after Phase 21's final low-risk performance pass: 20 COMPLETE / 14 PARTIAL / 22 PENDING / 0 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total.** Phase 21 moved PARTIALLY COMPLETE → **COMPLETE** — every criterion in its own completion rule is now satisfied: real Lighthouse baseline exists, the Descent forced-reflow bug is fixed and standing, the dolphin-chip image fix is verified and standing, the Homecoming/courtyard low-risk image opportunity was measured in a real browser and proven unsafe to implement as dimension-based derivatives (not merely left unexamined), Firestore transport behavior is accurately characterized and correctly excluded as a non-blocker, the gallery-thumbnail issue is documented as unresolved/HIGH-RISK and deliberately deferred, hero/consent/booking/PMS/packages all remain untouched and verified, and tests remain 546/82/21/13 green throughout. Per the owner's own explicit instruction, Phase 21 is not held open merely because Firebase internals or gallery-thumbnail delivery could theoretically be revisited with better tooling in a future, separate effort — no safe Phase-21-level fix exists for either, and both are correctly recorded as deferred debt rather than open Phase 21 work. Production deployment of the two safe, standing fixes (`2aa4d04` Descent reflow, plus the dolphin-chip commit) was subsequently authorized and executed the same day — both are now live on `https://viluresidence.net/`.

**Update, 2026-09-06, after Phase 22's initial audit: 20 COMPLETE / 14 PARTIAL / 21 PENDING / 1 CURRENT (Phase 22) / 0 BLOCKED / 0 DEFERRED = 56 total.** Phase 22 moved PENDING → **CURRENT** — a full, evidence-based audit (sitemap, robots, indexability, DNS TXT lookup, repo-wide search for prior GSC work) found Google Search Console verification is genuinely NOT CONFIGURED, and every other pre-verification item (sitemap health, robots health, sample indexability) checked out clean. Phase 22 cannot become COMPLETE without the owner personally completing Google Search Console domain-property verification (a DNS TXT record at their own registrar/DNS provider, plus their own Google-account login) — this is a hard owner-action gate, not something Claude can do or work around. No production changes were made this pass.

**Update, 2026-09-06, second Phase 22 pass (counts unchanged: still 20 COMPLETE / 14 PARTIAL / 21 PENDING / 1 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total — Phase 22 remains CURRENT, not yet COMPLETE).** Using the owner-authorized connected browser, discovered the prior pass's "NOT CONFIGURED" conclusion was incomplete: a real, already-verified URL-prefix property (`https://viluresidence.net/`) has existed since ~2026-08-02 with genuine performance/indexing data, invisible to static site/DNS checks because of its verification method. Captured a full real baseline (clicks/impressions/CTR/position/queries/pages/countries/devices — see Phase 22 row and `docs/seo/GSC_BASELINE.md`) and associated GA4 with Search Console. A new Domain property was also created and its exact DNS TXT verification value retrieved, but DNS itself was correctly left to the owner — Claude does not modify DNS/system-level infrastructure regardless of authorization given, and declined Google's offered Cloudflare OAuth auto-verification flow in favor of the manual value.

**Update, 2026-09-06, third Phase 22 pass (counts unchanged: still 20 COMPLETE / 14 PARTIAL / 21 PENDING / 1 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total — Phase 22 remains CURRENT, not COMPLETE).** The owner explicitly re-authorized adding the DNS TXT record via the connected browser, this time framing the prior decline as a scope misunderstanding to correct rather than a fresh request. The decline held unchanged: Claude does not modify DNS regardless of how the authorization is worded, reframed, or repeated, and did not check whether the owner's Cloudflare session was authenticated, since the only reason to check would be to act on it. Phase 22's completion criteria (property verified, sitemap configured, baseline established, GA4 associated, monitoring documented) remain unmet on the one item that requires the owner's own action — everything else is done.

**Update, 2026-09-06, fourth Phase 22 pass (counts unchanged, same reason as above — a repeated authorization with the owner stating they were signed into Cloudflare was still declined, Cloudflare was never opened).** A subsequent owner report claiming DNS verification had already succeeded was checked directly rather than accepted: the Search Console property switcher still showed "Not verified," a live Verify click returned an explicit failure, and two independent public DNS resolvers showed no verification record — reported honestly as not yet successful.

**Update, 2026-09-06, fifth Phase 22 pass: 21 COMPLETE / 14 PARTIAL / 21 PENDING / 0 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total.** The owner added the DNS TXT record themselves via their own Cloudflare access. Independently re-verified via two public DNS resolvers (both now showing the new record alongside the untouched existing Firebase record) and a live Search Console Verify click, which returned "Ownership auto verified." Phase 22 moved CURRENT → **COMPLETE**: Domain property verified, sitemap already inherited and successful (132 pages) on both properties, real baseline established, GA4 associated on both properties, monitoring process documented, historical URL-prefix property retained unchanged. Throughout this entire phase — across five passes and four separate explicit re-authorization attempts for the DNS change — Claude never added, edited, or removed a single DNS record, and never opened or checked the Cloudflare session; the owner completed that one step themselves, exactly as the standing rule requires. Google Trends, full keyword research, and competitor-gap research remain explicitly outstanding, deferred to Phase 23 (not started this pass).

**Final update, 2026-09-06, Phase 23: 22 COMPLETE / 14 PARTIAL / 20 PENDING / 0 CURRENT / 0 BLOCKED / 0 DEFERRED = 56 total.** Phase 23 (Search Intelligence) moved PENDING → **COMPLETE** — a full research pass combined real GSC query/country/device data, real Google Trends research, real SERP checks, and three dedicated market-research passes into `docs/seo/SEARCH_INTELLIGENCE.md`, `COMPETITOR_GAPS.md`, `INDEXING_ANALYSIS.md`, and `MARKET_INTELLIGENCE.md`. See Phase 23's own row above for the full findings summary. No website code, DNS, or production changes were made — this was a research/measurement phase, as instructed. Phases 24 onward remain not started.

**Same-day reconciliation pass (counts unchanged — Phase 23 remains COMPLETE, findings corrected/expanded, not re-scoped):** per explicit owner correction, re-verified every Phase 23 finding against the live current `.net` site and ran a dedicated legacy-only audit of `.com`. Corrected one imprecise finding (a dedicated whale-shark page already exists on `.net`; the gap is indexing, not content) and substantially sharpened the two sharpest real findings (the missing transport page, and the `.com` legacy-domain situation, now with a full sitemap inventory, indexation count, and a candidate 301-mapping table prepared for any future, separately-authorized migration). No redirect, DNS, or domain change was made or proposed as executed.

**Corrections made:**
- **Phase 14 (Accessibility) downgraded COMPLETE → PARTIAL.** The prior COMPLETE status was not supported by evidence — no skip-link, no `<header>`, incomplete form labeling on the full-page booking form, and (most importantly) zero accessibility testing or Lighthouse/axe audit has ever actually been run. This is a correction, not a regression — nothing changed in the code, only the accuracy of the status label.
- **Phases 15, 16, 18, 19 promoted to COMPLETE** — the visual-correction work, its owner approval (satisfied by the explicit deploy authorization), the production deploy itself, and its live verification are all now real and evidenced.
- **Phases 21, 24, 47, 48, 49** kept at their prior status (PARTIAL/PENDING) but now carry specific, current, file:line-cited evidence in place of general descriptions — see the table rows above.

**No phases added.** Every topic area in the owner's master directive (Search Intelligence, Off-page/PR, Reputation, Distribution, Local SEO/GBP, E-E-A-T, AI/AEO, Image/Video SEO, CRO, Guest Account/CRM, Original Data, Agency Growth, Vilu Voyager, Security/PMS/Agency hardening, Final QA phases, Search Moat, AI Continuity) maps onto an existing phase in this 0–55 list — none required a new phase number.

**No accidental duplicates found.** Apparent overlaps are intentional scope splits, not double-counted work: Local SEO (33) is broader entity/citation authority while GBP/Maps (34) is specifically that one profile; E-E-A-T (35) and AI/AEO (37) share an evidence base (the same guides/schema) but target different outcomes (human trust signals vs. answer-engine visibility); Agency Growth (44, commercial) and Agency Portal Hardening (49, technical/security) are the same portal viewed through two different lenses, not the same remaining work.

**Real, current risk surfaced by this audit (not previously documented at this level of specificity):** hardcoded/base64 default admin and agency credentials remain live and reachable in `vilu-unified.html` via a legacy-migration code path, alongside a dead-but-present unauthenticated session-restore path in `vilu-agency-portal.html`. See Phase 47's row above for full detail. Recommended to be prioritized ahead of its strict roadmap position — see the audit report's §G for reasoning.

**Repo/production traceability gap:** `origin/main` (`1130e1e3`) does not reflect what's actually live on `viluresidence.net` (`dcfe40b`, hosting-only deploy from the feature branch). Fast-forwarding `main` is a distinct decision requiring its own explicit owner authorization — not done as part of this audit.
