# VILU — Current State

**Every future Vilu session must read this file FIRST**, then `VILU_COMPLETION_MATRIX.md`, `VILU_PROTECTED_CONTRACTS.md`, `VILU_DECISIONS.md`, and `VILU_MASTER_CONTEXT.md` — before substantial work. Then state explicitly: CURRENT PHASE / CURRENT STATUS / LAST COMPLETED PHASE / NEXT PLANNED PHASE / BLOCKERS / OWNER APPROVAL REQUIRED? / PRODUCTION IMPACT? Never rely only on conversational memory — this file is a snapshot, not a live query, and must be re-verified against actual repository/production state (branches, SHAs, and file contents drift).

**This file is deliberately kept short and current-only.** Full phase-by-phase history lives in `VILU_CHANGELOG.md` (dated, newest first) and `VILU_COMPLETION_MATRIX.md` (authoritative status table, all 56 phases). Do not let this file regrow into a phase-by-phase prose history — when a phase closes, add one line here (if the CURRENT section below needs updating) and the full entry to `VILU_CHANGELOG.md`, not both here.

**Snapshot date: 2026-09-08, after Phase 45 (Vilu Voyager) closed the roadmap to 56/56.**

> **CURRENT AUTHORITATIVE STATE (updated 2026-09-18, post-migration reconciliation) — supersedes the domain line below:** Primary canonical domain is now **`https://viluresidence.com`**. Legacy domain **`https://viluresidence.net`** 301-redirects to `.com`. Search Console Change of Address is active. The line below reflects the pre-migration state as of the 2026-09-08 snapshot date and is kept for historical accuracy — do not treat it as current.

---

## CURRENT

- **Canonical domain (historical, as of 2026-09-08 snapshot — see correction banner above for current state):** `https://viluresidence.net/` (Firebase Hosting, project `vilu-residence`, site `viluresidence`). `viluresidence.com` is a legacy asset the owner owns — never redirected/migrated/DNS-changed without separate authorization (`VILU_DECISIONS.md` §domain safety). **This has since changed — see the 2026-09-18 banner above.**
- **Branch:** `feat/vilu-reference-design-system`. `origin/main` remains untouched/behind — production is served via scoped `firebase deploy`, not a git merge. **Always re-verify the actual latest commit with `git log`** — do not trust any SHA cited in prose (including this file) without checking.
- **Deploy model:** isolated, clean worktree at `C:\Users\hp\vilu-residence-deploy`, checked out at the exact approved commit; hosting-only deploys unless Firestore rules/functions changes are separately authorized and reviewed. Never deploy from the (intentionally dirty) primary worktree `C:\Users\hp\vilu-residence`. Full rules: `VILU_PROTECTED_CONTRACTS.md` §"Deployment safety rules".
- **Roadmap:** 56 COMPLETE / 0 PARTIALLY COMPLETE / 0 PENDING = 56 total — genuinely, evidence-backed complete, not inflated. Authoritative table: `VILU_COMPLETION_MATRIX.md`. The last phase to close, **Phase 45 (Vilu Voyager Guest Guide)**, was resolved 2026-09-08 by tracing every flagged fact to real, dated evidence rather than guessing: the domestic flight fare (**USD 155 per person, one-way** — corrected same-day per the owner's own explicit authoritative confirmation, superseding an earlier same-program inference of $110; the public website's hardcoded $110 was fixed to $155 across the English source and all 11 locales), room taxes (17% TGST/$6 Green Tax/10% service charge, confirmed against real Maldives Inland Revenue Authority policy — the printed Voyager PDF's 16%/$3 figures predate 2025's tax changes and the physical PDF should be reprinted), and the "Picnic Island Experience" ($49) vs. public "Private Island Picnic" ($51) question (confirmed genuinely different products — a picnic on an island vs. a picnic on a sandbank — not a stale price). Two placeholders (clinic contact, room AC/hot-water notes) were removed rather than left dangling, since no real fact exists to fill them. One narrow, non-material item remains outside the phase's own completion bar: the exact house-to-airport transfer time.
- **Tests:** 20 files under `test/*.test.js` (`node test/<name>.test.js` to run one; no single `npm test` script — run each, or loop `for f in test/*.test.js; do node "$f"; done`). All green as of this snapshot.
- **Locales:** 11 (10 full + Spanish-partial), generated by `node build-i18n-pages.js` from `i18n/*.json` into `{locale}/*.html`. Run the build after any change to a translated static-HTML string or an `i18n/*.json` value.
- **PMS / Agency Portal / security state:** Phases 47 (auth hardening), 48 (PMS hardening), 49 (Agency Portal hardening) all COMPLETE and deployed. Stored-XSS escaping (`esc()`), agency isolation (`agencyId==auth.uid`), `ROOM_CONFLICT`, and the `status=='Pending'` public-create rule are all live and verified. See `VILU_PROTECTED_CONTRACTS.md` §"Authentication / security protected contracts" before touching any of this.
- **Agency state:** zero real external agency relationships confirmed as of Phase 44 (2026-09-07) — only a bootstrap/test account. No self-registration exists; accounts are staff-created only.
- **Search Console / Yandex:** GSC Domain property verified (DNS TXT, owner-added); URL-prefix property retained for history. Yandex Webmaster verified, sitemap submitted. GA4 `G-1EPZ71Q331` associated with GSC.
- **Public AI-tool exposure:** clean, audited twice (Phase 49 source-level, Phase 55 rendered-production + Firestore-content level) — nothing found. `docs/`, `test/`, `.claude/`, `firestore.rules`, `functions/` all confirmed excluded from hosting via `firebase.json`'s `hosting.ignore`.
- **Known owner-action items:** see `VILU_DECISIONS.md` §"PERMANENT: owner-action register" — currently: the "Mexiczone" legal-operator confirmation, the proposed `room_details` Firestore copy edit (exact before/after in `VILU_COMPLETION_MATRIX.md` Phase 55), the scoped-but-unauthorized `.com` migration option, residual Phase 34 GBP follow-ups (Phase 34 itself is COMPLETE, see `docs/seo/PHASE34_LOCAL_SEO_GBP_STRATEGY.md` §33): the Facebook page name variant, whether the Activities section's Scuba/Watercraft rental/Water skiing checkmarks are accurate, and continuing the review-reply backlog beyond the 10 already published; and Phase 36's follow-on decisions (Phase 36 itself is COMPLETE, see `docs/seo/PHASE36_BACKLINKS_DIGITAL_PR_STRATEGY.md` §32): the Why Maamigili page publication review, whether to pursue NHGAM membership (naming question resolved — only the fee/eligibility inquiry remains), and whether to send any of the six drafted (unsent) outreach templates.

## HISTORICAL

Full dated history of every phase lives in `VILU_CHANGELOG.md` (newest first) and the per-phase detail column of `VILU_COMPLETION_MATRIX.md`. Do not duplicate that prose here — read those two files for "what happened and why." Headline facts worth knowing without opening either: the site was fully redesigned into its current cinematic architecture (Phase 15, deployed 2026-09-06); security/PMS/Agency-Portal hardening (Phases 47/48/49) closed across 2026-09-06/07; the 11-locale international build, full SEO/E-E-A-T/entity/media/CRO/local-SEO program, and Agency/Partner Growth strategy are all COMPLETE (see the matrix for the full list of what remains PENDING).

## FUTURE

- **Roadmap:** `VILU_ROADMAP.md` (this project's original 1–36 sequence, cross-referenced against the matrix's 0–56 numbering where they overlap — the matrix is authoritative for status, this file is authoritative for scope detail on not-yet-started phases).
- **Idea backlog:** `VILU_IDEA_BACKLOG.md` — owner ideas not yet approved for implementation (Vilu Voyager, Vilu Ari Dive/Spice/Griffin, Guest Account/CRM loyalty sub-feature, technical SEO cleanup candidates).
- **Package-first strategy / Vilu Voyager continuity:** `VILU_DECISIONS.md` §"PERMANENT: package-first commercial strategy / Vilu Voyager continuity" — read before Phase 45 (or whichever phase builds Vilu Voyager) begins.
- **Post-56 Growth Operating Model:** `VILU_DECISIONS.md` §"PERMANENT: post-56-phase Growth Operating Model". Once all 56 phases are COMPLETE, Claude's role shifts from building the site to a continuous MEASURE → detect global demand → match to real Vilu services → owner-approved implementation → QA → measure loop, worldwide, not limited to the phases already built. **The Stage 1 gate is now satisfied (56/56 COMPLETE, 2026-09-08)** — Stage 2 is eligible to begin in a future session, but is not started automatically by reaching 56/56; it requires the owner's own separate decision to begin that ongoing operating mode.

## Hard blockers (standing, not phase-specific)

- No production deploy without a fresh, explicit owner authorization for that specific deploy — a past authorization covers exactly the release it named, not standing permission for future ones.
- No merge of this feature branch into `main` has occurred or is authorized.
- No Firestore rules/Cloud Functions/Auth-config deploy without separate, explicit authorization and a stated reason, even when a hosting deploy in the same phase is already authorized.
