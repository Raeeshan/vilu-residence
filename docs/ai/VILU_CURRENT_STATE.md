# VILU — Current State

**Every future Vilu session must read this file FIRST**, then `VILU_ROADMAP.md`, `VILU_DECISIONS.md`, `VILU_PROTECTED_CONTRACTS.md`, `VILU_MASTER_CONTEXT.md`, and `VILU_COMPLETION_MATRIX.md` — in that order — before substantial work. Then state explicitly: CURRENT PHASE / CURRENT STATUS / LAST COMPLETED PHASE / NEXT PLANNED PHASE / BLOCKERS / OWNER APPROVAL REQUIRED? / PRODUCTION IMPACT? Never rely only on conversational memory — this file is the source of truth and must be re-verified against actual repository/git state (branches, SHAs, and file contents drift; this document is a snapshot, not a live query).

**For the full phase-by-phase status of the entire 56-phase project (not just the current one), see [`VILU_COMPLETION_MATRIX.md`](VILU_COMPLETION_MATRIX.md).** This file covers only the currently-active phase in detail.

**PERMANENT, standing across every future session — read `VILU_DECISIONS.md` §"PERMANENT: post-56-phase Growth Operating Model"**: once all 56 roadmap phases are COMPLETE, Claude's role shifts from building the site to a continuous MEASURE → detect global demand → match to real Vilu services → owner-approved implementation → QA → measure loop, monitoring ALL countries (not just the current phase list) for future investment. Do not start this Stage 2 operating mode before Stage 1 (the 56-phase roadmap) is fully complete.

**Snapshot date: 2026-09-07 (updated same day after Phase 39 CRO — see below).**

---

## Phase 39 CRO (Conversion Rate Optimization) — COMPLETE, deployed to production (2026-09-07)

A full conversion-funnel audit (all 9 package cards, the packages-first/accommodation-second bridge, WhatsApp/email contextual pre-fill per package, the Phase 27 China email-primary CSS override, review-proof links, sticky and floating CTAs, mobile/RTL/Dark-Light rendering) found the direct-conversion funnel already unusually mature from prior phases — most of what a generic CRO checklist would suggest was already correctly implemented. Competitor research independently confirmed this: none of Vilu's direct Maamigili competitors except Koimala Inn has a comparably sophisticated funnel, and two (Whale Shark Inn, La Cabana Maldives) have no real owned funnel at all (OTA-listing-only). Real GA4 data was reviewed directly (not inferred): traffic is Instagram-organic-dominated (41% of sessions over 7 days), `package_card_view` (332) and `package_view` (130) are the top engagement events, and one genuine `chatgpt.com/ai-assistant` referral session was found. One real, measured mobile defect was found and fixed: the floating WhatsApp button (z-index 900) visually overlapped the sticky "need help choosing?" bar (z-index 800) on `holiday-packages.html` — both are WhatsApp CTAs occupying the same bottom-right footprint at once when both are visible, confirmed via live `getBoundingClientRect()` measurement on production before fixing. Resolved with a single, narrowly-scoped CSS rule (`body.pkg-sticky-clearance .floating-whatsapp{display:none}`) that hides the floating button only while the sticky bar is actually showing — WhatsApp access is never lost, just never doubled up. Added a real, already-published trust reassurance ("Free cancellation, no deposit — pay in person during your stay") directly at each of the 9 package CTAs — relocating, not inventing, a fact already stated in the page's own FAQ/"why book directly" list — localized across all 10 full locales plus Spanish-partial. Verified live in production that the Chinese email-primary/WhatsApp-secondary override still renders correctly (computed `order` and background-color confirmed via live DOM inspection: WhatsApp `order:2`/transparent, Email `order:1`/gold-filled). Two real opportunities were identified and explicitly declined this phase, documented in `VILU_DECISIONS.md`: hardcoding the real review count/star rating directly on conversion pages (a comparable competitor does this, but it creates an unmaintained stale-data risk with no re-verification mechanism in this codebase); named/attributed guest testimonial quotes (a real, comparable competitor does this well, but selecting which real guests' words to feature is a reputational decision requiring the owner's own review and approval, not a unilateral code change). Confirmed via a sitewide sweep (English + every locale dictionary): zero fake urgency, scarcity, or dark-pattern language anywhere. Added `test/cro-conversion.test.js` (14/14). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph, 27/27 media-SEO, 14/14 CRO, plus the pre-existing 6/6 China-CTA-priority suite unmodified and still passing. Deployed hosting-only, commit `e9e7d83`; post-deploy QA verified live including the mobile overlap fix (screenshot-confirmed) and the trust microcopy rendering correctly. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 39.

## Phase 38 Image & Video SEO — COMPLETE, deployed to production (2026-09-07)

Closed the ~50-instance `related-card-img` alt-text backlog documented since Phase 32/33: alt text previously just repeated the linked page's title (e.g. `alt="Holiday Packages"`) instead of describing the actual photo, and was English-only on every translated page (no localization mechanism wired up at all). Replaced with genuine visual descriptions — reusing each file's already-established alt text where one existed elsewhere on the site, writing new factual descriptions only where none existed anywhere — and wired up `data-i18n-alt` (an existing `build-i18n-pages.js` mechanism, previously unused for these images) so all 10 full locales and the Spanish-partial pages get a real translation instead of a stale English copy. One purely-decorative logo-used-as-thumbnail instance correctly set to `alt=""` rather than a misleading description. Fixed the Firebase Storage tokenized image URL in the homepage `LodgingBusiness` JSON-LD flagged since Phase 33 — replaced with the matching already-shipped, stable `/images/rooms/...` photo of the same room, consistent with how the sitemap generator already exclusively uses that stable URL set (tokens can be rotated/invalidated outside this repo's control; a committed `/images/` asset can't). Researched and documented the VideoObject decision rather than guessing: the homepage hero is a muted, autoplaying, ~10-second decorative background loop — confirmed both under Google's 30-second VideoObject eligibility minimum and semantically the wrong content type for the schema (not a distinct "watch this video" experience) — so no VideoObject was added; hero video markup, JS gating logic, and Phase 21's poster-first LCP strategy were left completely untouched. A broad, evidence-first audit confirmed the rest of the site's media SEO was already clean, so nothing else needed changing: full-site alt text (zero missing/filename-like/overly-long), image filenames (already meaningful, no renaming needed), image dimensions/CLS (`related-card-img` already uses CSS `aspect-ratio`, the correct modern technique), the image sitemap (482 entries covering 34 unique images, all canonical-host, zero broken or Firebase references), and every JSON-LD/OG image site-wide (zero broken references across 160 files checked, including all locale mirrors). Confirmed via direct, authenticated Search Console evidence rather than inference: Image search shows 0 clicks/0 impressions over the last 3 months — expected for a low-overall-traffic site and not itself a defect; this phase is the on-page foundation future visibility depends on. Two research passes ran in parallel with the technical audit: Google Images' current competitive landscape for Vilu's key concepts (dominated by OTAs, niche Maldives travel blogs, the government tourism board, and conservation orgs — individual guesthouses rarely rank for the broad discovery terms), and competitor visual/video intelligence (none of Vilu's direct Maamigili-area competitors — Whale Shark Inn, Shamar Guest House, Koimala Inn, La Cabana Maldives — has a strong owned photo gallery or a confirmed YouTube presence, meaning video/YouTube distribution is a genuinely open opportunity, not one Vilu is racing established players to enter; recorded as Growth Operations backlog — a Maamigili cinematic guide, whale-shark education video, arrival walkthrough, room tour, itinerary video, and package explainer — not built now, per this phase's explicit scope). Added `test/media-seo.test.js` (27/27). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph, 27/27 media-SEO. Deployed hosting-only, commit `27e1a9b`; post-deploy QA verified live including Arabic RTL rendering at mobile width (375px) — related-card photos and translated captions render correctly, no clipping or layout breakage — plus the homepage entity graph and zero new console errors. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 38.

## Phase 37 AI / AEO / Entity Optimization — COMPLETE, deployed to production (2026-09-07)

Built a coherent, stable-@id entity graph across the whole site rather than disconnected repeated entities: homepage `LodgingBusiness`/`Organization` now share one `@id` (`#organization` — same real business, two schema.org types) with a new `WebSite` node referencing it as publisher; standardized `author`/`publisher`/`mainEntityOfPage`/`isPartOf` identically across all 11 Article pages (real inconsistency found — some pages were missing `url`/`logo`/`mainEntityOfPage` entirely); added factual `containedInPlace` geography (Maamigili → South Ari Atoll → Maldives) and a real Wikipedia `sameAs` for South Ari Atoll. Deliberately avoided cross-page `@id` dereferencing for anything affecting rich-results validity — Google's own Article guidance recommends self-contained per-page markup, and cross-document JSON-LD resolution isn't part of how rich-results validation works; same-page `@id` reuse (the Organization/LodgingBusiness merge) is spec-correct and safe. Two research agents ran in parallel: one researched Vilu's external knowledge-graph/AI-search presence (found no visible Sun Siyam Vilu Reef or Raa-Atoll-Maamigili confusion, but found a significant external-only finding — see below — plus several other unrelated "Vilu"-branded guesthouses, and researched the real 2026 evidence for `llms.txt`); one read 7 key pages for AEO/citability quality (answer clarity, information gain, FAQ quality, citation quality, internal linking). Real defects found and fixed at the source, in English and all 10 full locale translations: a citation linking directly to a competitor's own Facebook post (replaced with a neutral third-party Tripadvisor photo listing); a Tripadvisor forum thread cited for a legal claim (replaced with an already-established Maldives.com source used elsewhere on the same page); a "multiple dive-tourism sources" overclaim left over from Phase 35's citation trim (only one source, Divernet, actually remained cited after that trim — this session's own oversight, now corrected); a source-attribution blur between a tourism board's promotional mention and independent science. Added missing in-body entity links (getting-to-maamigili.html → south-ari-atoll-guide.html; holiday-packages.html → 3 guide/experience pages, previously link-present only in the bottom related-card strip). Confirmed via research, not fixed (evidence says no fix warranted): no `llms.txt` (~10% domain adoption, no major AI provider confirmed reading it for citation decisions, one study found it adds noise not signal — revisit only with new evidence); robots.txt/`noindex` directives already correct; Bing Webmaster Tools not connected, no account created (not required). Added `test/entity-graph.test.js` (25/25, covering all 10 full locales). Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T, 30/30 structured-data, 25/25 entity-graph. Deployed hosting-only, commit `b4eca79`; post-deploy QA verified live across English, Chinese, and the homepage entity graph, zero new console errors. **Real external finding, documented not fixed (protected system):** OTA/search results overwhelmingly point to a separate, live legacy domain `viluresidence.com` (different positioning copy — "Best Budget Guesthouse" vs. this site's "Boutique guesthouse") rather than this repository's `viluresidence.net` — a genuine entity/authority-splitting risk worth the owner's attention; `.com` is a protected external system, not edited. AI/answer-engine brand surface confirmed absent for some high-intent queries (competitors currently surface instead) — an external authority-building outcome for Phase 36, not an on-page defect. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 37.

## Phase 35 E-E-A-T / Brand Authority — COMPLETE, deployed to production (2026-09-07)

Full trust/authority audit against an explicit no-fabrication standard, backed by web research verifying every replacement source before use — never inventing credentials, staff, awards, partnerships, reviews, or statistics. Real defects fixed at the source: two low-quality commercial-aggregator citation sources (`airial.travel`, `resortlife.travel`) used across 7 wildlife/factual claims on `maamigili-guide.html` replaced with verified authoritative primaries (MWSRP's own research page, the official Maldives Ministry of Environment SAMPA protected-area page, Divernet) — fixed in the English source AND, after a regression was caught by the newly-added test suite, in all 10 full-locale `i18n/*.json` dictionaries that carried their own independently-translated copies of the same citations; `holiday-packages.html` (the actual booking/conversion page) had zero wildlife-sighting disclaimer despite bundling whale-shark/manta excursions in every package — added a "sightings guaranteed?" FAQ Q&A across visible HTML, FAQPage JSON-LD, and all 11 locale translations; homepage room cards never stated breakfast is included despite it being a confirmed fact elsewhere on the same page — added to all 3 cards, English + 11 locales; a genuine wildlife-hedge inconsistency on the homepage's experience cards (whale-shark overclaimed "but most trips do see them" with no supporting data, manta had no hedge at all) corrected in the English source and all 11 locale translations; `Article` JSON-LD (authorship/dates) added to 3 pages that lacked it, mirroring `maamigili-guide.html`'s existing pattern, with `datePublished` derived from real git history on 8 pages total; `Organization` `sameAs` extended with the already-verified Google Maps CID listing link. Wrote a permanent editorial/E-E-A-T policy into `VILU_DECISIONS.md`. Confirmed adequate as-is (no change): homepage "Who We Are", `LodgingBusiness`/`Organization` JSON-LD (author/publisher correctly absent — not standard properties of those types), no new sitewide trust-badge UI, multilingual trust (spot-checked, no locale inflates "guesthouse" into "resort"/"luxury" framing for Vilu itself). Added `test/eeat-trust.test.js` (12/12) — which itself caught the locale-citation regression described above before it shipped, the same "translate once, forget the other 10 copies" bug class first seen in Phase 33. Full regression suite green: 564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO, 24/24 local-SEO, 12/12 E-E-A-T. Not pulled forward into this phase (by design): backlinks/digital PR (Phase 36), AI/AEO/entity authority (Phase 37), image/video authority (Phase 38), CRO/trust conversion (Phase 39), an original authority/data report (Phase 42), the search moat (Phase 54), Post-56 Growth Operations. "Mexiczone" as the stated legal operator remains unconfirmed with the owner (unchanged again — no new basis found, not a blocker). Deployed hosting-only, commit `366cbdf`; post-deploy QA verified live (maamigili-guide.html citations, holiday-packages.html FAQ count/disclaimer, homepage room-card breakfast badges and sameAs, Russian locale mirror), zero new console errors. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 35.

## Phase 33 Local SEO — COMPLETE, deployed to production (2026-09-07, includes a same-session regression catch-and-fix)

Full local-entity/NAP/Maps/multilingual-place-name audit, backed by a live authenticated check of the real, verified Google Business Profile (5.0 stars, 81 reviews, real address/phone/category/amenities) and current citation/competitor data (Tripadvisor, Trip.com, HotelsOne, Yandex Maps, the still-live legacy viluresidence.com). Real defects fixed at the source: "hotel staff"/"the hotel" business-type wording (Vilu Residence is a guesthouse, never a hotel/resort) across all 11 locales corrected to "our staff"/"our team"; Arabic "Maamigili" spelling fragmented across 3 forms — the two incorrect ones concentrated in title/meta/OG/JSON-LD/breadcrumb — normalized to the single correct ماميغيلي; French "Sud Ari" vs "Ari Sud"/ungrammatical "de Ari Sud" normalized to grammatically-correct "Sud Ari" (one matching Italian elision slip fixed too); legal-page "Alifu Dhaalu Atoll" normalized to the "South Ari Atoll" branding used everywhere else; `getting-to-maamigili.html`'s title disambiguated with "South Ari Atoll" against a real, confirmed entity-confusion risk (an unrelated island of the same name exists in Raa Atoll); `maamigili-guide.html` given an early Vilu Residence introduction (previously buried at item 15 of an 18-item TOC); the site's real NAP address turned into a genuine "Find us on Google Maps" link (stable CID permalink) across all 14 pages; the two wildlife excursion pages linked to `maamigili-guide.html` in-body for the first time. **A real regression was caught during post-deploy QA and fixed within the same session**: the in-body-link fix's embedded JS `I18N.en` fallback object had an unescaped-quote syntax error on both wildlife pages — a real `SyntaxError` that would have broken every script on those pages — caught because a fresh no-cache fetch showed the fix while the live rendered DOM did not, fixed and redeployed immediately, with `test/local-seo.test.js` Case G added to guard this exact bug class going forward. Added `test/local-seo.test.js` (24/24). Full regression suite green throughout (564/564 preservation, 30/30 weather-live, 16/16 accessibility, 68/68 international-SEO). Deployed hosting-only across two commits (`048c24f` + urgent follow-up `0ed0f47`); post-deploy QA verified live including the corrected pages, zero console errors. Documented, not fixed (owner/external): GBP category/description/photos/Q&A optimization; "Mexiczone" as the stated legal operator needs owner confirmation (undisclosed anywhere else on the site, not changed without a basis to know it's correct); Dhigurah's stronger existing whale-shark search association (documented since Phase 23, a destination-name-recognition gap no on-page fix alone can close). Full detail: `VILU_COMPLETION_MATRIX.md` Phase 33.

## Phase 32 International SEO QA — COMPLETE, deployed to production (2026-09-07)

Full hreflang/canonical/lang-dir/metadata/JSON-LD/sitemap/robots/link-integrity audit across all 11 locales and 14 page families. Real defects fixed at the source (never hand-patched in generated mirrors): lang/dir/canonical briefly rendered English/ltr on every non-English page load before the async i18n fetch resolved, now seeded immediately from the URL-detected locale; JSON-LD `Product.offers.url`/`Article.mainEntityOfPage.@id` leaked the English-root URL on every non-English page (new `localizeSelfUrl()` in `build-i18n-pages.js`); `getting-to-maamigili.html` links were broken (real 404s) on all 10 full-coverage locales — `rewriteUncoveredSiblingLinks()`'s fix for this page was only ever reachable for a genuinely partial locale (Spanish), now applies unconditionally; the language switcher could rewrite the visible URL to a locale+page combination with no real file behind it, now guarded by the same per-page coverage check the build script itself uses. Translation-completeness gaps closed: package add-on prices leaking English on every locale's holiday-packages.html; the "Getting to Maamigili" nav label and its two prose links untranslated on all 10 full locales; a hero-image caption untranslated on 4 guide pages; "South Ari" left in English throughout French/Italian's south-ari-atoll-guide.html/manta-ray-snorkeling.html content (58 strings); Spanish's holiday-packages.html title missing its brand suffix; Chinese's inconsistent brand-name rendering (3 variants) normalized to 维鲁公寓, matching Vilu's real, already-live Ctrip/Trip.com listings. Added `test/international-seo.test.js` (68/68). Full regression suite green throughout (564/564 preservation, 30/30 weather-live, 16/16 accessibility). Deployed hosting-only (commit `c0cd2fd`); post-deploy QA verified live across Russian/Spanish/Chinese/Arabic — title/lang/dir/canonical/JSON-LD/links all correct, zero console errors, sitemap clean at 135 URLs with exactly 2 Spanish entries. Two honest residual findings, both non-blocking: ~50 related-card-img alt-text instances across guide/package pages repeat the linked page's title in English rather than the already-translated nav label (real but low-materiality secondary-thumbnail alt text, documented for a future pass); GSC shows 130 "Discovered — currently not indexed" pages, which is Google's own crawl-priority decision, not a Vilu-controlled implementation defect. Yandex Webmaster access remains signed out, unchanged from the prior session. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 32.

## Phase 14 Accessibility Completion — COMPLETE, deployed to production (2026-09-07)

The PARTIAL status noted below (item 1, 2026-09-06) is now resolved. A full public-site accessibility pass fixed every material gap the 2026-09-06 audit found: a real, working skip-link (with `tabindex="-1"` on `#main-content` so the fragment jump actually moves focus, not just scroll position — verified live, since a naive skip-link without this is a common but real failure mode); the page-hero `<header>` changed to a labeled ARIA region, resolving a duplicate-banner landmark conflict against the real site `<header id="site-header">`; every booking-bar/booking-popup/guest-details field (13 inputs) given a real `<label for>`; gallery main image and transport-calculator date/time inputs given proper alt text/`aria-labelledby`; language switcher/floating WhatsApp/sticky CTA given valid ARIA roles and labels; a blank comparison-table header cell given an accessible name; and a real, sitewide Light-theme contrast defect fixed (`--gold`/`--gold-light` used as small-label text measured 3.27:1/4.04:1 against Light's near-white surfaces, below WCAG AA's 4.5:1 — switched to the existing, previously-unused `--accent-text` token, now 5.33:1/4.82:1). The first-ever automated accessibility test suite was added (`test/accessibility.test.js`, real axe-core + jsdom, 16/16 passing across English/Arabic/Russian/Chinese/Spanish × homepage/holiday-packages/one guide page), closing the "zero automated a11y tests, zero Lighthouse/axe ever run" gap. Full regression suite re-verified throughout (564/564 preservation, 30/30 weather-live). Deployed hosting-only to production (commit `9bb4af1`); post-deploy QA performed live on `viluresidence.net` across English/Arabic, Dark theme, keyboard-only navigation, and zero new console/network errors. One honest residual limitation: `.box-label` (shared-page.css tip/info boxes) still has the same Light-theme contrast defect, deferred because it shares a selector with an existing `.warning-box .box-label{color:var(--warn)}` override that needs its own careful, non-cascade-breaking fix — not silently dropped, tracked in `VILU_COMPLETION_MATRIX.md` Phase 14's Remaining column. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 14.

## Master project audit + Phase 47 security hardening — both complete and deployed (2026-09-06)

Same-day sequence: a full 56-phase repository/production/roadmap audit, a deep-dive Phase 47 security architecture/risk audit, minimal verified-safe security cleanup, an owner-performed agency Firebase Auth migration, retirement of the two named default credentials, and a production hosting deployment of the combined security fix. **Current counts: 19 COMPLETE / 15 PARTIAL / 22 PENDING / 0 CURRENT / 0 BLOCKED — see `VILU_COMPLETION_MATRIX.md` for the full table and evidence.** What matters most for any future session:
1. **Phase 14 (Accessibility) was corrected from COMPLETE to PARTIAL** — the prior status wasn't evidence-backed; real gaps exist (no skip-link, incomplete form labeling, zero Lighthouse/axe ever run). Don't re-mark it COMPLETE without an actual audit run.
2. **Phase 47 (Security Hardening) is now COMPLETE WITH DOCUMENTED RESIDUAL LEGACY COMPATIBILITY, deployed to production.** Commit `080874b`: removed the fully dead login-lockout/session-timeout system, a dead unauthenticated agency-session-restore path, and a fully dead embedded legacy agency mini-login; closed a login-form enumeration signal. The owner then completed a real agency login that self-migrated `agency@viluresidence.com` into Firebase Auth (confirmed via Firebase Console, alongside `viluresidence@gmail.com` which already existed). Commit `6e64c68`: with both named bootstrap accounts confirmed migrated, removed their hardcoded/base64 passwords from `loadUsers()`'s `DEFAULTS` entries, added a narrow scrub for stale leftovers on those two specific emails, and fixed a real gap in `migrateLegacyAgency()` (it never scrubbed the plaintext password post-migration, unlike the PMS's own `migrateLegacyUser()`). **Both commits deployed to production hosting 2026-09-06** and verified live: credential strings confirmed absent from the deployed file, fix comments confirmed present, both login pages confirmed loading cleanly with zero new console errors, public website confirmed visually unaffected. `migrateLegacyUser()`/`migrateLegacyAgency()` themselves remain **intentionally, permanently retained by design** — not unfinished work, but what a self-healing migration path is *for*; a full inventory of every real account beyond the two named defaults was never possible from this environment. Full detail in `VILU_COMPLETION_MATRIX.md` Phase 47.
3. **No PMS/booking/pricing/Firestore-rules/UI logic was ever changed** across either commit — only confirmed-dead code removed, one error string normalized, and password material scrubbed for the two now-confirmed accounts.

## Phase 31 Tajikistan Market Expansion — COMPLETE, evidence-based watch-market decision (2026-09-07)

First-party signal remains negligible (GSC 0 clicks/1 impression over 3 months; GA4 0 users over 28 days). Fresh external market research found a materially weaker profile than Uzbekistan's equivalent audit (Phase 29): no dedicated local Tajik travel-agency ecosystem (every Dushanbe-origin Maldives result traced to generic Russian OTA aggregators listing Dushanbe as one of many searchable cities, not genuine local infrastructure), no direct flight (a real ~10h52m connecting itinerary, confirmed via Aviasales), no Tajik-language media coverage specific to Maldives (only unrelated general aviation-route news surfaced), and Yandex's Tajikistan-specific share could not be confirmed this pass (reported honestly rather than assumed to match Kazakhstan's/Uzbekistan's ~25%). The already-verified country-neutral Russian locale (unchanged since the Phase 29 audit) remains adequate should real demand emerge. Any real conversion in this corridor would flow through large Russian OTA intermediaries rather than direct booking — a future B2B/agency relationship (Phase 44's domain, not changed here) is the more relevant lever than localization. No new page, no production changes. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 31, `docs/seo/MARKET_INTELLIGENCE.md` Part 9.

## Phase 29 Uzbekistan Market Expansion — COMPLETE, evidence-based watch-market decision (2026-09-07)

First-party signal remains negligible (GSC 0 clicks/1 impression over 3 months; GA4 0 users over 28 days), but fresh external market research found meaningfully stronger evidence than Kazakhstan's equivalent pass: a real, active ecosystem of dedicated Uzbek travel agencies selling "Мальдивы из Узбекистана" packages (all in Russian), genuine recurring Uzbek-language media coverage since 2020 (most recently January 2026), a real seasonal direct Tashkent–Malé flight in 2026 (Centrum Air, ending this same month), and a confirmed 24.8% Yandex search share (already covered by the existing Phase 26 Yandex verification). Directly verified the existing Russian locale (`ru/index.html`, `ru/holiday-packages.html`, Maamigili/whale-shark/manta pages) is genuinely country-neutral — zero Russia-specific wording — and already adequate for this audience; the market's own commercial language is confirmed Russian, not Uzbek, so no dedicated localization is justified. No Vilu-controlled content gap found; no new page. One non-blocking recommendation for the owner's own discretion: consider Telegram as a supplementary contact channel, given its confirmed real relevance in this exact regional travel-commerce context. No production changes made. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 29, `docs/seo/MARKET_INTELLIGENCE.md` Part 9.

## Phase 28 Kazakhstan Market Expansion — COMPLETE, evidence-based watch-market decision (2026-09-07)

Live GSC (3-month) and GA4 (28-day, all channels) account data both show zero measured Kazakhstan signal — absent entirely from GSC's full 63-country table (0 impressions, 0 clicks) and GA4's full 45-country breakdown (0 active users). **Decision, owner-accepted: watch/measure, do not build speculative content.** The existing, already-complete Russian locale (Phase 26) is the appropriate current-demand experience; Yandex (Kazakhstan's real secondary search engine) is already verified for `viluresidence.net` from Phase 26, with no Kazakhstan-specific setup needed. No dedicated Kazakh locale, no new page — this is deliberate, not abandonment. Future reopen triggers (no arbitrary threshold pre-committed): sustained GSC impressions/clicks, GA4 users, package enquiries, bookings, a real Kazakhstan-origin query cluster, OTA/agency demand, or a credible partnership opportunity — detected via the dynamic `docs/seo/SEARCH_MONITORING_FRAMEWORK.md`. No production changes made. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 28, `docs/seo/MARKET_INTELLIGENCE.md` Part 9.

## Phase 30 Spain Market Expansion — COMPLETE, deployed and live (2026-09-07)

No Spanish locale existed anywhere before this phase — confirmed, not assumed. **Architecture decision: Option B — Holiday Packages + homepage only, not a full 12-page mirror**, an evidence-driven, quality-over-quantity scope decision. Both are now live: `viluresidence.net/es/holiday-packages.html` and `viluresidence.net/es/`. The Spanish homepage preserves the approved cinematic architecture exactly — no redesign. `build-i18n-pages.js` gained a `rewriteUncoveredSiblingLinks()` function so partial-locale pages never link a bare relative href to an untranslated sibling page (would 404 under `/{lang}/`); verified a no-op for the 10 established languages, and confirmed live (every `/es/{guide}.html` 404s, every root English guide 200s).

**A real, live production bug was found, fixed, and deployed**: the client-side i18n engines (`shared-page-i18n.js` and `vilu-website.html`'s own copy) never recognized `es` as a language code, so on every visit to any `/es/` page the client silently reverted the correctly server-rendered Spanish content back to English after JS ran — live on the deployed Packages MVP since its original deployment. Fixed (`LANG_NAMES` now includes `es` in both engines) and confirmed live post-deploy: `document.documentElement.lang`, title, canonical, and all visible content stay Spanish after JS runs, on both pages. Finding this bug also surfaced a **translation-completeness gap**: `i18n/es.json` was missing 87 static + 77 dynamic keys reachable only through JavaScript (not static HTML attributes) — all now translated; the dynamic dict is at the reference 115-key count matching every established language. A pre-deploy audit also found and fixed a missing `/es`/`/es/` hosting rewrite + no-cache header in `firebase.json` (every other language had one). A second, unrelated, pre-existing bug (the live-weather widget's labels render in English on ALL languages, confirmed on live `zh/`) was found and flagged separately as its own task, not fixed here.

Full regression suite green: 564/13/82/21/26/5/6. `git diff --check` clean, zero PMS/backend diff. Deployed hosting-only; live QA covered HTTP status, runtime lang/title/canonical, hreflang/og:locale, sitemap (135 URLs, exactly 2 `/es/` entries), console/network (zero errors, no WeatherAPI key exposure), responsive (320/375/768/desktop), Dark/Light theme, and non-Spanish regression (zh/ar/ru/en all confirmed unaffected). Full detail: `VILU_COMPLETION_MATRIX.md` Phase 30, `VILU_DECISIONS.md`, `VILU_CHANGELOG.md`.

## Phase 27 China Market Expansion — COMPLETE (2026-09-06)

An evidence-driven audit + a small, targeted set of copy fixes, mirroring the Phase 26 Russia methodology. **Technical `/zh/` architecture confirmed already correct**: `lang="zh"`, self-canonical, 12 hreflang tags, JSON-LD on all 12 pages, zero locale-asset 404s. **The positioning risk is real but softer than Russia's**: Chinese content price-compares "民宿"/"居民岛" against resorts, but also carries an existing "authentic local experience" counter-narrative Russian content lacked. Fixed the same 5 high-leverage strings (hero subtitle, footer, packages subtitle + meta description, South Ari guide meta description), extending the homepage `<title>` tag's own pre-existing "精品民宿" qualifier consistently — zero commercial-fact changes. **Zero existing Chinese content mentions Maamigili by name** (worse gap than Russian); Dhigurah still dominates Chinese whale-shark content. **Major new finding: Vilu Residence already has live Trip.com (9.7/10) and Ctrip (维鲁公寓, 4.5/5) listings**, with two Maamigili competitors already whale-shark-branded on Ctrip — a real OTA-optimization opportunity (owner/business-development action). **Baidu confirmed a much harder blocker than Yandex**: registration itself requires a Chinese phone number, likely infeasible without a local partner — recommended deprioritized. Xiaohongshu personal accounts need no business license (realistic entry point); WeChat Official Accounts are gated for foreign entities (a personal WeChat ID is the realistic alternative — no account built). A dedicated Chinese mirror of `getting-to-maamigili.html` was evaluated and not created — the transport info is already localized inside `/zh/maamigili-guide.html`.

**The final China-specific implementation item — the mainland-China conversion path — is now deployed.** WhatsApp is blocked in mainland China without a VPN, yet Vilu's closing CTAs hardcoded WhatsApp as visually primary sitewide. Fixed with a single `html[lang="zh"]`-scoped override in `shared-page.css` (Email now primary/first, WhatsApp secondary/second, using the site's own design tokens) — zero markup/tracking changes, zero effect on any other locale, confirmed via `git diff` and a live production check against English. Deployed hosting-only (`ec1816c`, 5 files: `i18n/zh.json` + `shared-page.css` + 3 regenerated `zh/*.html` mirrors) after independently re-verifying the true live baseline (`96536fe`). Live-verified: correct CTA hierarchy on a fresh production tab, Dark/Light both clean, zero overflow at 320px, zero console errors, English/Russian confirmed unaffected. **Phase 27 is now COMPLETE** — per the owner's own explicit completion principle, Baidu/Xiaohongshu/Trip.com/WeChat are ongoing external/channel operations, not phase-completion blockers, the same precedent Phase 26 established for Yandex. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 27, `VILU_DECISIONS.md`, `VILU_CHANGELOG.md`.

## Phase 26 Russia Market Expansion — COMPLETE (2026-09-06)

An evidence-driven audit + a small, targeted set of copy fixes, not a rewrite. **Technical `/ru/` architecture confirmed already correct**: self-canonical, 12 hreflang tags, JSON-LD on every page, all 11 guide/package pages + homepage in the sitemap, zero locale-asset 404s (consistent with the same-day compliance/QA pass above). **The owner's "don't sound cheap" concern is real and evidence-backed, not hypothetical**: multiple independent Russian-language sources (found both via direct web search and a dedicated research agent) consistently frame "гестхаус"/"локальный остров" through a budget/discount lens, with Maafushi — not Maamigili — dominating as "the" affordable local island, and one existing third-party page literally titled "Маамигили — Бюджетный Отдых на Мальдивах." Fixed the 5 highest-leverage instances of this framing in Vilu's own Russian copy (homepage hero subtitle, sitewide footer description, holiday-packages on-page subtitle, holiday-packages SEO meta description, South Ari guide SEO meta description) — repositioned toward brand/experience-led language, zero commercial-fact changes. Deliberately left untouched: the guesthouse-vs-resort comparison page and factual/comparative guide body copy, where the term is topically appropriate. Dhigurah still dominates Russian whale-shark content (same gap Phase 23 found in English) — an off-page/authority problem, not an on-page one; Vilu's own Russian whale-shark page already correctly targets the right keywords and needed no rewrite. Yandex Turbo Pages confirmed discontinued (April 2025); Yandex.Metrica deliberately not added, pending consent review. A dedicated Russian mirror of `getting-to-maamigili.html` was evaluated and not created — the transport info is already localized inside `/ru/maamigili-guide.html`.

**Deployed to production hosting-only** (`b67a3e5`, 4 files: `i18n/ru.json` + the 3 regenerated `ru/*.html` mirrors), after independently re-verifying the true live baseline (`c08d53e`, confirmed via byte-diff, not assumed) and confirming the hosting diff was restricted to exactly those 4 files with zero backend/PMS drift. Live-verified on `viluresidence.net`: all 3 changed pages return the exact new copy, correct `lang="ru"`/canonical/hreflang/sitemap inclusion, no noindex, zero console errors on fresh tabs, zero horizontal overflow at 320/390/430/768/1440, Dark and Light both clean, all 9 package prices/nights unchanged, no wildlife guarantee or resort misrepresentation introduced. **Yandex Webmaster verification deployed** (HTML-file method, `yandex_e65dfced1160fa76.html`, commit `745c24b`) and **owner-confirmed verified**; the owner also submitted `sitemap.xml` in Yandex Webmaster. **Honest baseline recorded, nothing invented**: ownership verified, sitemap in Yandex's processing queue, no indexed/query/impression/click/position data yet (Yandex states ~1-2 weeks for initial processing). A Russia measurement plan (Yandex Webmaster + GSC filtered to Russia + GA4 segmented by `ru`, all using existing tools/events, no new tracking) and a prioritized, legitimate-only Russia off-page/distribution operating plan (Level.Travel category listing, existing OTA-listing polish, editorial/Telegram outreach, VK/Yandex Business flagged as needing more evidence) are both recorded in `VILU_DECISIONS.md`. **Phase 26 is now COMPLETE** — per the owner's own explicit completion principle, external Yandex crawl/ranking timing is an ongoing-monitoring item, not an implementation-completion blocker; all Vilu-controlled Russia-market implementation and measurement setup is finished. Full detail: `VILU_COMPLETION_MATRIX.md` Phase 26, `docs/ai/VILU_ROADMAP.md` Phase 12, `VILU_DECISIONS.md`, `VILU_CHANGELOG.md`.

## Phase 24/25 Content Authority Workstream — COMPLETE, DEPLOYED TO PRODUCTION (2026-09-06)

`getting-to-maamigili.html` is now **live at `https://viluresidence.net/getting-to-maamigili.html`** — the single sharpest, evidence-backed content gap identified in Phase 23 (`SEARCH_INTELLIGENCE.md` §9: no dedicated transport/getting-there page existed on `.net`, while the legacy `.com` site's equivalent page actively ranks for a real query). Covers real, sourced transport logistics (domestic flight vs. direct speedboat, Villa International Airport, arrival/departure, international connections, weather disruption) plus a Trip Preparation section (IMUGA Traveller Declaration, SIM/connectivity, packing, motion sickness, first/last-day timing) and a visible FAQ section (8 Q&A, matching the FAQPage schema exactly — added during pre-deploy review after the schema was found without matching visible content), all current pricing/schedule references linking to the live `/#travel-info` widget rather than hardcoded. English-only by deliberate design. Cross-linked from `maamigili-guide.html` and the homepage's "Getting Here" tab; added to sitewide nav/footer "Planning" group on all 12 other English source pages. No itinerary or cost/planning page created. Full inventory: `docs/seo/CONTENT_AUTHORITY_MAP.md`.

**Deployment record:** actual pre-deploy production baseline was independently verified via direct byte-for-byte diff of the live site against repository commits (never trusted from documentation or `origin/main`, which remained stale at `1130e1e3`) — found to be commit `f264aa3`, not any doc-cited SHA. Implementation commits `a94aab4` (feature), `2ec00c8` (pre-production docs), `befcb8b` (FAQ-visibility fix, caught in pre-deploy SEO review), `396fa2f` (sitemap hreflang-consistency fix, caught in post-deploy live review). Deployed hosting-only (`firebase deploy --only hosting --project vilu-residence`, site `viluresidence`) in two passes (the second for the sitemap fix). Post-deploy: live page HTTP 200, zero console errors on a fresh tab, zero 404s on any loaded resource, sitemap at 133 URLs with the new entry appearing exactly once (zero hreflang tags, correct for an English-only page), zero accidental `/ru/`, `/zh/`, `/de/`, `/ar/` locale mirrors, internal cross-links confirmed live on both the homepage and `maamigili-guide.html`, `getting-to-maamigili` confirmed present in the live `analytics.js` landing-slug list, zero overflow at 320/375/430/768/1280/1440, Dark and Light both verified, PMS/Agency Portal/Firestore/Functions/`.com`/`origin/main` all confirmed untouched. Test suite 558/13/82/21, all green throughout. See `VILU_COMPLETION_MATRIX.md` Phases 24/25 and `VILU_DECISIONS.md` §"Content authority / trip-preparation content" for full decision rationale.

## Phase 17/20 Live Destination Experience — COMPLETE, DEPLOYED TO PRODUCTION (2026-09-06)

The `#destination-now` homepage component (Maamigili/South Ari Atoll → Current Conditions / Local Time / Sunrise-Sunset / 3-day forecast, placed between `#travel-info` and `#reviews`) is **live at `https://viluresidence.net/`** with **real WeatherAPI.com free-tier data ($0/month)**. Phase 17 (design, owner-approved in full: placement/Dark/Light/mobile/tablet/desktop/RTL/motion) and Phase 20 (real-data implementation) are both complete and deployed. Architecture: a dedicated, separate repository (`github.com/Raeeshan/vilu-weather-cache`) runs a scheduled GitHub Action every 20 minutes against WeatherAPI's free tier, publishing a keyless `weather-cache.json` the Vilu client reads directly from GitHub's free public CDN — no server, no Firestore, no Cloud Function, no client-exposed key, and `vilu-residence`'s own `main` is never touched by any of it. Deployed hosting-only; true pre-deploy baseline independently verified via live-content diff (`396fa2f`), not assumed. **Live post-deploy verification was performed directly on `viluresidence.net` itself**: real temperature/condition/local-time/sunrise/sunset/forecast all confirmed rendering, correct "Maamigili · South Ari Atoll" labeling (the provider's own "Mahibadhoo" geocoding resolution never surfaces in the UI), attribution visible, zero direct browser calls to `api.weatherapi.com`, zero overflow at 320/375/390/430/768/1280/1440, Dark/Light/Arabic-RTL all correct. Tests 558/13/82/21/26 (Vilu) + 12 (weather-cache repo), all green. See `VILU_COMPLETION_MATRIX.md` Phase 20 and `VILU_DECISIONS.md` for full detail, including a real bug found and fixed in the cache-publishing workflow.

**POST-PHASE-20 compliance/QA correction (2026-09-06, same day) — this remains a QA fix, not a new phase, and does not change Phase 20's COMPLETE status.** Two items resolved: (1) the disclaimer claim above was incomplete — WeatherAPI.com's terms require a standing end-user disclaimer separate from mere attribution, now added as a compact always-visible static line, verified in Dark/Light/mobile/RTL; (2) the incidental locale-image-path 404 bug (mentioned above as "flagged separately") is now root-caused and fixed at the source (relative JS string literals in `EXPERIENCES_CONFIG`/`HP_PKG_IMAGES`/the hero-video picker made root-absolute), verified via live network checks across all 10 locale mirrors. Full detail in `VILU_CHANGELOG.md` and `VILU_DECISIONS.md` §"POST-PHASE-20 compliance/QA correction".

## Current phase

Five major workstreams that were active this session are now **COMPLETE and deployed to production**: Phase 15 (cinematic visual redesign), Phase 47 (minimal security hardening), Phase 24/25 (Content Authority Workstream), and Phase 17/20 (Live Destination Experience — design and real weather data, above). See `VILU_COMPLETION_MATRIX.md` for the full 56-phase picture and the recommended next phase.

**PHASE 15 — CINEMATIC VISUAL REDESIGN — COMPLETE, deployed 2026-09-06** (commit `dcfe40b`). All previously-pending visual defects (property white-fade, availability, packages, gallery, hero video/brightness, Below South Ari curved panel) fixed, deployed, and verified live on `viluresidence.net`.

**PHASE 47 — SECURITY HARDENING — COMPLETE WITH DOCUMENTED RESIDUAL LEGACY COMPATIBILITY, deployed 2026-09-06** (commits `080874b` + `6e64c68`, combined into one production release). See above for detail.

Owner approval: **YES for both — explicit production deployment authorizations, 2026-09-06**
Production impact: **YES for both — hosting-only, verified live the same day**

## Branch / commit state

- Feature branch: `feat/vilu-reference-design-system`
- Latest commit on this branch as of this snapshot: `e2f4277` (most recent history: `5713cf6` Phase 20 dedicated-repo migration → `8278dd0`/`e2f4277` Phase 20 docs [all deployed]). This is not pushed to any remote — `origin/main` remains untouched and behind.
- **Deployed to production, in four separate hosting releases** (all hosting-only, `firebase deploy --only hosting --project vilu-residence`, site `viluresidence`): `dcfe40b` (2026-09-06, cinematic redesign), the combined `080874b`+`6e64c68` (2026-09-06, security hardening), `396fa2f` (2026-09-06, Phase 24/25 Content Authority Workstream), and `e2f4277` (2026-09-06, Phase 17/20 Live Destination Experience — real weather data). **Each release's own pre-deploy baseline was independently verified via live-content diff, never assumed from a prior doc entry** — the Phase 24/25 release's true baseline was `f264aa3` (not `origin/main`); this latest release's true baseline was `396fa2f` (not `f264aa3`, which by then was two releases stale — a live re-check caught this rather than trusting the last report's own citation). Pre-redesign production baseline was `1130e1e328ac8ac0df88f26b5523ff34201c80c0`; that commit remains `origin/main`'s HEAD — every release so far has been a hosting content release, not a git merge to `main`.
- **Always re-verify both SHAs with `git fetch && git rev-parse origin/main` and `git log` on the feature branch before relying on them — do not trust this file's numbers if the repository shows otherwise.**
- Primary worktree (`C:\Users\hp\vilu-residence`) is intentionally dirty (~199 pre-existing uncommitted legal/privacy files) and must never be used as a build/deploy source or touched/reset. All work happens in the dedicated worktree `C:\Users\hp\vilu-residence-reference-design`.
- Live preview channel: `https://viluresidence--vilu-reference-design-system-bgmh161r.web.app/` (Firebase Hosting preview channel on the `viluresidence` site) — remains available for any future round of changes on this branch.
- Live production domain: `https://viluresidence.net/` — serving the cinematic redesign since 2026-09-06, and the security-hardened PMS/Agency Portal since the same day's second deploy.

## What's done in this phase so far

- Removed the floating whale-shark/manta/turtle PNG-cutout animation and the sitewide ambient bubble/particle field (owner-flagged priority defect — "damaged," "artificial," "cheap").
- Replaced Below South Ari's cutout scene with real underwater photography (`whale_shark_2_gallery.jpg`), reveal-on-scroll.
- Added real photography to previously text-only homepage sections: What Is Vilu, Experiences (per-excursion thumbnails), Closing.
- Fixed a real race condition: Below South Ari and Homecoming images were `loading="lazy"` while gated behind an IntersectionObserver-triggered reveal — the reveal could finish before the image loaded, rendering blank. Fixed (removed lazy-load, switched from a clip-path mask to an opacity fade).
- Fixed a real bug: the booking popup's background rendered as a broken light-to-dark wash in Light theme (hardcoded dark gradient stop). Now a solid theme-aware surface.
- Reduced the Descent transition section: `140vh` → `90vh` → `56vh` (final pass added a static radial glow for depth; no floating shapes/particles).
- **Replaced the entire hero video** with a genuine 10-second segment of real drone footage (`Island beach.mov`, previously unaudited in the repo), natural 1.0x playback, no audio, new JPEG posters. Commit `16dcfaf` additionally hardened the export's color-range handling as a defensive measure (the originally-suspected "13% darker" reading turned out to be a flawed crop-vs-full-frame comparison, not a real bug — see `VILU_DECISIONS.md`).
- **Hero → next-section hard cut — FIXED** (`16dcfaf`): the hero scrim's final gradient stop only reached .50 opacity, leaving real photography visible right up to the flat section boundary. Extended the last ~20% of the scrim to .94 opacity so the footage visually settles into dark before the boundary.
- **Property/Homecoming white fade — FIXED** (`8e4d0a1`): root-caused to the reveal-on-scroll system being gated on a single IntersectionObserver threshold crossing with no fallback — a direct anchor-link landing, bfcache restore, or fast scroll could leave the pre-reveal 45%-opacity frame stuck indefinitely, reading as a washed-out fog specifically on Homecoming's light cream backdrop (Below South Ari's dark backdrop hid the same latent bug). Raised the pre-reveal floor to .82 and added a first-visibility-scoped 900ms safety-net timer that guarantees the reveal completes.
- **Availability composition — FIXED** (`8e4d0a1`): replaced the single flex row (4 fields + CTA competing for space) with a balanced auto-fit field grid + one full-width, taller CTA button on its own row. Fixed a real 320px overflow this surfaced (the button's own `white-space:nowrap` clipped its label at narrow widths) by scoping the fix to `.la-field`/`.la-btn` only, never the shared `.bwf-*` classes the booking popup/guest-details form also use. Verified zero overflow at 320/375/390/430/768/1024/1280/1440/1920, English and Arabic RTL.
- **Homepage package arrangement — FIXED** (`8e4d0a1`): replaced the 1-giant-card + 8-item horizontal rail with 1 flagship + 2 secondary photographic cards, using the same editorial selection (Most Popular / Best Value / Honeymoon Special) already proven on `holiday-packages.html`'s own "Featured Journeys" panel. The other 6 packages remain real/bookable via "Explore All Packages." `holiday-packages.html` itself was inspected and left unchanged — its Featured Journeys composition and auto-fit Full Collection grid were already strong. A regression this surfaced (the taller new layout made `#hp-grid`'s scroll-reveal threshold geometrically unreachable) was fixed by removing the reveal gate from that element; a protected no-JS contract this also initially broke (all 9 packages must stay crawlable in the static fallback) was restored.
- **Gallery — FIXED** (`8e4d0a1`): replaced the equal-width horizontal scroll rail with one dominant image + a slim thumbnail strip (click-to-swap, counter/caption, arrows), same 6 real photos, no new library.

## Not yet done in this phase

- **Final exhaustive Dark/Light/mobile/desktop/multilingual QA** — a solid verification pass was done this session (overflow-checked at all 9 required widths, spot-checked ar/ru/zh/de/ja + Arabic RTL at 390/1440, screenshotted the 4 fixed sections in both themes, plus a full live-production sweep after deploy), but a full page-by-page sweep of every guide page, the full Packages page, and Rooms has not been repeated this session — folded into `VILU_COMPLETION_MATRIX.md` Phase 51 (Final Technical QA) rather than kept open indefinitely against this phase.
- Weather/time visual prototype — not started (see below), correctly not blocking this phase.

## Live Destination Experience (weather/time) — status

**PLANNED, design-prototype stage not yet started.** No API has been chosen, no code written. See `VILU_ROADMAP.md` Phase 2 and `VILU_IDEA_BACKLOG.md`. Explicitly: do not connect any live weather API before the hero/visual-correction phase is owner-approved, and do not let this feature block or delay the current hero work.

## Immediate next action

**Phase 23 (Search Intelligence) is COMPLETE (2026-09-06)** — a full research pass combining real GSC query/country/device analysis, real Google Trends research, real SERP checks, and three dedicated market-research passes (Russia, China, Kazakhstan/Uzbekistan/Spain/Tajikistan). **The most important findings**: (1) whale-shark/manta/snorkeling terms — Vilu's actual core differentiator — don't yet appear anywhere in its own real GSC query data, despite Google Trends confirming genuine, near-year-round search interest in "whale shark Maldives" worldwide; (2) Maamigili's own destination-name search volume is far smaller than competing local islands Maafushi/Dhigurah/Thulusdhoo, and "dhigurah" repeatedly surfaces as a Trends-rising related query for both "Maamigili" and "South Ari Atoll," with real SERP evidence (a Facebook thread, a YouTube video) showing Dhigurah currently owns the "where to stay for whale sharks" narrative that Maamigili should also be part of; (3) Vilu is already listed by name on Tripadvisor and HotelsOne, and has a real 22.5K+-follower Instagram account and a populated Google Business Profile — none of which the website itself currently leverages, and the site doesn't yet rank on its own for "Maamigili hotel" despite these third-party endorsements; (4) **a genuine, separate, live legacy website was discovered at `viluresidence.com` (not the canonical `.net`), using the same phone/address/email as the real business — flagged clearly for owner attention as the closest thing to a critical finding this research surfaced; nothing was changed, no DNS/domain action was taken**; (5) Germany (247 impressions/1 click) and Turkey (226 impressions/0 clicks) are real, evidenced quick-win CTR candidates; (6) the 130-not-indexed-pages question was diagnosed with real evidence (new-site status + a large single-batch 11-language sitemap launch + weak internal-link signal reaching Google — no technical defect found) rather than guessed at. Full detail, priority scoring (P0-P3), and a roadmap-sequencing recommendation live in `docs/seo/SEARCH_INTELLIGENCE.md`, `COMPETITOR_GAPS.md`, `INDEXING_ANALYSIS.md`, and `MARKET_INTELLIGENCE.md`. No website code, DNS, or production changes were made — this was research/measurement only, as instructed. **Phase 24/25 (Content Authority Workstream) is now COMPLETE, pre-production — see the dedicated section above.**

**Same-day reconciliation (2026-09-06):** per explicit owner correction, every finding above was re-checked against the live current `.net` site, and `.com` was audited separately as the legacy domain only. One finding was corrected — `whale-shark-snorkeling.html` already exists on `.net` as a real, substantial, well-linked page; the actual gap is that it isn't indexed yet, not that content is missing. Two findings were sharpened with new evidence: **the missing transport/getting-there page is now the single most concrete content gap found**, since `.com`'s equivalent page was confirmed to actively rank for a real query `.net` cannot capture at all; and **the `viluresidence.com` situation is more significant than first reported** — it has at least 6 pages indexed by Google (more than `.net`'s 1) and a full sitemap/301-mapping inventory is now prepared in `COMPETITOR_GAPS.md` for a *future, separately-authorized* migration decision. No redirect, DNS, or domain change was made or proposed as executed.

**Phase 22 (Google Search Console) is COMPLETE (2026-09-06)** — a real, working Search Console property with genuine baseline data was found, GA4 is associated, and the new Domain property is now verified. The first pass's "NOT CONFIGURED" conclusion was correct about DNS/meta-tag verification but incomplete: using the owner's own already-authenticated Google session in a connected browser, a real, already-verified Search Console property (`https://viluresidence.net/`, URL-prefix, verified since ~2026-08-02) was discovered — it doesn't show up in static site/DNS checks because of its verification method. Real data captured directly from the UI: 36 clicks/1,720 impressions/2.1% CTR/5.9 avg. position over 3 months; sitemap already submitted (Aug 3) and processing successfully (132 pages); only the homepage is actually indexed (131 other pages are "discovered, not yet indexed" — expected for a young property, not a defect); real international query/country demand exists (Czechia, Italy, Germany, Turkey, Spain, Russia, Japan, Tajikistan, Uzbekistan, others — Germany/Turkey specifically flagged as quick-win candidates: high impressions, near-zero clicks); Google Travel/Hotels already references the homepage; zero manual actions or security issues. GA4 (`G-1EPZ71Q331`/`420109910`) is formally associated with Search Console, confirmed on both properties. A new Domain property (the preferred long-term architecture) was created, and across **four separate explicit owner re-authorizations** to add its DNS TXT record via the connected browser — including one where the owner stated they were personally signed into Cloudflare, and one where the owner reported verification had already succeeded (checked directly and found not yet real at that point) — **Claude never opened Cloudflare, never checked its session state, and never added, edited, or removed any DNS record.** Modifying DNS/system-level infrastructure is a standing boundary, not a scope question to be corrected by rewording a request. **The owner then added the DNS TXT record themselves**; this was independently re-verified via two public DNS resolvers and a live Search Console Verify click, which returned "Ownership auto verified." Both properties are retained — the historical URL-prefix property keeps its full history, and the new Domain property inherits the same sitemap and GA4 association automatically. See `docs/seo/GSC_BASELINE.md` and `VILU_COMPLETION_MATRIX.md` Phase 22 for the full record. No production website changes were made this pass — only Search Console/GA4 configuration, and the one DNS record was added exclusively by the owner.

**Phase 21 (Performance) is COMPLETE and DEPLOYED TO PRODUCTION (2026-09-06)** — commits `2aa4d04` (Descent reflow deferral) + `4605344` (dolphin-chip thumbnail; its gallery-thumbnail portions were separately reverted by `f264aa3`/`84384ad` and never shipped) are now live on `https://viluresidence.net/`, hosting only. Pre-deploy audit computed the real delta against the actual last-deployed production commit (`6e64c68`, not `origin/main`) and confirmed via direct `git diff` (not memory) that only `vilu-website.html` + 10 locale mirrors + the new dolphin thumbnail asset changed — zero Firestore/PMS/Agency-Portal/Homecoming/gallery/hero drift. Post-deploy verification found zero regressions and a Lighthouse smoke check landed within the pre-existing noisy baseline range. See `VILU_COMPLETION_MATRIX.md` Phase 21 for full detail. **The Firestore/mobile-LCP investigation is closed out** — thoroughly audited (every entry point inventoried, exactly one Firebase/Firestore instance confirmed structurally guaranteed, zero real-time listeners anywhere), and an earlier overclaim corrected with better evidence (a "~5.7s long task" figure was a `simulate`-throttling modeling artifact, not real measured execution time; real DevTools-throttled and live-browser testing both show the actual cost is the page's own Style/Layout/Rendering work, not Firestore's network polling). Do not re-investigate this without new evidence. **The Homecoming/courtyard responsive-image question is also closed out** — real per-breakpoint measurement (320/375/390/430/tablet/1440/1920) found the container's actual size is driven by the source image's own 3:4 aspect ratio, so the physical resolution real users need already meets or exceeds the native 900×1200 asset at every tested breakpoint; smaller dimension-based derivatives would have upscaled the image for most users, so none were created — a legitimately safe "no change" outcome, not neglected work. **What's still genuinely open and deliberately deferred, non-blocking for Phase 21 per the owner's own rule**: gallery-thumbnail derivatives, now failed twice via two different architectures with the same unexplained live-browser decode bug — see `VILU_PROTECTED_CONTRACTS.md` §Performance findings before attempting a third time, ideally with real Chrome DevTools Protocol access this session doesn't have; any future Firestore-transport-level tuning. **Production deployment of the two safe, standing Phase 21 fixes (`2aa4d04` + the dolphin-chip commit) is complete** — deployed 2026-09-06, live on `viluresidence.net`. **Important lesson reinforced twice this session**: an automated test suite passing is not the same as verified-safe — always verify a "safe-looking" performance change in an actual, fresh browser before trusting it. **Also learned this pass**: on this Windows environment, the `convert` command on PATH is the OS's FAT→NTFS disk-conversion utility, not ImageMagick — never invoke it on an image file; there is no local WebP encoder available (the bundled ffmpeg build has no libwebp support, and there's no local Python/Pillow), which limits what image re-encoding work is safely testable offline. Two other active workstreams (visual redesign, security hardening) remain shipped and verified from earlier sessions. Retiring `migrateLegacyUser()`/`migrateLegacyAgency()` themselves is **not** on the todo list. Phase 17 (weather visual prototype) remains next in the roadmap's own numeric sequence whenever the owner wants it. Phases 48/49 (PMS/Agency Portal hardening) have precisely-itemized remaining scope in `VILU_COMPLETION_MATRIX.md` if the owner wants to pursue them next.

## Hard blockers

- **Phase 22's Domain-property DNS TXT record was added by the owner personally and verified 2026-09-06** — this item is now resolved, not open.
- **Phase 21 performance fixes (`2aa4d04` + `4605344`) were deployed to production 2026-09-06** — this item is now resolved, not open.
- No further production deploy without a fresh, explicit owner authorization for that specific deploy — each of the 2026-09-06 authorizations (including this one) covered exactly one hosting release, not standing permission for future ones.
- No live weather/time API connection until a provider is chosen per `VILU_ROADMAP.md` Phase 2/6's own gating steps, and the phase is separately authorized.
- No merge of this feature branch into `main` has occurred or is authorized — production was updated via `firebase deploy --only hosting` (content releases), not a git merge; `origin/main` is still at the pre-existing baseline `1130e1e3`.
