# VILU — Decisions Log

Owner-approved design/business/technical decisions. These are settled — don't re-litigate them without a new, explicit owner instruction. Dated where the date is known; several predate this documentation system and are recorded from established project history.

---

## Standing workflow

**ChatGPT ↔ Claude permanent workflow:** ChatGPT is the architecture/strategy/review gate; the owner relays ChatGPT's instructions to Claude Code verbatim; Claude executes exactly one approved stage at a time, verifies, reports in the exact required format, then stops for review. Claude must never autonomously advance phases, commit, push, deploy, or touch business/pricing/backend data without current, explicit authorization for that specific action. This applies to every phase in `VILU_ROADMAP.md`, not just the current one.

## Identity and positioning

- Vilu Residence is a real six-room local-island guesthouse — never presented as a resort, never implied to have resort facilities or a private island. (Standing.)
- Vilu (the brand) is broader than Vilu Residence (the property) — the long-term platform vision spans destination content, packages, and eventually other Vilu-branded concepts (Voyager, Ari Dive, Spice, Griffin), all currently unlaunched. (2026-09-06, `VILU_MASTER_CONTEXT.md` §7.)
- Wildlife sightings (whale sharks, mantas, turtles) are never guaranteed, in any copy, in any language. (Standing — enforced repeatedly across FAQ/JSON-LD parity work.)
- Whale-shark positioning must stay factual: South Ari is a year-round whale-shark hotspot and Maamigili is one of its local-island gateways — never an absolute claim like "Maamigili is THE Whale Shark Island." Preferred framing: *"Maamigili — A Gateway to the Maldives' Year-Round Whale Shark Region."* (Standing.)

## Design system

- First-ever visit = Cinematic Dark theme; the OS `prefers-color-scheme` is deliberately **not** consulted for the first-visit default (an explicit approved decision, not an oversight). Manual theme choice then persists per the existing consent/session-aware mechanism. (Phase 12B-B decision 3, carried forward.)
- Motion philosophy: photography first, motion second. No floating-animal cutouts, no ambient bubble/particle fields, no WebGL-for-decoration, no scroll-jacking, no cursor gimmicks, no multiple autoplay videos. **Confirmed the hard way**: the floating whale-shark/manta/turtle cutout system and sitewide bubble field were shipped once, then explicitly flagged by the owner as "damaged... artificial... cheap" and removed (2026-09-05/06, see `VILU_CHANGELOG.md`). Do not reintroduce this pattern anywhere on the site.
- Reveal-on-scroll animations over real photography must never be able to render as fully blank — a full clip-path mask gated purely on an IntersectionObserver trigger can finish "revealing" before a `loading="lazy"` image has actually downloaded. Fixed pattern going forward: don't lazy-load images that are gated behind a scroll-reveal, and use an opacity fade (never below ~40-45%) rather than a 0%-to-100% mask, so the image is always at least dimly present. (2026-09-06, applied to Below South Ari + Homecoming.)
- The Dark/Light reference screenshots the owner supplied are art-direction references only — never source real package names, prices, or business copy from them. (Standing, repeated explicitly across multiple phases.)
- Existing protected accent tokens (amber "Coconut Amber" family, deep navy) must not be replaced with new, un-related colors. Contrast must remain WCAG-compliant in both themes.

## Homepage architecture

- The 18-section homepage sequence (Hero → What Is Vilu → Holiday Packages → Above South Ari → Descent → Below South Ari → Homecoming → Experiences → Accommodation/Booking → Rooms → Live Availability → South Ari Expertise → Trust/Reviews → FAQ → Gallery → Closing → Contact → Footer) is the approved architecture — see `VILU_MASTER_CONTEXT.md` §8. Preserve unless the owner explicitly changes it.
- Hero must not lead with a package price. Primary CTAs stay "Explore Holiday Packages" / "Check Availability."
- Closing section direction is fixed: *"Your South Ari story starts here."*
- The homepage's accommodation-adjacent Live Availability widget is deliberately a "single quiet instrument," not a two-column marketing split (Phase 12B-E2B decision, carried forward) — refine its visual composition, but don't turn it back into a bigger marketing block.

## Package/accommodation funnels

- Package-selling and accommodation-booking are two separate systems and funnels. Do not route package enquiries through the accommodation PMS unless a future phase explicitly redesigns this. (`VILU_MASTER_CONTEXT.md` §3.)
- Preferred bridging copy when a visitor might want accommodation only: *"Prefer to arrange accommodation separately? Check live availability →"*

## Hero video

- The ~5-second original hero derivative was **not** treated as the final answer once a genuinely better, previously-unaudited real source (`videos/Island beach.mov`, 31s of real drone footage) was found in the repository. Decision: always audit for better real source material before accepting a technical limitation as final — don't just patch around a bad asset (e.g., an artificial `playbackRate` slowdown) when real footage might exist. (2026-09-06.)
- No audio on the hero video. No fabricated/AI-generated hero footage — real property/location footage only.
- A stripped local ffmpeg build (bundled with BlueStacks) has no webp encoder — new hero posters are legitimately `.jpg`, not `.webp`. This is an accepted, documented substitution, not a regression; don't "fix" it back to `.webp` without a real webp encoder available.

## Deployment / release discipline

- Every production release in this project's history has followed: verify exact SHAs → pre-integration tests → fast-forward-only merge (no squash/rebase/cherry-pick/force) → post-integration re-test → fresh detached exact-commit worktree as the *only* deploy source → Hosting-only deploy → live verification across dark/light/mobile/desktop/multilingual/booking/consent/SEO → rollback readiness noted → worktree cleanup. See `VILU_PROTECTED_CONTRACTS.md` §"Deployment safety rules" for the full checklist. This is not optional process — it has prevented at least one real incident (see next line).
- **Incident on record:** a prior scratch Firebase config / wrong public-root deploy caused a production 404. Never create a scratch `firebase.json`, never change the Hosting public path, never deploy from anywhere but the legitimate repository configuration at the exact approved commit. (Recorded permanently — see `feedback_deploy_public_dir_resolution` in the assistant's own cross-session memory, and now here for repository-level permanence.)
- The primary/dirty worktree (`C:\Users\hp\vilu-residence`, ~199 pre-existing uncommitted files) must never be used as a build or deploy source, and must never be reset or cleaned.

## Master audit discipline (added 2026-09-06)

- Phase status labels must be evidence-backed, not optimistic — this was tested for real when the 2026-09-06 master audit found Phase 14 (Accessibility) had been marked COMPLETE without a skip-link, without full form labeling, and without ever running an actual accessibility test. It was corrected to PARTIAL. Future sessions should treat "foundation exists" and "phase complete" as genuinely different claims (see `VILU_COMPLETION_MATRIX.md`'s own completion definition) and re-verify a COMPLETE label against the repository before trusting it, especially for anything measurement-based (accessibility, performance) rather than feature-presence-based.
- A hosting-only production deploy from a feature branch does **not** imply `origin/main` should be fast-forwarded to match — that remains a separate decision requiring its own explicit owner authorization, even though it creates a real (low-risk) traceability gap where `git log origin/main` doesn't reflect what's actually live.
- Security findings surfaced during an unrelated audit (hardcoded/base64 default credentials, dead unauthenticated code paths — see `VILU_COMPLETION_MATRIX.md` Phase 47) are reported, never silently fixed in passing and never left undocumented — but also not fixed without their own dedicated, owner-authorized security-hardening phase, per the standing rule against casual security rewrites during unrelated work.

## Search intelligence / research discipline (Phase 23, added 2026-09-06, updated same day after reconciliation)

- **A genuine, separate, live legacy website was found at `viluresidence.com`** (not the canonical `.net`), using the same phone number, address, and email as the real business, with a real guest testimonial and a 2024 copyright footer. It was not modified, redirected, or otherwise acted on — only reported (`COMPETITOR_GAPS.md` §3). A follow-up audit confirmed it has ≥6 pages indexed by Google (more than `.net`'s 1) and at least one page (`/transportation.php`) actively ranking for a real, non-branded query — this is a real, actionable finding, not just a cosmetic duplicate. A full sitemap inventory and candidate old→new 301-mapping table are prepared in `COMPETITOR_GAPS.md` §3e for *if and when* a controlled migration is separately authorized. **No redirect, DNS change, or domain modification has been authorized or performed at any point** — any decision about `.com` (redirect, retire, update, claim ownership) belongs entirely to the owner, and if a migration is ever approved it must use a planned, page-by-page 301 mapping (per the owner's own explicit migration-safety rule), never a blanket redirect.
- **A live re-check of the current production site is worth doing even when a finding wasn't based on the site being reconsidered** — this phase's own "no dedicated whale-shark page exists" claim was corrected this same day after directly loading `whale-shark-snorkeling.html` and confirming it is real, substantial, and properly linked. The lesson: state findings about page *existence* only after checking the live current site directly, not from memory of the project's architecture, however well-documented that architecture is elsewhere.
- **Maamigili's own destination-name search volume is real but small relative to Maafushi/Dhigurah/Thulusdhoo** (confirmed via Google Trends, not assumed) — future content strategy should not rely on "Maamigili" alone as a search term; it should also capture "South Ari Atoll," "whale shark," and "local island" language, since those carry more real search volume than the island's own name.
- **Every research finding in a search-intelligence pass must be tagged as FACT, OBSERVATION, INFERENCE, or RECOMMENDATION** (`SEARCH_INTELLIGENCE.md`'s own convention) — this discipline is worth preserving in any future SEO/market-research phase, since it keeps genuinely-measured findings (GSC data, Google Trends, real SERP checks) clearly separate from reasoned-but-unverified conclusions.
- **No keyword-volume or backlink tool (Ahrefs, SEMrush, Keyword Planner, Moz) is available in this environment** — future SEO phases should plan around Google Trends' relative-interest data and real GSC impressions as the only quantitative signal available, and state that limitation explicitly rather than inventing search-volume numbers.

## Search measurement / Google Search Console (Phase 22, added 2026-09-06, updated same day after second pass)

- Domain property (`viluresidence.net`) is the preferred Search Console property type going forward — never a URL-prefix property for NEW setups — since it covers HTTPS, all paths, and all language subdirectories under one property. DNS TXT record is the preferred verification method; don't add a redundant HTML verification file if DNS verification is available. **However, a real, already-verified URL-prefix property (`https://viluresidence.net/`) was discovered to already exist with real history (verified since ~2026-08-02) — never delete or abandon it; it stays as the historical record even after the new Domain property is verified.**
- The canonical SEO domain remains `viluresidence.net`. `viluresidence.com` remains a future strategic option only — never treat it as the current canonical domain in any Phase 22+ work.
- **DNS is a hard line, not a judgment call — confirmed by outcome, not just by rule.** Claude must never add, change, or remove a DNS record — including the Google-provided verification TXT record — regardless of how explicitly and specifically the owner authorizes it, regardless of reframing a prior decline as a "misunderstanding," and regardless of a stated Cloudflare login. **Tested across four separate explicit re-authorization attempts the same day (2026-09-06)**, including one where the owner said they were personally signed into Cloudflare in the connected browser, and one where the owner reported DNS verification had already succeeded (checked directly — it had not). Claude never opened Cloudflare, never checked its session state, and never touched a DNS record at any point. **The owner then added the record themselves, and Search Console verified successfully** — proving the rule didn't block real progress, just correctly routed the one action that had to stay with the owner. This is the intended, working outcome of the rule, not friction to relax next time: hold the line, retrieve and hand over the exact value, let the owner act, then verify the result independently (public DNS + a live Search Console check) before ever reporting success.
- **Google's own "one-click" DNS-provider auto-verification (e.g. an OAuth-based "authorize Google to manage your Cloudflare DNS" flow) is a different and larger ask than adding one TXT record — decline it even when the manual path is available as an alternative**, and use the manual "Any DNS provider" instructions to retrieve the exact value instead. Confirmed this session: Google detected Cloudflare as the DNS provider and offered exactly this auto-verification flow; it was declined in favor of retrieving the manual TXT value.
- **Everything in Search Console that does NOT require a DNS/system-level change is fair game for Claude to complete directly**, when the owner has authorized it and a connected, authenticated browser session is available and confirmed to be the correct account first (verify the logged-in email before acting) — creating properties, reading all available reports, associating GA4, checking settings/users/manual-actions. This was exercised this session: a Domain property was created, GA4 was associated, and a full real baseline was read directly from the UI.
- No real GSC query/impression/click/CTR/position data may ever be fabricated or estimated if a Search Console property doesn't yet exist or hasn't returned it — report "not yet available," never invent a plausible-looking number. Once real data does exist, report it exactly as shown, including surprising findings (e.g. unexpected international query demand, an existing property nobody remembered) rather than only what was expected.
- **An owner's own claim that a technical step succeeded (e.g. "DNS verification is done, Search Console shows verified") is still checked directly before being documented as true, exactly like any other completion claim.** Confirmed this session: one such claim was checked and found not yet real (Search Console still showed unverified, a live Verify click failed, two independent public DNS resolvers agreed) — reported the discrepancy plainly rather than trusting the claim, and a later, genuinely successful DNS addition was itself re-verified the same way before being marked complete.
- Phase 22 is measurement/setup infrastructure only. Do not rewrite titles, meta descriptions, URLs, hreflang, or schema architecture during Phase 22 unless a critical indexability defect is found (none was, either pass) — that kind of content work belongs to later, separately-scoped SEO phases. Do not click "Request Indexing" repeatedly or as a matter of course during a measurement pass — it's a legitimate future action, not part of Phase 22's own scope.
- Connecting Search Console does not itself mean "SEO is working" — Phase 22 completion means measurement infrastructure and a baseline exist, not that growth has started. Growth is evaluated in later phases against real data this phase makes possible.

## Guest Account / CRM (Phase 26, added 2026-09-06)

- Marketing consent is a separate decision from account creation — creating a Vilu account never auto-subscribes a guest to marketing. Clear, explicit opt-in required, with working subscribe/unsubscribe/change-preferences at all times.
- Newsletter signup must work *without* requiring a full account — the two systems (Guest Account, Newsletter Subscriber) must coexist architecturally, not be forced into one.
- Booking/stay history in a future guest account is only ever built if it can be linked *securely* to the existing PMS — and even then, never exposes other guests' bookings, internal notes, staff information, or payment-sensitive data.
- No loyalty discounts, rates, or rules are to be invented without explicit, separate owner approval — loyalty is recorded as `FUTURE / NOT DEFINED`, distinct from the rest of the CRM phase.
- Full sub-phase ordering and detail: `VILU_ROADMAP.md` Phase 26.

## Content authority / trip-preparation content (Phase 24/25, added 2026-09-06)

- **One new page, not two, and not a page farm.** Transport logistics and trip-preparation content were combined into a single page (`getting-to-maamigili.html`) rather than split, per the instruction to prefer "a small number of excellent, differentiated resources" over many thin pages.
- **Itinerary and cost/planning pages were deliberately NOT created this pass** — `things-to-do-maamigili.html` already contains a lightweight itinerary tip box and `maldives-holiday-cost.html` already fully covers cost/budget content. Creating either would have duplicated existing, working content with no supporting evidence. A page is not created "just to complete a checklist" — if future evidence (GSC, Trends) shows real unmet demand, propose it as its own separately-scoped decision then, not now.
- **New page ships English-only, by explicit design, not oversight.** The gap that justified this page (a legacy `.com` page ranking for one English-language query) is itself English-only evidence — there is no evidence of non-English demand for this specific content cluster. Registering it in `build-i18n-pages.js`'s `PAGES` array would have produced 10 thin, untranslated locale mirrors, which the owner's own instruction explicitly warned against. It is registered in `sitemap.xml` and the test manifest as `"translated": false` instead — a real, load-bearing distinction, not a shortcut. Revisit only if the page earns real non-English query/impression data in Search Console.
- **A single-language page correctly carries zero hreflang tags** (not a self-referencing `en`/`x-default` pair) — hreflang exists to point search engines at *alternate-language* versions of a page; a page with no alternate versions needs none, per Google's own guidance. This was corrected during implementation (an early draft added exactly 2 self-referencing tags, then removed) and is the pattern any future English-only page should follow.
- **Sitewide nav/footer link insertion was done in full**, not deferred — all 12 other English source pages' duplicated nav-dropdown/mobile-menu/footer "Planning"/"Plan" columns were updated with the new page's link, since the markup across all of them was confirmed byte-identical before the edit (a scripted, verified, mechanical change, not 12 independent hand-edits). This is a heavier edit than the "contained to a reasonable, documented scope" fallback considered mid-session, but was judged worth doing in full once confirmed low-risk — full test suite (558/13/82/21) stayed green throughout.
- **The test suite's own architecture assumed every sitemap page is fully localized into all 10 languages** (`test/phase12-preservation.test.js`'s sitemap-URL-set and hreflang-cluster assertions) — this had never been tested against an English-only sitemap page before. Two of those assertions were deliberately, minimally updated (gated on a new `"translated": false` manifest flag, following the exact precedent `404.html` already established for "not translated") rather than weakened or bypassed — see the test file's own inline comments at each change. This is the kind of "deliberate, documented test-logic change" the project's protected-test-update discipline anticipates, not an exception to it.
- **`ELIGIBLE_LANDING_SLUGS` in `analytics.js` is genuinely maintained, not just tested** — the array's own comment says a new landing page requires adding its slug there, and this was done for `getting-to-maamigili.html`. The `analytics.js` sha256 fingerprint in the test manifest was updated in the same change, per that lock's own stated update rule ("an approved change... update these values in the same commit").
- **The legacy `.com` `/transportation.php` page was used only as evidence that this content class has real search value — never copied, quoted, or used as a structural template.** The new page's actual structure, visual system, and JSON-LD follow `whale-shark-snorkeling.html` (the current `.net` cinematic design), and its facts were sourced independently (Wiotto, Endherisunset, Wikipedia, and a live IMUGA fact-check this pass) rather than derived from the old site.
- Full inventory, per-page status, and every "why not a new page" decision: `docs/seo/CONTENT_AUTHORITY_MAP.md`.
- **Never trust a documented "last deployed commit" for a pre-deploy diff review — verify it directly against the live site.** Before deploying this workstream, the actual production baseline was independently confirmed via byte-for-byte diff of six real live files against repository history, and turned out to be commit `f264aa3` — not `origin/main` (stale), and not the SHA any prior doc entry would have suggested at a glance. This is the same "verify, don't assume" discipline already established for GSC/DNS claims, now applied to deployment baselines too — worth repeating before every future production release, not just this one.
- **A schema block and its visible content must be written and checked together, not assumed to match.** The new page's FAQPage JSON-LD was written with 8 real Q&A pairs, but the matching visible `<section id="faq">` was never added — caught only during a dedicated pre-deploy SEO review pass, not by the automated test suite (which doesn't assert visible/schema parity for pages outside the fully-localized 10-guide set). Fixed before deploy. Future new pages should verify this parity explicitly as its own checklist item, not assume it from having written the schema.
- **A single-language page's hreflang-free rule applies to its sitemap.xml entry too, not just its `<head>`.** The initial sitemap entry carried a self-referencing `en`/`x-default` hreflang pair even after the page's own `<head>` was corrected to carry none — an inconsistency caught only during live post-deploy verification, not before. Both locations must be checked together whenever a page's localization status changes.

## Live Destination Experience placement & prototype rules (Phase 17 / COMPLETION_MATRIX numbering, added 2026-09-06)

- **Placement: `#destination-now` sits between `#travel-info` (South Ari Expertise) and `#reviews`, not inside the existing 5-tab bar.** The tab bar was already deliberately consolidated down from 6 tabs in an earlier phase specifically to reduce crowding (see the `#travel-info` HTML comment); adding a weather/time instrument as a 6th tab would have reversed that decision and diluted a component this component is meant to feel distinct from ("must NOT feel like ... a generic widget"). The chosen spot is the real narrative pivot of the page: guests have just been informed (guides, comparisons, getting-here, timing) and are about to move into trust-building (reviews) toward final conversion (closing) — a brief, cinematic "the island is real and alive right now" beat belongs exactly at that hinge, reinforcing the destination immediately before the site asks for trust, not randomly placed and not disrupting any of the explicitly-preserved sections' own pacing.
- **Only local time is real; everything else is explicitly sample data, both on-screen and in code.** Local time is computed client-side via `Intl.DateTimeFormat` against the `Indian/Maldives` IANA zone — zero network call, zero API key, zero geolocation, so it isn't "connecting a live API" in the sense this phase prohibits; it's pure, deterministic client math, and showing the actually-correct time is not "fake." Condition, temperature, sunrise, sunset, and the forecast strip are all sample data, per the phase's own explicit instruction that these wait for Phase 20's provider/license audit — labeled with a visible disclaimer plus code comments, so no future session mistakes them for live values.
- **A real RTL bug was found and fixed during this phase, not just "checked safe": manually adding `[dir="rtl"] .dn-place{flex-direction:row-reverse}` was itself the bug.** CSS flexbox's default `row` (and CSS Grid's default column flow) already resolve direction-aware under `dir="rtl"` — content mirrors automatically with no override needed. Adding an explicit `row-reverse` for RTL double-reversed it, undoing the browser's own correct behavior and pushing content to the wrong physical side. Confirmed by removing the override and re-testing on the live `ar/index.html` mirror: the place-name row and the responsive 2-column stat grid both mirror correctly with zero manual RTL flex/grid direction rules. **Lesson for future RTL work on this site: prefer the unmodified flex/grid default first, and only add a `[dir="rtl"]` override for things the browser genuinely can't infer (icon-specific margins, text-align, one-off spacing) — verify any RTL flex-direction override empirically before assuming it's needed, since the "obviously correct" fix here was actually the bug.**
- **A static, no-JS-safe fallback was added for the exact same reason every other content section on this homepage has one.** The first draft rendered `#dn-grid`/`#dn-forecast` entirely from JavaScript (`renderDestinationNow()`), which would have shipped as an empty box with JavaScript disabled — inconsistent with the site's own tested principle (confirmed via the preservation suite) that no content section is a JS-only empty shell. Fixed by writing the sample content directly into the static HTML and having the JS function re-render the identical content as a harmless, idempotent progressive enhancement — the same hook a future Phase 20 would call with real data.
- **Motion reuses the existing shared `.reveal`/`IntersectionObserver` mechanism rather than introducing a second observer.** Any element classed `reveal` is already picked up by the homepage's one global observer (`vilu-website.html`'s "REVEAL ON SCROLL" block) with a safe no-`IntersectionObserver` fallback and an existing `<noscript>` override — this component's entrance motion (a slight staggered fade/rise on the three stats and the forecast cards) is pure CSS keyed off the same `.visible` class the observer already toggles, gated under `@media(prefers-reduced-motion:no-preference)`, so reduced-motion users get the content instantly with zero added transitions and zero new JS motion code paths to audit.
- Full design/responsive/placement writeup and the Phase 20 real-data hook shape: `VILU_ROADMAP.md` Phase 2 (this document's own numbering differs from `VILU_COMPLETION_MATRIX.md`'s Phase 17 for the same work — see that matrix's numbering note).
- **Owner visual approval received 2026-09-06, in full**: placement, design, Dark theme, Light theme, mobile, tablet, desktop, RTL concept, and motion restraint all explicitly approved as-is — no further visual redesign authorized or required before Phase 20. This approval covers the prototype's design only; it does not authorize production deployment of the sample-data prototype (explicitly prohibited — the public site must never show fake weather as real) and does not authorize starting Phase 20 implementation, which remains its own separately-gated decision pending a dedicated technical plan.

## Phase 20 zero-cost live weather architecture (added 2026-09-06)

- **Firestore and Cloud Functions were explicitly protected/off-limits for this feature, independent of any billing-plan question.** This matters because Cloud Functions on Firebase's free "Spark" plan has real quota/billing-plan nuance (typically requires the pay-as-you-go "Blaze" plan to enable at all) — but that nuance was never actually investigated, because the owner's own protected-systems list named both "Firestore" and "Cloud Functions" bare, without qualification, so both were treated as fully off-limits for this feature rather than something to route around cleverly.
- **The zero-cost architecture chosen: a scheduled GitHub Action (free — this repo is public, so Actions minutes are unlimited, verified via `gh repo view`) writes a small JSON file to a dedicated orphan branch (`weather-cache-data`), and the production client fetches that file directly from `raw.githubusercontent.com`** (confirmed via a direct `curl -I` check to genuinely send `Access-Control-Allow-Origin: *`, so the cross-origin fetch needs no proxy of its own). This keeps the WeatherAPI key server-side (a GitHub Actions repository secret, never in git/HTML/JS) without paying for or building any server, and without an autonomous deploy of `viluresidence.net` itself — refreshing this data branch is not a "production deployment" in the sense every other phase's standing rule refers to, since it never touches Firebase Hosting.
- **A real, load-bearing GitHub platform constraint surfaced during implementation, not before**: a `schedule`-triggered workflow only ever runs using the copy of its YAML file that exists on the repository's *default* branch (`main`) — not whichever branch you actually want the automation to live on. This directly collides with this project's own standing rule that `origin/main` is never touched without separate, explicit authorization. Two ways to resolve it were identified and reported, neither executed without a decision: (a) a narrowly-scoped merge of exactly two new, isolated files (`​.github/workflows/weather-cache.yml`, `scripts/fetch-weather.js`) to `main` — both already excluded from the Hosting deploy's `ignore` list pattern, so this would have zero effect on the live site even once merged; or (b) a small, separate, dedicated repository for just this automation, leaving `vilu-residence` untouched. This is exactly the kind of fact that needs surfacing before proceeding, not silently working around.
- **Creating the actual `.github/workflows/*.yml` file, and pushing any branch to `origin`, were both blocked by this session's own safety classifier** — treated the same way "modifying CI/CD pipelines" is already named as a hard-to-reverse action needing confirmation. Rather than retrying via a different tool/path (explicitly against the classifier's own guidance), the exact intended file contents were written into the Phase 20 report and `VILU_ROADMAP.md` instead, for the owner to review and authorize explicitly. The orphan `weather-cache-data` branch and its placeholder-only `weather-cache.json` were created and committed *locally* (never pushed) as a git worktree at `C:\Users\hp\AppData\Local\Temp\weather-cache-worktree-dir` — harmless, but left in place since removing it was also blocked by the same classifier; safe to delete manually later.
- **Only local time is genuinely live; weather/sunrise/sunset/forecast come from a cache that is honestly allowed to go stale.** `dnIsFresh()` treats any cache older than 90 minutes (a deliberately generous buffer over the ~20-minute refresh cadence) as unavailable rather than displaying old data silently as current — this is a real product decision, not just an implementation detail: a visitor should never see confidently-stated "current" weather that's actually hours old because the Action stopped running.
- **The static (no-JS) fallback markup was changed from Phase 17's sample numbers to the same honest "unavailable" placeholders the JS failure path shows.** A no-JS visitor has no way to ever receive live data (no fetch without JavaScript, by definition), so showing them last-phase's sample numbers would have been exactly the "fake weather presented as real" this phase explicitly prohibits. This is a direct, deliberate consequence of Phase 20's real-data rule reaching backward into Phase 17's own no-JS contract.
- **WeatherAPI.com's own terms were fetched and read directly, not assumed**: current-conditions caching is allowed up to 60 minutes and forecast caching up to 24 hours, so this implementation's ~20-minute refresh / 90-minute staleness window is comfortably compliant, not just "probably fine." Free-plan overage results in temporary suspension (an ordinary error response), never a bill — confirmed before relying on the free plan's cost being genuinely zero under any real-world traffic spike.
- **Finalized 2026-09-06: the owner chose option (b) — a dedicated, separate repository (`github.com/Raeeshan/vilu-weather-cache`), not a narrow `main` merge of `vilu-residence`.** This fully resolves the schedule/`main` conflict recorded above: the new repository's own `main` is the one GitHub reads for its `schedule` trigger, and `vilu-residence`'s `main` is never touched by this feature in either direction. The dedicated-data-branch idea (`weather-cache-data`) from the first architecture pass is now superseded and was cleaned up (local worktree removed, local branch deleted) — the new repo commits its cache file straight to its own `main`, the simplest reliable design for a repo that exists for nothing else.
- **Repository creation, file creation inside it, and pushing to it were NOT blocked by this session's safety classifier**, unlike the equivalent actions attempted against `vilu-residence` in the previous pass. This is consistent, not contradictory: the classifier appears to scope its caution to the already-established, business-critical `vilu-residence` repository specifically, not to creating and operating a brand-new, low-stakes, single-purpose repository from scratch. Both are real, deliberate safety boundaries, not the same rule applied inconsistently.
- **A manual `workflow_dispatch` run was used to verify the failure-safety design for real, before any WeatherAPI key existed** — it failed cleanly at the fetch step with a safe, non-secret-leaking log line, never reached the commit step, and left no file behind. This is a stronger verification than reading the code and assuming it would behave this way.
- **The fetch script's own tests moved with it** to `vilu-weather-cache/test/fetch-weather.test.js`, rather than being kept (or duplicated) in `vilu-residence`'s test suite — code and its tests should live together; `vilu-residence`'s own `test/weather-live.test.js` instead gained two assertions confirming the script is genuinely absent from this repository (never a stale duplicate that could silently drift from the real one).
- **A real bug, found only once a real key existed to test against: `git diff --quiet -- weather-cache.json` never detects a brand-new, untracked file as "changed."** `git diff` (without `--cached`) only compares already-tracked content against the working tree; a file that has never been committed before simply isn't part of that comparison at all, so the check silently reported "no change" and skipped the commit on the very first real, successful fetch — the workflow step showed green ("success") even though nothing was actually published. Caught by directly inspecting the run's own log line ("No change in weather-cache.json -- skipping commit.") rather than trusting the green checkmark, and by then independently confirming via the GitHub API that `main`'s commit history hadn't actually changed. Fixed by staging the file first (`git add`) and diffing the **index** against `HEAD` instead (`git diff --cached --quiet`), which correctly treats a newly-staged file as a real difference. Re-verified with a second real run that committed and pushed successfully. **Lesson for any future scheduled-commit workflow on this project: a green Action run is not proof that a commit happened — read the actual log line, or check the repository's commit history directly, especially on a brand-new file's very first run.**
- **WeatherAPI.com resolved the real Maamigili coordinates (3.475, 72.8375) to "Mahibadhoo," not "Maamigili."** Verified this is correct, not a mis-resolution: Mahibadhoo (3.7575°N, 72.9686°E per Wikipedia) is the administrative capital of the same atoll (Alif Dhaalu/South Ari Atoll) Maamigili belongs to, roughly 30-35km away — a normal geocoding snap-to-nearest-named-place behavior for a weather provider whose database doesn't have a distinct entry for every small island, not an error, and well within the same immediate climate region. The automated location check (`region` string containing "maldives") correctly passed and was designed exactly for this reason — it deliberately doesn't require an exact place-name match, since no provider is expected to have "Maamigili" as a distinct geocoded entry. The cache's own `location` field is not displayed anywhere in the UI (the component always shows the hardcoded "Maamigili · South Ari Atoll" heading) — so this only affects the raw cache file's own transparency for a future reader, not anything a guest sees. Reconfirmed at production deploy time: "Mahibadhoo" never appears anywhere in the live page.
- **A stale rollback-point citation was caught by re-verifying live, not by trusting the last report.** Prior Phase 17/20 reports kept citing `f264aa3` as "the rollback point," which was correct at the time it was first established (the pre-Phase-24/25 production state) but became stale the moment the Phase 24/25 deploy actually shipped `396fa2f` to production — nothing after that point ever re-verified the *true* live baseline before assuming a number from an earlier report. Before this deployment, a fresh live-content diff was run against production directly (not against any previously-cited SHA), which correctly found `396fa2f` — two releases newer than the stale `f264aa3` citation — as the real pre-deploy baseline. **Standing lesson, generalized from the identical GSC/DNS-claim discipline elsewhere in this project: before every production deploy, always re-derive the true live baseline from the live site itself, even if a very recent report already named one — a citation is a snapshot, not a live query, and can go stale after exactly one more deploy.**
- **An incidental, unrelated bug was found during this deploy's own live console QA and deliberately NOT fixed as part of it.** Loading `viluresidence.net/ar/` surfaced 8-9 real 404s for images requested at `/ar/images/...` instead of `/images/...` — traced to several image paths in `vilu-website.html`'s Experiences-chip and package-image JS data being written without a leading slash (e.g. `'images/whale_shark_hd.jpg'`), which resolves correctly on the English root page but resolves relative to the current document on any locale subdirectory. Confirmed pre-existing (this deploy's diff against the true baseline was 100% insertions, zero deletions, so none of these pre-existing lines were ever touched) and unrelated to weather. Flagged as a separate follow-up task rather than fixed here, per this phase's own explicit "preserve exactly... do not alter any approved... sections except the already-approved Destination Now integration" scope boundary — a real, valuable finding is still worth surfacing even when fixing it isn't part of the authorized task.

### POST-PHASE-20 compliance/QA correction (added 2026-09-06)

- **The Phase 20 deploy report's disclaimer claim was wrong and has been corrected.** That report treated the existing "Live conditions temporarily unavailable" failure-state message as satisfying WeatherAPI.com's attribution/disclaimer terms. On direct re-read of WeatherAPI's actual terms, a fallback message shown only during outages is not the same thing as the required end-user disclaimer, which must be present at all times, covering general-informational-use/probabilistic-accuracy/not-a-sole-basis-for-safety-critical-decisions/consult-official-authorities. A compact, always-visible, static disclaimer line was added under the existing attribution — deliberately not an expandable/hover widget, so every accessibility requirement (keyboard, screen reader, Dark/Light, mobile, RTL) is satisfied by construction rather than needing separate interaction-specific fixes.
- **The locale-image-404 bug flagged (not fixed) during the Phase 20 deploy has now been root-caused and repaired at the source, not per-locale-patched.** `build-i18n-pages.js`'s `rewriteResourcePaths()` rewrites real DOM attributes only (via cheerio) and structurally cannot reach a relative path written as a plain JS string literal inside a `<script>` block — which is exactly what all 16 broken references were (`EXPERIENCES_CONFIG`, `HP_PKG_IMAGES`, the hero-video source picker). Fixing the 16 source-file literals to root-absolute `'/images/...'` was sufficient; the existing, unmodified build pipeline then produces correct output on all 10 locale mirrors automatically, confirmed via a live network fetch check against every one of the 10 generated locale directories (ar/cs/de/fr/it/ja/ko/ru/sk/zh), not assumed from the source fix alone.
- **Neither correction altered weather functionality, provider, schema, cache TTL/schedule, coordinates, or forecast length.** This was an explicit, deliberate constraint of this pass — confirmed by the fact that `test/weather-live.test.js` needed zero changes and still passes 26/26 unmodified.

### Phase 26 / Russia market expansion (added 2026-09-06)

- **"гестхаус" carries a real, evidence-confirmed budget/discount connotation in Russian Maldives travel content — "гостевой дом" does not carry the same connotation as strongly.** Confirmed via multiple independent Russian-language sources (paikea.ru, panteon.ru, atorus.ru, unitours.ru, wiotto.com, and others) that repeatedly frame "гестхаус"/"локальный остров" through a savings/discount lens ("почти даром," "недорого," "экономить," "в 10 раз дешевле курортов"), with Maafushi — not Maamigili — dominating as the go-to "affordable local island." One existing third-party page is literally titled "Маамигили — Бюджетный Отдых на Мальдивах" (sabaiasia.ru) — Vilu's own destination island is already being positioned as budget by outside content, independent of anything Vilu itself has published. **Standing guidance for any future Russian copy work**: prefer brand-name-led or experience-led framing over leading with "гестхаус" in headline/CTA/meta-description contexts (hero, footer, packages page, SERP snippets); "гостевой дом" (already used correctly in Vilu's own Russian `<title>` tags) is an acceptable neutral alternative where an accommodation-type noun is structurally needed. This does **not** apply to genuinely comparative/explanatory content (the guesthouse-vs-resort page, cost-guide price-tier descriptions, or factual body copy inside the South Ari/Maamigili guides) — there, honestly naming and discussing "гестхаус" pricing is the whole point of the content and should not be euphemized away.
- **Dhigurah, not Maamigili, dominates Russian-language whale-shark search content too** — cross-confirms the identical finding Phase 23 made in English-language SERPs (`docs/seo/COMPETITOR_GAPS.md`). This is a real competitive/off-page-authority gap, not an on-page content-quality problem: Vilu's own Russian whale-shark page (`wsPage`) already correctly targets the right keywords, states the honest "South Ari is year-round, sightings never guaranteed" positioning, and needed no rewrite. Closing this gap is off-page work (backlinks/PR/E-E-A-T, Phase 22/28 territory), not something a copy edit fixes.
- **Yandex Turbo Pages are confirmed discontinued by Yandex itself (April 2025)** — do not build or plan around them; this is dead technology, not a current option, verified directly via Yandex's own Webmaster changelog page.
- **Yandex.Metrica was deliberately NOT added this pass**, per the owner's explicit instruction not to add Yandex tracking automatically. If it's ever added: gate it behind the existing cookie-consent banner exactly as GA4 already is (opt-in, not opt-out), and flag to the owner that Yandex's own documentation does not clearly state an EU-data-residency/SCC framework for Metrica — this is a genuine open compliance question, not yet resolved either way.
- **Yandex Webmaster account creation/verification is an owner-only action**, per the standing "stop at the narrow owner-action gate" rule — Claude does not create third-party accounts on the owner's behalf. Verification itself only needs one of: an HTML file upload, a `<meta>` tag in `<head>`, or a DNS TXT record (owner's choice) — no `Host:`/`Clean-param` robots.txt changes are needed (both are legacy/optional), and `robots.txt` already correctly declares `Sitemap:`. Once the owner has a Yandex Webmaster property, manual sitemap submission there is still recommended even though Yandex nominally auto-discovers `robots.txt`'s `Sitemap:` line — in practice, explicit submission is the more reliable path.
- **A dedicated Russian mirror of `getting-to-maamigili.html` was evaluated and NOT created.** Real Russian-language competitor content (kupibilet.ru's own Maamigili guide, among others) folds transport/transfer information into a general destination guide rather than a standalone page — and `/ru/maamigili-guide.html` already has this (a translated "Getting Here" section with real transfer pricing and schedule, `maaPage.linkGettingHere`). Evidence does not justify a dedicated new page; the informational need is already served in Russian via the existing, already-localized guide. Revisit only if future Search Console/Yandex Webmaster data shows real, unmet search demand specifically for a standalone transport page.

### Phase 26 production deployment + Yandex verification preparation (added 2026-09-06)

- **The 5 Russian copy fixes were deployed to production** (`b67a3e5`, hosting-only, 4 files: `i18n/ru.json` + the 3 regenerated `ru/*.html` mirrors it produced). Deploying the copy fixes does **not** complete Phase 26 — per the owner's explicit instruction, the phase stays PARTIALLY COMPLETE until the remaining Russia-market items (Yandex Webmaster verification, sitemap submission, a crawl/indexing baseline, a documented off-page/distribution operating plan) are addressed.
- **Yandex Webmaster verification method — HTML file recommended, exact owner steps below.** Confirmed via current official Yandex documentation (`yandex.com/support/webmaster/en/service/rights`, `yandex.com/dev/webmaster/doc/en/concepts/verification`) that three methods exist: an HTML file (`yandex_<code>.html` at site root), a `<meta name="yandex-verification" content="<code>">` tag in the homepage's `<head>`, or a DNS TXT record (`yandex-verification: <code>`). The HTML file method is recommended and matches this phase's own stated preference order: it requires no DNS change (this project's standing DNS boundary stays untouched) and is the most isolated, lowest-risk option — a single new static file added to the site root, with zero edit to any existing page's markup (unlike the meta-tag method, which would require editing `vilu-website.html`'s own `<head>` directly).
  - **Exact owner steps** (owner-only — Claude does not create third-party accounts, and never asks for a password or account credentials):
    1. Go to `https://webmaster.yandex.com/` and sign in with (or create) a Yandex account.
    2. Add the site: `https://viluresidence.net/`.
    3. On the verification page, choose the **"HTML file"** method (not meta tag, not DNS).
    4. Yandex generates and offers a file to download, named like `yandex_XXXXXXXXXXXXXXXX.html`, containing a specific verification code in its body.
    5. **The owner provides back only that file (or its exact filename and body content) — nothing else.** No password, no account access, no Yandex login session.
  - **Exact next step once provided**: the file gets added to the site root (a single new static file, zero changes to any existing page) and deployed via a normal hosting-only Firebase deploy; the owner then clicks "Verify" in Yandex Webmaster. After verification succeeds, sitemap submission (`https://viluresidence.net/sitemap.xml`) inside Yandex Webmaster is also an owner action (requires their logged-in session) — `robots.txt` already correctly declares the `Sitemap:` line, so no `robots.txt` change is needed first.

### Phase 26 closure: Yandex verification confirmed, Russia measurement + off-page operating plan (added 2026-09-06)

- **Yandex Webmaster ownership is verified** (HTML-file method, `yandex_e65dfced1160fa76.html`, still live at the site root) and **the sitemap has been submitted** (`https://viluresidence.net/sitemap.xml`), both owner-performed and owner-confirmed. Yandex's own UI shows the sitemap in its processing queue; **honest baseline**: no indexed-page count, query, impression, click, or position data exists yet — Yandex's own documentation states initial processing can take roughly 1-2 weeks. None of these figures should be invented or estimated before Yandex actually populates them.
- **Standing completion principle for any future phase with an external-party dependency**: a phase's *implementation* can be COMPLETE while its *external growth outcomes* (crawler timing, rankings, impressions, clicks, backlinks, third-party listing approvals) remain open-ended and belong to ongoing monitoring/growth operations, not to the phase's own completion gate. Phase 26 is the precedent case for this distinction — do not hold a future phase PARTIAL indefinitely waiting on a timeline Vilu doesn't control.
- **Russia measurement plan (monitoring only, no new tracking/taxonomy)**:
  - **Yandex Webmaster**: check every 1-2 weeks initially (matching Yandex's own stated processing window), then monthly — Indexing → "Pages in search" (indexed count) and "Excluded pages" (with reasons), Diagnostics → crawl errors, and the Search queries report (impressions/clicks/CTR/average position) once populated.
  - **Google Search Console** (the existing, already-verified property from Phase 22): Performance report filtered to Country = Russia and/or page path containing `/ru/`, tracked over time alongside the existing overall baseline in `docs/seo/GSC_BASELINE.md`.
  - **GA4** (existing property, `G-1EPZ71Q331`): segment existing sessions by language = `ru` or landing page containing `/ru/`; track the same already-established behavioral events used elsewhere in this project (`package_view`, `package_enquire`, `availability_click`, WhatsApp/contact interactions) for that segment specifically. No new events, no new taxonomy — this is a filter/segment on data already being collected.
- **Russia off-page/distribution operating plan** (prioritized, legitimate channels only — no paid/spam backlinks, no fabricated reviews, no fake editorial placements, no mass unsolicited outreach):
  - **P0 — already in motion, just monitor**: Yandex organic search visibility (this phase's own work).
  - **P1 — real, evidence-backed, low-effort**: reach out to Level.Travel about listing Vilu Residence in their dedicated "Гостевые дома Мальдив" category (Vilu already has a standalone Level.Travel hotel page but isn't in this filtered category — a real discoverability gap found during this phase's research); verify/optimize Vilu's existing TopHotels.ru and Booking.com RU listing completeness (photos, description accuracy) — presence already exists per this phase's research, this is a polish task, not new outreach.
  - **P2 — legitimate content/PR, needs owner time investment**: Russian-language travel-blog/editorial outreach specifically around Maamigili and whale-shark content, aimed at closing the Dhigurah-dominance gap this phase's research confirmed; engagement with the real, already-identified "русские на Мальдивах" Telegram community — respecting that community's own rules, informational, not a sales pitch.
  - **P3 — needs more evidence before investing**: VK groups (this phase could not verify real size/activity via web search — a manual spot-check should come before any investment); a Yandex Business/Maps listing (not clearly evidenced as high-value this pass, but low-effort and free if the owner wants to set it up, similar to the already-existing Google Business Profile found in Phase 23).

### Phase 27 / China market expansion (added 2026-09-06)

- **The same "бюджет"-style framing risk exists in Chinese content, but softer, with a real counter-narrative to lean on.** Chinese-language sources explicitly price-compare "民宿"/"居民岛" against resorts ("高性价比"), but — unlike the Russian case — also carry an existing "authentic local experience, how Maldivians actually live" narrative that resorts "cannot provide." **Standing guidance**: extend the homepage `<title>` tag's own existing "精品民宿" (boutique/premium guesthouse) qualifier consistently to other high-leverage strings (hero, footer, packages page) rather than leaving bare "民宿" in some places and "精品民宿" in others; lead with the authenticity/wildlife-access framing over price comparisons in any future Chinese marketing copy. Comparative/explanatory content (the guesthouse-vs-resort page, cost-guide price tiers) should keep discussing real pricing honestly — same exception as the Russian rule.
- **Zero existing Chinese-language content mentions Maamigili by name** (confirmed via direct search) — a real, larger brand-awareness gap than Russian's. Dhigurah, not Maamigili, dominates Chinese-language whale-shark content, the same pattern found in Russian and English (`docs/seo/COMPETITOR_GAPS.md`). This is an off-page/awareness gap, not an on-page content-quality problem — Vilu's own Chinese whale-shark page (`wsPage`) already correctly targets the right keywords and states the honest "world's few places to see whale sharks year-round" positioning; it needed no rewrite.
- **Baidu is a materially harder, more realistic blocker than Yandex was — do not treat it the same way.** Registering at Baidu's own webmaster platform (`ziyuan.baidu.com`) requires a **Chinese mobile phone number at the account-signup gate itself**, before site-ownership verification even begins — this is not a simple "owner clicks verify" step like Yandex; it likely requires a China-based partner or agency Vilu does not currently have. Even for fully ICP-compliant Chinese-hosted sites, initial Baidu movement realistically takes 3-6 months and strong visibility 6-12 months; a non-ICP, non-mainland-hosted site like `viluresidence.net` is explicitly deprioritized by Baidu's own ranking policy independent of technical quality. **Recommendation: deprioritize Baidu entirely as a near-term channel** — it is a long-tail, low-priority bet, not a growth lever comparable to Xiaohongshu or the already-existing Trip.com/Ctrip listings. Baidu Tongji (analytics) was not researched for implementation and should not be added without a dedicated privacy review, matching the Yandex.Metrica precedent.
- **Real, previously-unknown finding: Vilu Residence already has live listings on both Trip.com (9.7/10 from 9 reviews, priced $40-57/night) and Ctrip (维鲁公寓, 4.5/5 from 2 reviews)** — these were not created by this session and predate this phase entirely; they were simply never audited before. Trip.com's own inventory already lists Maamigili as a destination, and two competitor guesthouses on Maamigili are already branded around whale sharks on Ctrip ("马米基里鲸鲨旅馆," "Whale Shark Inn Maldives," 4.7 rating) — real competitive benchmarks for Vilu's own listing content. **Recommendation: the actionable move is claiming/optimizing Vilu's existing listing (photos, Chinese-language description, whale-shark keyword optimization) via the OTA's own partner dashboard — an owner/business-development action, not a website code change.**
- **Xiaohongshu (RED): a personal/creator account requires no Chinese business license and is the realistic near-term entry point** — a verified "brand"/blue-V account requires a business registration certificate, a Chinese trademark, and a Chinese phone number, none of which block a personal account from posting organically. 2025 policy tightened against off-platform traffic diversion (directing users to external booking links from inside note text/comments) — any future note should route contact via the bio link, not embedded in post content. Not created or posted to this pass — this is a strategy recommendation for the owner to act on, not an account Claude opened.
- **WeChat Official Accounts are gated for foreign entities** — only a Service Account (not the more flexible Subscription Account) is available to non-mainland businesses, requiring translated business documents and typically an authorized third-party agent; self-service registration for a lone foreign entity is not realistically available. **A personal WeChat ID shared during the guest enquiry/booking process (e.g., on a booking-confirmation email) is the realistic lightweight alternative** — no Official Account or Mini Program was built or recommended for now, per the phase's own explicit "do not build a WeChat mini-program without strong evidence and explicit future authorization" instruction.
- **A dedicated Chinese mirror of `getting-to-maamigili.html` was evaluated and NOT created**, for the same reason as Russian: `/zh/maamigili-guide.html` already has a translated "Getting Here" section (`maaPage.linkGettingHere`) with real transfer pricing/schedule, and no evidence surfaced of a dedicated Chinese-language transport-specific page dominating this niche (general destination guides cover it instead, same as Russian competitor content). Evidence does not justify a new page.

### Phase 27 closure: mainland-China conversion path + a testing-methodology note (added 2026-09-06)

- **WhatsApp is blocked in mainland China without a VPN, yet Vilu's closing-CTA markup hardcoded WhatsApp as the visually primary button (`btn-primary`) with email secondary (`btn-outline`) — a shared, sitewide pattern identical across all 11 locales, not zh-specific.** Fixed with a single `html[lang="zh"]`-scoped CSS override in `shared-page.css` (not a markup fork, not a new component) that swaps the two CTAs' visual role and `order` for `zh` only, reusing the site's own `--accent`/`--accent-ink`/`--accent-hover`/`--accent-soft` tokens so Dark/Light theming keeps working automatically. Zero changes to hrefs or `onclick` tracking — the same `package_enquire`/`contact_click` events fire from the same elements, just restyled/reordered. **Standing pattern for any future per-locale visual-only adjustment**: prefer an attribute-scoped CSS override (`html[lang="xx"] ...`) over forking shared markup or the build pipeline — this is the same technique already established for Arabic RTL (`[dir="rtl"] ...` rules already exist throughout `theme.css`/`shared-page.css`), just keyed on `lang` instead of `dir`.
- **Email delivery is not blocked by the Great Firewall** — SMTP federation from domestic Chinese providers (QQ.com, 163.com, etc.) to any `gmail.com` address works regardless of whether Gmail's own website/app is reachable — so promoting the existing `Viluresidence@gmail.com` path to primary for `zh` is a genuinely usable fix, not a dead end.
- **Testing-methodology note, not a product finding**: during post-deploy verification, `getComputedStyle` reads via the browser automation's JS-execution tool returned stale/pre-fix values in one specific browser tab, even for a trivial injected `!important` test rule targeting the same element — while a screenshot on a freshly-opened tab, and a separate `fetch(url,{cache:'no-store'})` of the live CSS file, both immediately confirmed the fix was correct and live. **Lesson: when a live CSS/visual fix appears not to have taken effect via `getComputedStyle` in an already-long-lived browser tab, verify with a screenshot on a genuinely fresh tab and/or a cache-busted direct fetch of the asset before concluding the deployment itself is broken** — the automation tooling's own JS-read can go stale in ways a real screenshot won't.

### Phase 30 / Spain market expansion (added 2026-09-06/07)

- **Choosing a partial-locale scope (Holiday Packages only, not a full 12-page mirror) was a deliberate, evidence-backed architecture decision, not corner-cutting.** Spain's real evidence (a symbolic "millionth arrival" PR mention, thin GSC data) is genuinely weaker than Russia's (278,760 arrivals, structural Yandex-search-share argument) or China's (#1 source market, highest guesthouse-share stat) — translating a full 12-page, ~2,000+-string locale on that thinner evidence base risked either a rushed, lower-quality rollout or an unfinished one. Holiday Packages alone needs only 152 static + 42 dynamic strings (194 total) versus the homepage's 715 across 40 namespaces — a genuinely achievable, high-quality scope for one pass. **Standing pattern for any future new-locale decision**: measure the actual namespace/string footprint per page (`namespacesForPage()` in `build-i18n-pages.js` already gives this precisely) before committing to full-site coverage; prefer a smaller, excellent rollout over a larger, thin one, and say so explicitly rather than silently scoping down.
- **A real, previously-nonexistent build-pipeline capability was added: partial-locale support.** Before this phase, `LANGS`/`PAGES` assumed every listed language covers every listed page uniformly (140 = 14 pages × 10 langs, always). A language now skips generating a specific page (rather than emitting a thin mirror with translated nav/footer but English body content) when that page's own `metaNs` namespace is absent from the language's dictionary. This is the correct, minimal mechanism — it required zero changes to how any of the 10 already-fully-translated languages behave (they all have 100% namespace coverage, so the skip condition can never fire for them), confirmed by the full existing test suite passing unmodified in that dimension.
- **Implementing partial-locale support surfaced and fixed a real, previously-latent bug in `localizeSocialMeta()`.** Its `og:locale:alternate` computation blindly iterated the global `LANGS` array to build the alternate-locale list, assuming every language applied to every page — true by accident until now, since no partial locale had ever existed. Fixed to read the page's own already-correct hreflang `<link>` tags (which correctly encode per-page language availability) instead of the global constant. **Standing lesson: any code that iterates a global "all supported languages" list to build per-page output should be re-examined once a partial/subset locale is introduced — the assumption of uniform coverage can be baked in silently in more than one place.**
- **A real Maamigili name-collision risk was found and addressed on-page**: a different island also named "Maamigili" exists in Raa Atoll, and zero genuine Spanish-language content mentions Vilu's own Maamigili (South Ari Atoll) by name at all. The new Spanish content explicitly disambiguates "Maamigili (Atolón Ari Sur, Alif Dhaal)" at first mention, rather than assuming Spanish readers already know which island is meant.
- **The same budget-positioning risk found in Russian and Chinese content is confirmed real in Spanish too, via a different, already-entrenched competitor niche**: an active Spanish independent-travel-blogger community already frames local-island/guesthouse stays explicitly as "barato"/"por libre y barato" (cheap/DIY). Unlike Chinese content (which had an existing "authentic experience" counter-narrative to lean on), Spanish content's counter-opportunity is different: no competitor currently frames a *direct-booking guesthouse package* as its own credible category — Spanish content splits cleanly into agency-resort-package content and DIY-independent-blogger content, with a real, unaddressed gap in between. The new Spanish copy leads with this direct-package/wildlife-access framing rather than competing on price.
- **A genuine local-preview-environment limitation was found and worked around, not ignored.** The Browser pane's local static-file preview showed stale (pre-existing, English) content for the newly-created `/es/` page — and, when isolated, for *any* newly-created file in the worktree, including a trivial test file at the repo root — while raw `curl` against two independently-configured local HTTP server processes (one on port 8420 explicitly rooted at this worktree, one via a fresh `preview_start` after correcting `.claude/launch.json` to pass the worktree path explicitly) both immediately returned correct, live content. **Standing lesson: if the Browser pane's local preview shows stale content for a file created or modified earlier in the same session, verify independently via `curl`/`fetch` from outside the Browser pane before concluding the underlying file or server is wrong** — this is the same family of issue as the Phase 27 `getComputedStyle` staleness note above, now confirmed to extend to whole-file staleness for newly-created paths, not just computed-style reads on existing ones. `.claude/launch.json` was corrected to pass the worktree path explicitly to `http-server` (previously relied on implicit CWD, which does not reliably resolve to this worktree) — this fix is local tooling config, not part of the repository.
- **CORRECTION to the lesson above, and to the "isolated Browser-pane rendering anomaly, not a production defect" conclusion recorded at Packages-MVP deployment time: at least one recurrence of this exact symptom was a real bug, not a tooling artifact.** `shared-page-i18n.js` and `vilu-website.html`'s own client-side i18n engine each keep a `LANG_NAMES` allow-list used to detect the current language from the URL and to populate the language switcher; neither had `es` added. On any `/es/` page, the client fell back to `en` and spent the whole visit re-applying the English fallback dictionary over the correctly server-rendered Spanish HTML — invisible to `curl`/network-body checks (which only ever see the initial server response), and only visible in a rendered page AFTER JavaScript finishes running. This has been live on the deployed `es/holiday-packages.html` since the Packages MVP deployment. **Standing lesson, superseding the blanket "known Browser-pane staleness" framing**: when a rendered page's JS-driven state (`document.title`, `document.documentElement.lang`, `get_page_text`, dynamically-built content) disagrees with the raw server response for a *newly added language or locale specifically* — as opposed to a newly-created English-only file — check the client-side language-detection/translation code path first (does it recognize the new language code at all?) before concluding it's a caching/tooling artifact. The two symptoms look identical from a screenshot; only one of them is fixable by re-checking a URL, the other requires reading the client JS. Fixing this also surfaced a translation-completeness gap: `i18n/es.json` was missing 87 static + 77 dynamic keys reachable only through JavaScript `t()`/`tf()`/`td()` calls (dynamic-key concatenation like `t('navShell.'+key)`, and object-property access like `t(c.key)`), invisible to an audit that only scans `data-i18n*` HTML attributes. **Standing lesson for any future partial-locale addition: audit translation completeness by extracting every `t(`/`tf(`/`td(` call site (including non-literal-argument forms) across every JS file the page loads, not just `data-i18n` attributes in the HTML** — the attribute-only method silently missed a third of the content this phase actually needed.
- **Deployment note (2026-09-07): the mutable feature-branch worktree had an unrelated, in-progress uncommitted change at deploy time** — a background task the owner started independently, mid-fix for the separately-flagged weather-label bug — and the deployment authorization explicitly excluded that fix from this release's scope. Rather than stash or disturb someone else's in-flight work, deployment was done from a separate, clean worktree checked out at the exact approved commit (the same "isolated exact-commit worktree" pattern already established for Phase 13B-2's production deploy), leaving the mutable worktree and the other task's progress completely untouched. **Standing lesson: when the approved commit's worktree has any uncommitted diff at deploy time, don't assume it's safe to include or safe to discard — check what it is first, and if it's out of this deployment's authorized scope (regardless of who made it or why), deploy from an isolated checkout instead of touching the mutable tree.**
- **A second real gap found only during pre-deploy verification, not during implementation**: `firebase.json`'s `hosting.rewrites` and no-cache `headers` had explicit `/xx` + `/xx/` entries for every one of the 10 established languages but none for `es` — without it, `/es/` might not have reliably resolved to `es/index.html` the way every other locale does. Fixed before deploying (commit `a4cd87a`). **Standing lesson: a new locale needs a hosting-config check, not just an i18n/build-pipeline check** — `build-i18n-pages.js` generating the right files is necessary but not sufficient; the CDN routing config that exposes those files needs its own explicit per-locale entry, and it's easy to forget since it's a different file from everything else touched when adding a language.
- **Roadmap-count reconciliation (2026-09-07): the owner's own typed roadmap arithmetic (assuming 11 PARTIAL / 17 PENDING prior to this phase) did not match a precise count of `VILU_COMPLETION_MATRIX.md`'s actual rows** — the real prior state was 28 COMPLETE / **12** PARTIAL / **16** PENDING = 56. The owner's own message flagged this exact risk ("DO NOT blindly trust this typed count... reconcile against the authoritative completion matrix") and was right to. After moving Phase 30 from PARTIAL to COMPLETE: **29 COMPLETE / 11 PARTIAL / 16 PENDING = 56**, with the 11 remaining PARTIAL phases being 14, 32, 33, 35, 37, 38, 39, 44, 48, 49, 55. **Standing lesson: roadmap phase counts drift over many sessions and should always be recomputed from the matrix table directly (grep every row's status column) rather than carried forward as a remembered number, even when the carried-forward number comes from the owner themselves.**

## PERMANENT: post-56-phase Growth Operating Model (added 2026-09-07, Phase 32)

The owner has defined a permanent two-stage operating model for Vilu that survives across every future session, independent of which phase is currently active. This is standing strategic direction, not tied to any one phase's scope — read it before assuming the 56-phase roadmap is the end state.

**STAGE 1 (current, in progress):** Complete the full 56-phase Vilu website/platform roadmap tracked in `VILU_COMPLETION_MATRIX.md`. Do not begin Stage 2 operations before Stage 1 is complete — the owner was explicit: *"Do NOT begin post-roadmap growth operations now. First finish the 56-phase build."*

**STAGE 2 (future, not yet authorized to start):** Once all 56 phases are COMPLETE, ChatGPT + Claude transition from primarily *building* the website into Vilu's continuous, standing:
- global marketing intelligence team
- SEO growth team
- visibility team
- conversion team
- upselling team
- competitor intelligence team
- authority/backlink team
- review/trust team
- distribution team
- agency/B2B growth team
- CRM/customer-lifecycle team
- commercial intelligence team

**The Stage 2 operating loop, once active:**
`MEASURE → detect worldwide demand → analyze competitor visibility → identify what travelers are searching for → match search demand to REAL Vilu services → identify content/service/conversion gaps → prioritize by likely bookings/revenue → owner approves material changes → Claude implements → QA → measure conversion/revenue → repeat.`

**Standing rule, effective immediately even though Stage 2 hasn't started**: the current roadmap's target markets (Russia, China, the established 11 locales, etc.) are **not permanent**. Once Stage 1 completes, Vilu must monitor demand from ALL countries globally — actual traffic, search demand, enquiries, bookings, conversion rate, and revenue are what determine which countries receive future investment, not the original phase list. Don't treat the Phase 26-31-era market-expansion decisions as a closed, final list once Stage 2 begins.

**How Phase 33 (Local SEO) feeds this loop once Stage 2 begins**: local SEO is not a one-time fix, it becomes a recurring sub-loop within the same MEASURE → ... → REPEAT cycle above — `monitor local queries (Maamigili/South Ari intent) → monitor Google Maps/Business-Profile visibility and review growth → monitor competitor listings on the same island (Koimala, White Sand Inn, Dravida, etc., per COMPETITOR_GAPS.md) → identify unmet local demand (e.g. the still-unclosed Dhigurah whale-shark search-association gap) → improve content/service connection → increase direct enquiries/bookings → repeat`. This recurring operation does not start now — same Stage-1-first rule as everything else in this section — but the loop shape is recorded here so it isn't reinvented from scratch once Stage 2 is authorized.

**The long-term commercial funnel this all serves:**
`SEARCH / DISCOVERY → VILU AUTHORITY CONTENT → REAL VILU SERVICE / PACKAGE → ENQUIRY → BOOKING → UPSELL → REPEAT / REVIEW / REFERRAL.`

**Why this is recorded here rather than left in a single session's context**: this instruction is explicitly meant to survive across every future session regardless of which phase is active, so it must live in a permanent continuity doc (this file), not just be inferred from a single directive message that will eventually scroll out of any one conversation's context.

---

## PERMANENT: E-E-A-T / editorial trust policy (added 2026-09-07, Phase 35)

This is the standing editorial standard for all Vilu-published content, not a one-time Phase 35 checklist — it governs every future page, translation, and content edit.

**Core rule — never fabricate:** No invented years-in-operation, qualifications, certifications, awards, partnerships, staff biographies, guest/review counts, media mentions, journalist endorsements, scientific/wildlife authority, or statistics. Every trust claim must be (A) already supported by Vilu-controlled evidence, (B) publicly/independently verifiable, (C) supplied directly by the owner, or (D) framed plainly as ordinary first-hand/local experience rather than specialist credential.

**Wildlife claims:** Sightings (whale shark, manta, turtle) are never guaranteed, in any language, on any page — this was already standing policy (see Identity and positioning, above) and Phase 35 extended it to cover translated marketing copy (the homepage's whale-shark/manta experience cards) and the commercial booking page (`holiday-packages.html` FAQ), not just the dedicated excursion pages.

**Sourcing standard:** Wildlife/factual claims on authority pages should cite Maldives government sources, Visit Maldives, MWSRP (`maldiveswhalesharkresearch.org`), the Maldives government Protected Areas portal (`protectedareas.environment.gov.mv`), Manta Trust/established marine-research bodies, or recognized dive-trade media (e.g. Divernet, Oceanographic Magazine) — never low-quality commercial SEO-aggregator sites. Phase 35 removed two such sources (`resortlife.travel`, `airial.travel`) from `maamigili-guide.html` and replaced them with MWSRP's own site and the official Maldives Ministry of Environment SAMPA page, reusing already-verified facts rather than researching new ones from scratch.

**Freshness dates:** `datePublished` is derived from the file's real git history, never invented. `dateModified` reflects genuine edits made that day — an honest "updated today" is fine when the content actually changed that day; a cosmetic date bump with no real edit is not.

**Authorship:** Content is attributed to Vilu Residence as an organization (`"author": {"@type": "Organization", "name": "Vilu Residence", ...}` in `Article` JSON-LD nodes), never to an invented individual expert. A named individual author is only ever used with explicit owner approval.

**Schema appropriateness:** `author`/`publisher`/`datePublished`/`dateModified` are `CreativeWork`/`Article` properties, not `LodgingBusiness`/`Product`/`TouristDestination` properties — pages needing authorship signals get a separate `Article` JSON-LD node alongside the primary entity node (the pattern `maamigili-guide.html` already established), rather than bolting non-standard fields onto the primary node.

**Local/first-hand voice vs. cited fact:** It's acceptable for local-guide content to speak from first-hand island experience for practical/logistics matters while citing external sources for objective/historical facts in the same piece — this is normal for genuine local guide content, not treated as a contradiction requiring removal of either voice.

**Commercial copy is out of scope for E-E-A-T rewrites:** Package names, IDs, prices, nights, inclusions and their marketing "hooks" are locked and were not altered under this policy, even where a first-hand-voice audit found them generic — trust-content work does not extend to unauthorized rewrites of approved commercial copy.

**Reviewed and confirmed adequate as-is (no change made, Phase 35):**
- Homepage "Who We Are" section — factual, no invented founder story or corporate fluff.
- `sameAs` entity links — Instagram, Facebook, Tripadvisor were already real/Vilu-owned; Phase 35 added the verified Google Maps CID link (`maps.google.com/?cid=8624726302398144856`, the same confirmed listing already used in the footer) as a fourth, equally verified entry. No unverified directories or competitor/platform-search URLs were added.
- No sitewide compact "trust badge" section was built — existing scattered signals (Google Maps embed, footer Maps link, real Google Reviews link, Tripadvisor links, explicit no-fabricated-testimonials dev comment already in the code, cancellation/payment-trust FAQ language) were judged adequate and restrained; adding a generic trust-badge block risked exactly the unsupportable "100% Trusted"/"#1"-style claims this policy exists to avoid.
- Multilingual trust: spot-checked German and Spanish locale dictionaries for any translation inflating "guesthouse" into "resort"/"luxury" framing — every `Resort`/`Luxus`/`lujo`-type occurrence found was legitimate guesthouse-vs-resort comparison content (the dedicated comparison page and FAQ), never a claim about Vilu itself. No exaggeration found.

**Unresolved, not blocking (owner action, carried forward again):** the legal operator name "Mexiczone" in `privacy-policy.html` predates this and every prior session (present in the original Phase 12 baseline snapshot) and has no corroborating or contradicting source anywhere in this repository. Not guessed at or altered. Continue treating this as an owner-confirmation item, not a blocker for any phase.

**Authority backlog — deliberately not executed in Phase 35:** deeper authority-building work is intentionally sequenced into later phases and must not be pulled forward: Phase 36 (backlinks/digital PR), Phase 37 (AI/AEO/entity authority), Phase 38 (image/video authority), Phase 39 (CRO/trust conversion), Phase 42 (original authority/data report), Phase 54 (search moat), and Post-56 Growth Operations (per the Growth Operating Model above) once Stage 2 is authorized.

---

## PERMANENT: entity architecture / AEO policy (added 2026-09-07, Phase 37)

**One coherent entity, not disconnected repeated nodes.** The homepage's `LodgingBusiness` and `Organization` JSON-LD nodes share the same stable `@id` (`https://viluresidence.net/#organization`) — they describe the same real business through two complementary schema.org types, not two separate entities. A `WebSite` node (`#website`) references that Organization as its `publisher`. Every Article page's `isPartOf` and every locale's `mainEntityOfPage.@id` connect back to this same graph (the latter correctly locale-prefixed per page by `build-i18n-pages.js`'s existing `localizeSelfUrl()`).

**Self-contained markup per page, not cross-page @id dereferencing.** Google's own Article guidance recommends complete, self-contained structured data on every page rather than relying on a crawler resolving a bare `{"@id": "..."}` reference to a node defined on a *different* page — cross-document JSON-LD resolution isn't part of how rich-results validation works. Same-page `@id` reuse (e.g. Organization/LodgingBusiness above) is safe and spec-correct; cross-page cross-references are not used for anything that affects rich-results eligibility. Don't "fix" this by making Article author/publisher a bare cross-page `@id` — keep them fully inlined, identical across every page.

**No corporate hierarchy beyond what's real.** "Vilu" (the aspirational broader platform brand) has no separate, launched public entity — do not create a standalone "Vilu" Organization node or `parentOrganization` relationship implying one exists. "Vilu Residence" is the one real entity. Do not add `alternateName: "Vilu"` to any Organization/LodgingBusiness node — the established policy of always saying "Vilu Residence," never bare "Vilu," in external-facing contexts (see Identity and positioning, above) is a deliberate disambiguation measure against "Sun Siyam Vilu Reef" and several other unrelated "Vilu"-branded Maldives guesthouses (Vilu Thari Inn, Vilu Rest, Vilu Beach) found during Phase 37 research — an `alternateName` would work against it.

**Citations must survive scrutiny as citations, not just as links.** A citation that happens to be technically live is not automatically good sourcing — Phase 37 found and replaced two citations that were factually harmless but a poor look: a direct link to a *competitor's own Facebook post* (as evidence for a shared local landmark — replaced with a neutral third-party Tripadvisor photo listing), and a Tripadvisor forum thread cited for a legal claim (replaced with an already-established Maldives.com source, already used elsewhere on the same page). When a single source is cited, the prose must say "one source" / name it, never imply "multiple sources" — Phase 37 also found and fixed exactly that overclaim (a citation trimmed from two sources to one during Phase 35's low-quality-source cleanup, without correcting the now-inaccurate "multiple sources" wording it had supported).

**Source attribution must distinguish government tourism promotion from independent science**, even when both are true and both are cited — Visit Maldives (the national tourism board, an interested promotional party) restating a research finding is not the same evidentiary weight as the research itself (MWSRP), and copy should read that way.

**llms.txt and similar AI-discovery files: evidence-based, not fashion-based.** Phase 37 researched the real 2026 adoption/evidence state of `llms.txt` — real but low (~10% of domains), with no major AI provider (OpenAI, Anthropic, Google, Perplexity) confirming it's read for citation/inference decisions, and one study finding its presence adds noise rather than signal for predicting AI citation. Its confirmed value is for developer/API-product sites serving coding-assistant/RAG audiences, not consumer hospitality sites. Do not add it (or a similar speculative AI-discovery file) without new, real evidence of adoption by systems actually relevant to Vilu's audience — re-research before revisiting, don't assume the 2026 finding is permanent.

**Known external, not-Vilu-controlled entity finding (Phase 37, document only — never edit external services):** `viluresidence.com` (a separate, live legacy/parallel domain, different positioning copy — "Best Budget Guesthouse" vs. this site's "Boutique guesthouse") is the domain external OTA backlinks and search snippets overwhelmingly point to, not `viluresidence.net` (this repository's site). This is a real entity/authority-splitting risk worth the owner's attention, but `viluresidence.com` is a protected, hands-off external system — do not attempt to edit, redirect, or de-index it from here.

---

## PERMANENT: image/video SEO policy (added 2026-09-07, Phase 38)

**Reuse established, real alt text before inventing new alt text.** When the same image file is used in multiple places, its alt text should describe the same real photo consistently, not be reinvented per placement. Phase 38 closed a ~50-instance backlog where `related-card-img` alt text repeated the *linked page's title* (e.g. `alt="Holiday Packages"`) instead of describing the photo — the fix reused whichever genuine visual description already existed for that exact file elsewhere on the site (e.g. via the `PKG_IMAGES` map), only writing a new description when none existed anywhere. Never write alt text implying a guaranteed sighting, private encounter, or unverified individual-animal identity.

**Redundant-with-adjacent-heading images may get an empty alt — but only when they carry no unique visual information.** The `related-card` pattern places an image directly beside an `<h3>` with matching link text; a real photo there still gets a genuine descriptive alt (it adds real information beyond the title), but a generic brand logo used as a placeholder thumbnail (no dedicated photo exists for that topic) correctly gets `alt=""`, since repeating "Vilu Residence" there would just double-announce the same text and would also misrepresent what the link is actually about.

**Localize alt text using `data-i18n-alt`, not just page prose.** `build-i18n-pages.js` already resolves `data-i18n-alt="key"` from `dict.static` per locale (`applyStaticTranslations()`) — the exact same mechanism as `data-i18n`/`data-i18n-placeholder`/`data-i18n-aria-label`. Any new meaningful image alt text added to a standalone page must use this, or it silently ships English-only to all 11 locale mirrors (exactly the defect Phase 38 fixed) — this class of bug won't show up in a build-warning, so must be checked for directly (see `test/media-seo.test.js`).

**Never leave a Firebase Storage tokenized URL (`firebasestorage.googleapis.com/...?token=...`) in shipped structured data when a stable `/images/...` equivalent exists.** Tokens can be rotated/invalidated by admins outside this repository's control, silently breaking the reference; a `/images/` asset committed to this repo is guaranteed stable. Phase 38 found and fixed one in the homepage `LodgingBusiness.image` array (flagged since Phase 33), replacing it with the matching already-shipped `/images/rooms/...` photo of the same room.

**VideoObject schema requires a real, distinct, watchable video experience — not a decorative background loop.** Google requires VideoObject's video be ≥30 seconds and requires the page to genuinely function as "watch this video" (name/thumbnailUrl/uploadDate required, matching what's actually true and visible). Vilu's homepage hero is a muted, autoplaying, looping ~10-second ambient background clip behind hero text — both too short for eligibility and semantically the wrong kind of content for the schema. Do not add VideoObject to it. Only add VideoObject to a future video that is genuinely presented as a standalone watchable piece (see the YouTube/distribution note below) with real, non-invented `duration`/`uploadDate`.

**Video/YouTube distribution is a real, currently-open opportunity — but it's Growth Operations territory, not a build-now item.** Phase 38 research found neither Vilu nor any of its direct Maamigili-area competitors (Whale Shark Inn, Shamar Guest House, Koimala Inn, La Cabana Maldives) has a confirmed, substantive YouTube/video presence — this is a genuinely open channel, not one Vilu would be racing established players to enter. Recorded here as backlog for whenever Stage 2 (post-56) growth work is authorized: a Maamigili cinematic guide, whale-shark education/expectations video, "how to reach Maamigili" walkthrough, a real room tour, a South Ari itinerary video, and a package explainer. Do not create any account or publish any video for this now — document only, per this phase's explicit scope.

**Image-search visibility is currently zero, confirmed by direct GSC evidence (not inferred)** — 0 clicks / 0 impressions on Image search over the last 3 months, checked directly in Search Console. This is expected for a low-overall-traffic site and is not itself a defect; the alt-text/stable-URL/clean-sitemap work in Phase 38 is the necessary foundation for future image visibility, not something that shows results immediately. Don't treat a lack of image-search traffic as evidence something is still broken once the on-page fundamentals (this policy) are met.
